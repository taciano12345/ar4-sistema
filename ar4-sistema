<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AR4 — Sistema de Indenizações</title>
<meta name="theme-color" content="#0c0c0c">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="AR4">
<meta name="mobile-web-app-capable" content="yes">
<link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg viewBox=%220 0 200 230%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cpath d=%22M100 8 L185 42 L185 130 Q185 185 100 222 Q15 185 15 130 L15 42 Z%22 fill=%22%230c0c0c%22 stroke=%22%23F5C518%22 stroke-width=%224%22/%3E%3Cpath d=%22M100 22 L172 52 L172 128 Q172 175 100 208 Q28 175 28 128 L28 52 Z%22 fill=%22%23141414%22/%3E%3Ctext x=%22100%22 y=%22108%22 font-size=%2228%22 fill=%22%23F5C518%22 font-family=%22Arial%22 font-weight=%22900%22 text-anchor=%22middle%22%3EAR-4%3C/text%3E%3Ctext x=%22100%22 y=%22160%22 font-size=%229.5%22 fill=%22%23F5C518%22 font-family=%22Arial%22 font-weight=%22700%22 text-anchor=%22middle%22%3EPROTECAO VEICULAR%3C/text%3E%3C/svg%3E">
<link rel="manifest" id="pwaManifest">
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{
  /* ── Cores base ── */
  --bg:#080a0f;
  --bg2:#0d1017;
  --bg3:#12161f;
  --bg4:#1a1f2e;
  --bg5:#252b3b;
  --bg6:#2e3548;

  /* ── Amarelo AR4 ── */
  --y:#F0B429;
  --y2:#c9920f;
  --y3:rgba(240,180,41,.08);
  --y4:rgba(240,180,41,.04);
  --yg:linear-gradient(135deg,#F0B429,#f5c518);

  /* ── Texto ── */
  --w:#f8fafc;
  --w2:#cbd5e1;
  --w3:#64748b;
  --w4:#334155;

  /* ── Status ── */
  --green:#10b981;--green2:rgba(16,185,129,.12);
  --red:#ef4444;--red2:rgba(239,68,68,.1);
  --orange:#f59e0b;--orange2:rgba(245,158,11,.1);
  --blue:#3b82f6;--blue2:rgba(59,130,246,.1);
  --purple:#8b5cf6;--purple2:rgba(139,92,246,.1);

  /* ── Glass ── */
  --glass:rgba(255,255,255,.03);
  --glass2:rgba(255,255,255,.06);
  --glass-border:rgba(255,255,255,.07);
  --glass-border2:rgba(255,255,255,.12);

  /* ── Shadows ── */
  --shadow-sm:0 1px 3px rgba(0,0,0,.4);
  --shadow:0 4px 16px rgba(0,0,0,.5);
  --shadow-lg:0 12px 40px rgba(0,0,0,.6);
  --shadow-y:0 0 30px rgba(240,180,41,.15);

  /* ── Radius ── */
  --r:8px;--r2:12px;--r3:16px;--r4:20px;
}

html,body{height:100%;height:100dvh;background:var(--bg);color:var(--w);font-family:'IBM Plex Sans',sans-serif;font-size:14px;overflow:hidden;line-height:1.5;-webkit-font-smoothing:antialiased}
::-webkit-scrollbar{width:3px;height:3px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:var(--bg5);border-radius:2px}

/* ── Noise texture overlay ── */
body::before{
  content:'';position:fixed;inset:0;
  pointer-events:none;z-index:0;opacity:0;
}

/* ── Ambient glow ── */
body::after{
  content:'';position:fixed;top:-20%;left:50%;transform:translateX(-50%);
  width:600px;height:400px;
  background:radial-gradient(ellipse,rgba(240,180,41,.04) 0%,transparent 70%);
  pointer-events:none;z-index:0;
  overflow:hidden;
}

/* ── Animations ── */
@keyframes fadeUp{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes shimmer{0%,100%{opacity:.5}50%{opacity:1}}
@keyframes spin{to{transform:rotate(360deg)}}
@keyframes glow{0%,100%{box-shadow:0 0 20px rgba(240,180,41,.1)}50%{box-shadow:0 0 40px rgba(240,180,41,.25)}}
@keyframes slideUp{from{transform:translateY(100%);opacity:0}to{transform:translateY(0);opacity:1}}
.au{animation:fadeUp .35s cubic-bezier(.22,1,.36,1) both}
.au1{animation:fadeUp .35s .06s cubic-bezier(.22,1,.36,1) both}
.au2{animation:fadeUp .35s .12s cubic-bezier(.22,1,.36,1) both}
.au3{animation:fadeUp .35s .18s cubic-bezier(.22,1,.36,1) both}
.au4{animation:fadeUp .35s .24s cubic-bezier(.22,1,.36,1) both}

/* ══════════════ LOGIN ══════════════ */
#ls{
  position:fixed;inset:0;display:flex;align-items:center;justify-content:center;
  background:var(--bg);z-index:1000;padding:20px;overflow-y:auto;
}
.lw{width:100%;max-width:900px;margin:auto}
#ls>div{
  display:flex;min-height:520px;border-radius:var(--r4);overflow:hidden;
  box-shadow:var(--shadow-lg),0 0 80px rgba(240,180,41,.05);
  border:1px solid var(--glass-border);
  backdrop-filter:blur(20px);
}

/* Left panel */
#ls>div>div:first-child{
  flex:1;padding:50px;
  background:linear-gradient(160deg,rgba(240,180,41,.06) 0%,rgba(240,180,41,.01) 40%,var(--bg3) 100%);
  border-right:1px solid var(--glass-border);
  display:flex;flex-direction:column;justify-content:space-between;
}
/* Right panel */
#ls>div>div:last-child{
  width:360px;flex-shrink:0;
  background:var(--glass);
  backdrop-filter:blur(40px);
  padding:50px 44px;
  display:flex;flex-direction:column;justify-content:center;
}

.lsh{
  width:52px;height:52px;background:var(--yg);
  clip-path:polygon(50% 0%,100% 20%,100% 65%,50% 100%,0% 65%,0% 20%);
  display:inline-flex;align-items:center;justify-content:center;
  font-weight:800;font-size:14px;color:#000;margin-bottom:10px;
  filter:drop-shadow(0 0 20px rgba(240,180,41,.4));
}
.lb h1{font-size:30px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1.2;margin-bottom:10px}
.lb p{font-size:13px;color:var(--w3);line-height:1.7}

.lc h2{font-size:22px;font-weight:700;color:var(--w);letter-spacing:-.3px;margin-bottom:6px}
.lc>p{font-size:13px;color:var(--w3);margin-bottom:28px}

/* Role buttons */
.rg{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:20px}
.rb{
  padding:12px 10px;border-radius:var(--r);
  border:1px solid var(--glass-border2);
  background:var(--glass);backdrop-filter:blur(10px);
  color:var(--w3);cursor:pointer;text-align:center;
  transition:all .2s;font-size:12px;font-weight:500;
  font-family:'IBM Plex Sans',sans-serif;
}
.rb svg{display:block;margin:0 auto 7px;width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:1.75}
.rb:hover{border-color:rgba(240,180,41,.3);color:var(--w2);background:var(--glass2)}
.rb.on{border-color:var(--y);color:var(--y);background:var(--y3);box-shadow:0 0 20px rgba(240,180,41,.1)}

.pinp{
  width:100%;padding:11px 14px;
  background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border2);border-radius:var(--r);
  color:var(--w);font-size:13px;font-family:'IBM Plex Sans',sans-serif;
  transition:all .2s;margin-bottom:12px;
}
.pinp:focus{outline:none;border-color:rgba(240,180,41,.5);box-shadow:0 0 0 3px rgba(240,180,41,.08)}
.pinp::placeholder{color:var(--w4)}

.lbtn{
  width:100%;padding:13px;
  background:var(--yg);color:#000;border:none;
  border-radius:var(--r);font-family:'IBM Plex Sans',sans-serif;
  font-weight:700;font-size:14px;cursor:pointer;
  transition:all .2s;letter-spacing:.2px;
  box-shadow:0 4px 20px rgba(240,180,41,.3);
}
.lbtn:hover{transform:translateY(-1px);box-shadow:0 6px 28px rgba(240,180,41,.4)}
.lbtn:active{transform:translateY(0)}
.lhint{text-align:center;font-size:11px;color:var(--w4);margin-top:12px}

/* ══════════════ SHELL ══════════════ */
#app{display:none;height:100dvh;height:100vh;overflow:hidden;position:relative;z-index:1}
.lay{display:flex;height:100dvh;height:100vh;overflow:hidden;position:relative}

/* ══════════════ SIDEBAR ══════════════ */
.sb{
  width:220px;flex-shrink:0;
  background:linear-gradient(180deg,var(--bg2) 0%,var(--bg) 100%);
  border-right:1px solid var(--glass-border);
  display:flex;flex-direction:column;
  backdrop-filter:blur(20px);
  position:relative;z-index:10;
  overflow:hidden;
}
.sbt{
  padding:22px 18px;display:flex;align-items:center;gap:12px;
  border-bottom:1px solid var(--glass-border);
}
.sbsh{
  width:32px;height:32px;background:var(--yg);flex-shrink:0;
  clip-path:polygon(50% 0%,100% 20%,100% 65%,50% 100%,0% 65%,0% 20%);
  display:flex;align-items:center;justify-content:center;
  font-weight:800;font-size:10px;color:#000;
  filter:drop-shadow(0 0 8px rgba(240,180,41,.3));
  overflow:hidden;
}
.sbb strong{display:block;font-size:13px;font-weight:700;color:var(--w);letter-spacing:-.1px}
.sbb span{font-size:10px;color:var(--w3)}

.sbn{flex:1;padding:10px 0;overflow-y:auto;overflow-x:hidden;min-height:0}
.sbsec{
  padding:14px 16px 4px;font-size:10px;font-weight:600;
  color:var(--w4);text-transform:uppercase;letter-spacing:.8px;
}
.ni{
  display:flex;align-items:center;gap:9px;
  padding:9px 16px;color:var(--w3);cursor:pointer;
  font-size:12px;font-weight:400;transition:all .15s;
  border-left:2px solid transparent;margin:1px 8px;
  border-radius:var(--r);
}
.ni svg{width:15px;height:15px;stroke:currentColor;fill:none;stroke-width:1.75;flex-shrink:0}
.ni:hover{color:var(--w2);background:var(--glass2)}
.ni.on{
  color:var(--y);background:var(--y3);
  border-left-color:var(--y);font-weight:500;
}
.nbadge{
  margin-left:auto;background:var(--orange);
  color:#000;font-size:9px;font-weight:700;
  padding:1px 6px;border-radius:10px;
}
.sbft{border-top:1px solid var(--glass-border);flex-shrink:0}
.sbft-desk{padding:10px 14px;display:flex;flex-direction:column;gap:0}
.sbft-mob{display:none}
.sbu{display:flex;align-items:center;gap:9px;margin-bottom:10px}
.sbav{
  width:32px;height:32px;border-radius:50%;
  background:var(--yg);display:flex;align-items:center;
  justify-content:center;font-weight:700;font-size:11px;color:#000;flex-shrink:0;
  box-shadow:0 0 12px rgba(240,180,41,.3);
}
.sbui strong{display:block;font-size:12px;font-weight:600;color:var(--w)}
.sbui span{font-size:10px;color:var(--w3)}
.outbtn{
  width:100%;padding:7px;background:var(--glass);
  border:1px solid var(--glass-border);border-radius:var(--r);
  color:var(--w3);font-size:11px;font-family:'IBM Plex Sans',sans-serif;
  cursor:pointer;transition:all .15s;
  display:flex;align-items:center;justify-content:center;gap:5px;
  backdrop-filter:blur(10px);box-sizing:border-box;
}
.outbtn svg{width:12px;height:12px;stroke:currentColor;fill:none;stroke-width:2}
.outbtn:hover{color:var(--red);border-color:rgba(239,68,68,.3)}

/* ══════════════ MAIN ══════════════ */
.main{flex:1;overflow-y:auto;overflow-x:hidden;background:var(--bg);overscroll-behavior:contain;-webkit-overflow-scrolling:touch}
.page{display:none;padding:28px 28px}
.page.on{display:block}

/* ══════════════ TOPBAR ══════════════ */
.tb{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:24px}
.tb h1{font-size:24px;font-weight:700;color:var(--w);letter-spacing:-.4px}
.tb p{font-size:12px;color:var(--w3);margin-top:3px}
.tbr{display:flex;align-items:center;gap:8px}
.pbtn{
  padding:8px 14px;background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border2);border-radius:var(--r);
  color:var(--w2);font-size:12px;cursor:pointer;
  display:flex;align-items:center;gap:5px;transition:all .18s;
  font-family:'IBM Plex Sans',sans-serif;
}
.pbtn svg{width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:2}
.pbtn:hover{border-color:rgba(240,180,41,.3);color:var(--w)}

/* ══════════════ KPI CARDS ══════════════ */
.kg{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:12px;margin-bottom:20px}
.kpi{
  background:var(--glass);backdrop-filter:blur(20px);
  border:1px solid var(--glass-border);border-radius:var(--r2);
  padding:16px;transition:all .2s;position:relative;overflow:hidden;
}
.kpi::before{
  content:'';position:absolute;inset:0;
  background:linear-gradient(135deg,var(--glass2) 0%,transparent 60%);
  pointer-events:none;
}
.kpi:hover{border-color:var(--glass-border2);transform:translateY(-1px);box-shadow:var(--shadow)}
.kpi.ky{
  background:linear-gradient(135deg,rgba(240,180,41,.15),rgba(240,180,41,.05));
  border-color:rgba(240,180,41,.25);
}
.kpi.ky::before{background:none}
.kpi.ky .kl{color:rgba(240,180,41,.7)}
.kpi.ky .kv{color:var(--y)}
.kpi.ky .ks{color:rgba(240,180,41,.5)}
.kl{font-size:10px;color:var(--w3);text-transform:uppercase;letter-spacing:.6px;margin-bottom:5px;font-weight:600}
.kv{font-size:20px;font-weight:700;letter-spacing:-.4px;font-family:'IBM Plex Mono',monospace;color:var(--w)}
.ks{font-size:11px;color:var(--w3);margin-top:3px}
.ks.up{color:var(--green)}
.ks.warn{color:var(--orange)}

/* ══════════════ CARDS ══════════════ */
.card{
  background:var(--glass);backdrop-filter:blur(20px);
  border:1px solid var(--glass-border);border-radius:var(--r2);padding:18px;
  position:relative;overflow:hidden;
}
.card::before{
  content:'';position:absolute;top:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,transparent,var(--glass-border2),transparent);
}
.ch{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px}
.ct{font-size:11px;font-weight:600;color:var(--w3);text-transform:uppercase;letter-spacing:.6px}

/* ══════════════ TABLES ══════════════ */
.tw{overflow-x:auto}
.dt{width:100%;border-collapse:collapse;font-size:13px}
.dt th{
  text-align:left;padding:9px 12px;color:var(--w4);
  font-weight:600;font-size:10px;text-transform:uppercase;
  letter-spacing:.5px;border-bottom:1px solid var(--glass-border);
}
.dt td{padding:10px 12px;border-bottom:1px solid rgba(255,255,255,.03);vertical-align:middle}
.dt tr:last-child td{border-bottom:none}
.dt tbody tr{cursor:pointer;transition:background .1s}
.dt tbody tr:hover td{background:var(--glass)}
.nc{font-weight:500;color:var(--w)}
.vc{font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);text-align:right;font-size:13px}
.mc{color:var(--w3)}

/* ══════════════ BADGES ══════════════ */
.badge{
  padding:3px 9px;border-radius:20px;font-size:10px;
  font-weight:600;letter-spacing:.3px;display:inline-block;
  border:1px solid transparent;
}
.bp{background:var(--green2);color:var(--green);border-color:rgba(16,185,129,.2)}
.bpend{background:var(--orange2);color:var(--orange);border-color:rgba(245,158,11,.2)}
.bf{background:var(--purple2);color:var(--purple);border-color:rgba(139,92,246,.2)}
.bdc{background:var(--blue2);color:var(--blue);border-color:rgba(59,130,246,.2)}
.bvr{background:var(--y3);color:var(--y);border-color:rgba(240,180,41,.2)}
.bfor{background:var(--red2);color:var(--red);border-color:rgba(239,68,68,.2)}

/* ══════════════ TABS ══════════════ */
.tabs{display:flex;gap:4px}
.tab{
  padding:5px 13px;border-radius:var(--r);font-size:12px;font-weight:500;
  cursor:pointer;border:1px solid var(--glass-border);
  color:var(--w3);background:var(--glass);transition:all .15s;
  font-family:'IBM Plex Sans',sans-serif;backdrop-filter:blur(10px);
}
.tab:hover{color:var(--w2);border-color:var(--glass-border2)}
.tab.on{background:var(--y3);color:var(--y);border-color:rgba(240,180,41,.3);font-weight:600}

/* ══════════════ BUTTONS ══════════════ */
.btn{
  padding:8px 16px;border-radius:var(--r);font-size:13px;font-weight:500;
  cursor:pointer;border:none;transition:all .18s;
  font-family:'IBM Plex Sans',sans-serif;
  display:inline-flex;align-items:center;gap:6px;
}
.btn svg{width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:2}
.btn-p{
  background:var(--yg);color:#000;font-weight:600;
  box-shadow:0 2px 12px rgba(240,180,41,.25);
}
.btn-p:hover{transform:translateY(-1px);box-shadow:0 4px 20px rgba(240,180,41,.35)}
.btn-g{
  background:var(--glass);color:var(--w3);
  border:1px solid var(--glass-border2);backdrop-filter:blur(10px);
}
.btn-g:hover{color:var(--w2);border-color:var(--glass-border2)}
.btn-yg{background:var(--y3);color:var(--y);border:1px solid rgba(240,180,41,.2)}
.btn-yg:hover{background:rgba(240,180,41,.14)}
.btn-d{background:var(--red2);color:var(--red);border:1px solid rgba(239,68,68,.2)}
.btn-d:hover{background:rgba(239,68,68,.18)}
.btn-sm{padding:5px 12px;font-size:12px}

/* ══════════════ CALENDAR ══════════════ */
.cg{display:grid;grid-template-columns:repeat(7,minmax(44px,1fr));gap:3px}
.ch2{
  text-align:center;padding:7px 0;font-size:10px;
  font-weight:700;color:var(--w4);text-transform:uppercase;letter-spacing:.5px;
}
.cc{
  background:var(--glass);backdrop-filter:blur(10px);
  border-radius:var(--r);padding:5px;min-height:68px;
  cursor:pointer;transition:all .15s;border:1px solid var(--glass-border);
  overflow:hidden;min-width:0;
}
.cc:hover{background:var(--glass2);border-color:var(--glass-border2)}
.cc.oth{opacity:.15;pointer-events:none}
.cc.hp{border-color:rgba(240,180,41,.25);background:var(--y4)}
.cc.ih{background:var(--purple2);border-color:rgba(139,92,246,.2)}
.cc.today{border-color:var(--y);box-shadow:0 0 16px rgba(240,180,41,.15)}
.cn{font-size:11px;font-weight:700;color:var(--w4);margin-bottom:3px;line-height:1}
.cp{border-radius:3px;padding:1px 3px;margin-bottom:1px;font-size:8px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:100%;display:block}
.cpp{background:var(--green2);color:var(--green)}
.cpd{background:var(--orange2);color:var(--orange)}
.cpf{background:var(--purple2);color:var(--purple)}
.cpb{background:var(--blue2);color:var(--blue)}
.cpvr{background:var(--y3);color:var(--y)}

/* ══════════════ MODAL ══════════════ */
.mbk{
  position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:500;
  display:none;align-items:center;justify-content:center;padding:16px;
  backdrop-filter:blur(8px);
}
.mbk.on{display:flex}
.mbox{
  background:linear-gradient(160deg,var(--bg3),var(--bg2));
  border:1px solid var(--glass-border2);border-radius:var(--r4);
  width:100%;max-width:460px;max-height:92vh;overflow-y:auto;
  animation:fadeUp .25s cubic-bezier(.22,1,.36,1);
  box-shadow:var(--shadow-lg);
  backdrop-filter:blur(40px);
}
.mbox::before{
  content:'';display:block;width:36px;height:4px;
  background:var(--bg5);border-radius:2px;margin:12px auto 0;
}
.mh{display:flex;justify-content:space-between;align-items:flex-start;padding:16px 20px 0}
.mh h2{font-size:16px;font-weight:700;color:var(--w);letter-spacing:-.2px;flex:1;padding-right:10px;line-height:1.4}
.mclose{
  width:28px;height:28px;background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border);border-radius:var(--r);
  color:var(--w3);cursor:pointer;display:flex;align-items:center;justify-content:center;
  flex-shrink:0;transition:all .15s;
}
.mclose svg{width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:2.5}
.mclose:hover{background:var(--glass2);color:var(--w);border-color:var(--glass-border2)}
.mb{padding:14px 20px 22px}
.dg{
  background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border);border-radius:var(--r);
  padding:13px;margin-bottom:10px;
}
.dg h4{font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.7px;color:var(--w4);margin-bottom:9px}
.dr{display:flex;justify-content:space-between;align-items:flex-start;padding:5px 0;border-bottom:1px solid rgba(255,255,255,.04);font-size:13px}
.dr:last-child{border-bottom:none}
.dr .dl{color:var(--w3);flex-shrink:0;margin-right:10px}
.dr .dv{font-weight:500;color:var(--w);text-align:right}
.dr .dv.y{color:var(--y);font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:16px}
.dr .dv.r{color:var(--red);font-family:'IBM Plex Mono',monospace}
.ctot{
  background:var(--y3);border:1px solid rgba(240,180,41,.2);
  border-radius:var(--r);padding:12px 14px;
  display:flex;justify-content:space-between;align-items:center;margin-top:10px;
}
.ctot .cl{font-weight:600;color:var(--w);font-size:13px}
.ctot .cv{font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:20px;color:var(--y)}

/* ══════════════ ACUMULADO ══════════════ */
.ah{
  background:linear-gradient(135deg,rgba(240,180,41,.08),rgba(240,180,41,.02));
  border:1px solid rgba(240,180,41,.15);border-radius:var(--r4);
  padding:30px;text-align:center;margin-bottom:16px;
  position:relative;overflow:hidden;
}
.ah::before{
  content:'';position:absolute;top:-40px;left:50%;transform:translateX(-50%);
  width:300px;height:200px;
  background:radial-gradient(ellipse,rgba(240,180,41,.12) 0%,transparent 70%);
  pointer-events:none;
}
.ah .al{font-size:10px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;margin-bottom:8px;font-weight:600}
.ah .at{font-family:'IBM Plex Mono',monospace;font-size:40px;font-weight:700;color:var(--y);letter-spacing:-1.5px;line-height:1.1}
.ah .as{font-size:13px;color:var(--w3);margin-top:6px}
.hr{
  display:flex;align-items:center;justify-content:space-between;
  padding:12px 14px;
  background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border);border-radius:var(--r);
  margin-bottom:5px;cursor:pointer;transition:all .15s;
}
.hr:hover{background:var(--glass2);border-color:var(--glass-border2)}
.hr .hm{font-size:13px;font-weight:600;color:var(--w)}
.hr .hp{font-size:11px;color:var(--w3)}
.hr .hv{font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:14px;color:var(--y)}
.hr .hb{height:2px;background:var(--bg5);border-radius:2px;margin-top:5px;width:100px}
.hr .hbf{height:100%;background:var(--y);border-radius:2px;transition:width .5s}

/* ══════════════ FORMS ══════════════ */
.fc{
  background:var(--glass);backdrop-filter:blur(20px);
  border:1px solid var(--glass-border);border-radius:var(--r2);padding:20px;
}
.fg{display:grid;grid-template-columns:1fr 1fr;gap:13px}
.fgr{display:flex;flex-direction:column;gap:5px}
.fgr.fw{grid-column:1/-1}
.fl{font-size:10px;font-weight:600;color:var(--w3);text-transform:uppercase;letter-spacing:.4px}
.fi,.fs,.fta{
  padding:10px 13px;
  background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border2);border-radius:var(--r);
  color:var(--w);font-size:13px;font-family:'IBM Plex Sans',sans-serif;
  transition:all .18s;width:100%;
}
.fi:focus,.fs:focus,.fta:focus{
  outline:none;border-color:rgba(240,180,41,.4);
  box-shadow:0 0 0 3px rgba(240,180,41,.08);
  background:var(--glass2);
}
.fi::placeholder,.fta::placeholder{color:var(--w4)}
.fs option{background:var(--bg3)}
.fta{resize:vertical;min-height:65px}
.fa{display:flex;gap:8px;margin-top:18px;padding-top:16px;border-top:1px solid var(--glass-border)}

/* ══════════════ UPLOAD ZONE ══════════════ */
.upz{
  border:1.5px dashed var(--glass-border2);border-radius:var(--r2);
  padding:30px 20px;text-align:center;cursor:pointer;
  transition:all .2s;
  background:var(--glass);backdrop-filter:blur(10px);
  position:relative;
}
.upz:hover,.upz.drag{
  border-color:rgba(240,180,41,.4);
  background:var(--y4);
  box-shadow:0 0 30px rgba(240,180,41,.08);
}
.upz input{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%}
.upz svg{width:32px;height:32px;stroke:var(--w4);fill:none;stroke-width:1.5;margin:0 auto 10px;display:block;transition:stroke .2s}
.upz .ut{font-size:13px;font-weight:500;color:var(--w2);transition:color .2s}
.upz .us{font-size:11px;color:var(--w3);margin-top:4px}
.upz:hover svg,.upz.drag svg{stroke:var(--y)}
.upz:hover .ut,.upz.drag .ut{color:var(--y)}

/* ══════════════ PREVIEW ITEMS ══════════════ */
.pitem{
  display:flex;align-items:center;justify-content:space-between;
  padding:9px 12px;
  background:var(--glass);backdrop-filter:blur(10px);
  border:1px solid var(--glass-border);border-radius:var(--r);
  margin-bottom:4px;font-size:12px;gap:8px;
}
.pitem .pin{font-weight:500;color:var(--w);flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.pitem .pdt{color:var(--w3);flex-shrink:0;font-size:11px}
.pitem .pv{font-family:'IBM Plex Mono',monospace;color:var(--y);font-weight:600;flex-shrink:0}
.pitem .pb{flex-shrink:0}

/* ══════════════ ROW ACTIONS ══════════════ */
.ra{display:flex;gap:5px;justify-content:flex-end}
.rab{
  width:28px;height:28px;border-radius:var(--r);border:none;
  cursor:pointer;display:flex;align-items:center;justify-content:center;
  transition:all .15s;backdrop-filter:blur(10px);
}
.rab svg{width:12px;height:12px;stroke:currentColor;fill:none;stroke-width:2}
.re{background:var(--y3);color:var(--y);border:1px solid rgba(240,180,41,.2)}
.re:hover{background:rgba(240,180,41,.15)}
.rd{background:var(--red2);color:var(--red);border:1px solid rgba(239,68,68,.15)}
.rd:hover{background:rgba(239,68,68,.18)}

/* ══════════════ PROCESSING ══════════════ */
.proc{text-align:center;padding:32px 20px}
.spin{
  width:36px;height:36px;border:2px solid var(--glass-border);
  border-top-color:var(--y);border-radius:50%;
  animation:spin 1s linear infinite;margin:0 auto 14px;
}
.proc p{font-size:13px;color:var(--w2);font-weight:500}
.proc span{font-size:11px;color:var(--w3);display:block;margin-top:4px}

/* ══════════════ INFO BOX ══════════════ */
.info-box{
  background:var(--glass);border:1px solid var(--glass-border);
  border-radius:var(--r);padding:11px 13px;
  font-size:11px;color:var(--w3);line-height:1.7;margin-top:12px;
}
.info-box strong{color:var(--w2);display:block;margin-bottom:3px}

/* ══════════════ FORCE PASSWORD ══════════════ */
#forcePassScreen{position:fixed;inset:0;background:rgba(0,0,0,.85);z-index:5000;display:none;align-items:center;justify-content:center;padding:20px;backdrop-filter:blur(12px)}
#forcePassScreen.show{display:flex}

/* ══════════════ TIMEOUT WARNING ══════════════ */
#timeoutWarn{position:fixed;bottom:20px;left:50%;transform:translateX(-50%);background:var(--bg3);border:1px solid rgba(245,158,11,.3);border-radius:var(--r2);padding:12px 20px;z-index:8000;display:none;align-items:center;gap:12px;font-size:13px;box-shadow:var(--shadow-lg);backdrop-filter:blur(20px)}
#timeoutWarn.show{display:flex}
#timeoutWarn svg{width:16px;height:16px;stroke:var(--orange);fill:none;stroke-width:2;flex-shrink:0}
#timeoutCount{font-weight:700;color:var(--orange)}

/* ══════════════ SPLASH ══════════════ */
#splash{position:fixed;inset:0;background:var(--bg);z-index:9999;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:0;transition:opacity .7s ease}
#splash.hide{opacity:0;pointer-events:none}
.splash-logo{width:140px;height:auto;filter:drop-shadow(0 0 40px rgba(240,180,41,.5));animation:splashPop .7s cubic-bezier(.34,1.56,.64,1) both}
.splash-bar{width:180px;height:2px;background:var(--bg4);border-radius:2px;margin-top:36px;overflow:hidden}
.splash-fill{height:100%;background:var(--yg);border-radius:2px;animation:splashLoad 2.8s ease forwards}
.splash-txt{font-size:11px;color:var(--w3);margin-top:14px;letter-spacing:.5px}
@keyframes splashPop{from{opacity:0;transform:scale(.7)}to{opacity:1;transform:scale(1)}}
@keyframes splashLoad{0%{width:0}60%{width:75%}90%{width:92%}100%{width:100%}}

/* ══════════════ THEME TOGGLE ══════════════ */
body.light{
  --bg:#f0f2f5;--bg2:#ffffff;--bg3:#f5f5f5;--bg4:#e8e8e8;--bg5:#d0d0d0;--bg6:#c0c5cf;
  --w:#0f172a;--w2:#334155;--w3:#64748b;--w4:#94a3b8;
  --glass:rgba(255,255,255,.85);--glass2:rgba(255,255,255,.95);
  --glass-border:rgba(0,0,0,.08);--glass-border2:rgba(0,0,0,.12);
  --y:#c9920f;--y2:#a67808;--y3:rgba(201,146,15,.12);--y4:rgba(201,146,15,.06);
}
body.light .sb{background:#ffffff;border-top:1px solid #e0e0e0!important;border-right:1px solid #e0e0e0}
body.light .ni{color:#64748b}
body.light .ni.on{color:#c9920f;background:rgba(201,146,15,.1)}
body.light .sbt{border-bottom:1px solid rgba(0,0,0,.08)}
body.light .sbft{border-top:1px solid rgba(0,0,0,.08)}
body.light::before,body.light::after{display:none}

/* ══════════════ MISC ══════════════ */
.div{height:1px;background:var(--glass-border);margin:12px 0}
.shb{display:inline-flex;padding:2px 9px;background:var(--y3);border:1px solid rgba(240,180,41,.2);border-radius:20px;font-size:10px;font-weight:600;color:var(--y)}
.cp2{background:var(--glass);border:1px solid var(--glass-border);border-radius:var(--r);padding:10px 12px;margin-top:10px;font-size:11px;backdrop-filter:blur(10px)}
.cp2 .cl{display:flex;justify-content:space-between;padding:2px 0;color:var(--w3)}
.cp2 .cl.tot{color:var(--y);font-weight:600;font-family:'IBM Plex Mono',monospace;border-top:1px solid var(--glass-border);margin-top:3px;padding-top:5px}

/* ══════════════ MOBILE ══════════════ */
@media (max-width: 768px) {
  html,body{overflow:auto;-webkit-text-size-adjust:100%}

  /* Login */
  #ls{padding:0;display:block;overflow-y:auto}
  #ls>div{flex-direction:column!important;min-height:100dvh!important;max-width:100%!important;border-radius:0!important;border:none!important;box-shadow:none!important}
  #ls>div>div:first-child{padding:36px 24px 28px!important;border-right:none!important;border-bottom:1px solid var(--glass-border)!important}
  #ls>div>div:first-child img{width:90px!important}
  #ls>div>div:first-child h1,.lb h1{font-size:22px!important}
  #ls>div>div:last-child{width:100%!important;padding:28px 24px 40px!important;flex:1}

  /* App shell */
  #app{height:100dvh;overflow:hidden}
  .lay{display:flex;flex-direction:column;height:100dvh;overflow:hidden}
  .main{flex:1;overflow-y:auto;overflow-x:hidden;order:1;height:0;-webkit-overflow-scrolling:touch;overscroll-behavior-y:contain;padding-bottom:90px}
  .page{padding:14px 14px 20px}

  /* Bottom navigation bar — fixed to bottom */
  .sb{
    position:fixed!important;
    bottom:0;left:0;right:0;
    width:100%!important;height:auto!important;
    flex-direction:row!important;display:flex!important;
    border-right:none!important;
    border-top:1px solid var(--glass-border)!important;
    background:var(--bg2)!important;
    z-index:100;
    padding-bottom:env(safe-area-inset-bottom,0);
    backdrop-filter:blur(20px);
    -webkit-backdrop-filter:blur(20px);
  }
  .sbt{display:none!important}
  .sbn{
    display:flex!important;flex-direction:row!important;
    padding:0;flex:1;
    overflow-x:auto;overflow-y:hidden;
    align-items:stretch;
    scrollbar-width:none;
  }
  .sbn::-webkit-scrollbar{display:none}
  .sbsec{display:none!important}

  /* Nav items — bigger and easier to tap */
  .ni{
    display:flex!important;flex-direction:column!important;
    align-items:center!important;justify-content:center!important;
    padding:12px 4px 10px!important;
    font-size:10px!important;gap:4px;
    min-width:56px;flex:1;
    border-left:none!important;
    border-top:3px solid transparent;
    border-radius:0!important;margin:0!important;
    white-space:nowrap;
  }
  .ni.on{border-left:none!important;border-top-color:var(--y)!important}
  .ni svg{width:22px!important;height:22px!important}
  .nbadge{position:absolute;top:4px;right:50%;transform:translateX(14px)}

  /* Sidebar footer — theme + logout as nav items */
  .sbft{
    display:flex!important;flex-direction:row!important;
    padding:0!important;border-top:none!important;
    border-left:1px solid var(--glass-border)!important;
    flex-shrink:0;
  }
  .sbft-desk{display:none!important}
  .sbft-mob{display:flex!important;flex-direction:row!important}
  .mob-footer{display:none!important}

  /* Topbar */
  .tb{flex-wrap:wrap;gap:8px;margin-bottom:14px}
  .tb h1{font-size:20px!important}
  .tbr{flex-wrap:wrap;gap:6px;width:100%}
  .tbr select,.tbr .pbtn{width:100%}

  /* KPIs */
  .kg{grid-template-columns:1fr 1fr!important;gap:8px}
  .kpi{padding:12px!important}
  .kv{font-size:17px!important}

  /* Cards */
  .card{padding:12px;border-radius:var(--r)}

  /* Inline grids */
  [style*="grid-template-columns:1fr 1fr"]{grid-template-columns:1fr!important}
  [style*="grid-template-columns:3fr 2fr"]{grid-template-columns:1fr!important}
  [style*="grid-template-columns:repeat(4"]{grid-template-columns:1fr 1fr!important}
  [style*="grid-template-columns:repeat(3"]{grid-template-columns:1fr 1fr!important}
  [style*="grid-template-columns:260px"]{grid-template-columns:1fr!important}

  /* Tables */
  .tw{overflow-x:auto;-webkit-overflow-scrolling:touch}
  .dt th{font-size:10px!important;padding:7px 8px!important}
  .dt td{padding:9px 8px!important;font-size:12px!important}
  .dt th:nth-child(3),.dt td:nth-child(3){display:none}

  /* Big numbers */
  [style*="font-size:44px"]{font-size:28px!important}
  [style*="font-size:40px"]{font-size:26px!important}
  [style*="font-size:36px"]{font-size:22px!important}

  /* Calendar */
  .cg{gap:2px!important}
  .cc{min-height:50px!important;padding:3px!important;border-radius:5px!important;min-width:0!important;overflow:hidden!important}
  .cn{font-size:10px!important}
  .cp{font-size:7.5px!important;padding:1px 3px!important}
  .ch2{font-size:9px!important;padding:4px 0!important}

  /* Modal */
  .mbk{padding:0;align-items:flex-end}
  .mbox{border-radius:20px 20px 0 0;max-height:92dvh;width:100%;margin:0;animation:slideUp .3s cubic-bezier(.22,1,.36,1)}
  .mh{padding:4px 16px 0}
  .mb{padding:12px 16px 28px}

  /* Forms */
  .fg{grid-template-columns:1fr!important}
  .fgr.fw{grid-column:1!important}
  .fa{flex-direction:column;gap:8px}
  .fa .btn{width:100%;justify-content:center;padding:13px}
  .fi,.fs,.fta{font-size:16px!important}

  /* Tabs */
  .tabs{flex-wrap:nowrap;overflow-x:auto;scrollbar-width:none}
  .tabs::-webkit-scrollbar{display:none}
  .tab{white-space:nowrap;flex-shrink:0}

  /* Preview items */
  .pitem{flex-wrap:wrap;gap:4px}
  .pitem .pin{width:100%}

  /* Acumulado */
  .ah .at{font-size:28px!important}

  /* Splash */
  .splash-logo{width:110px!important}
}
@media (max-width:400px){
  .ni{min-width:48px;font-size:8px}
  .ni svg{width:17px;height:17px}
  .page{padding:10px}
  .kv{font-size:15px!important}
}

/* ══════════════ PRINT ══════════════ */
@media print {
  #splash,#timeoutWarn,.sb,.outbtn,.btn,.tabs,.tbr,.pbtn,button{display:none!important}
  body,html{background:#fff!important;color:#000!important;overflow:auto!important}
  .main{overflow:visible!important}
  .page{padding:10px!important}
  .cc{border:1px solid #ddd!important;background:#fafafa!important}
  .cc.hp{border-color:#c9920f!important;background:#fffde7!important}
  @page{margin:1cm;size:A4 landscape}
}


/* ══════════════ TOAST ══════════════ */
.toast{
  position:fixed;bottom:90px;left:50%;transform:translateX(-50%) translateY(20px);
  background:var(--bg3);border:1px solid var(--bg5);color:var(--w);
  padding:10px 18px;border-radius:30px;font-size:13px;font-weight:500;
  display:flex;align-items:center;gap:8px;z-index:9999;
  opacity:0;transition:all .3s;pointer-events:none;white-space:nowrap;
  box-shadow:0 4px 20px rgba(0,0,0,.4);
}
.toast.on{opacity:1;transform:translateX(-50%) translateY(0)}
.toast.suc .ti{color:#4ade80}
.toast.err .ti{color:#f87171}
.toast.err{border-color:rgba(248,113,113,.3)}
.toast.inf .ti{color:#93c5fd}
.toast .ti svg{width:14px;height:14px;stroke-width:2.5;stroke:currentColor;fill:none}
@media(max-width:600px){.toast{bottom:80px;font-size:12px;max-width:90vw;white-space:normal;text-align:center}}

/* ══════════ CSS RESTAURADO ══════════ */
.mc2{background:var(--bg3);border:none;border-radius:8px;width:32px;height:32px;display:flex;align-items:center;justify-content:center;cursor:pointer;color:var(--w3);transition:all .15s;flex-shrink:0}
.mc2:hover{background:var(--bg4);color:var(--w)}
.mc2 svg{width:16px;height:16px;stroke:currentColor;stroke-width:2;fill:none}
.cl2{display:flex;justify-content:space-between;padding:7px 0;font-size:13px;color:var(--w2);border-bottom:1px solid var(--bg3)}
.cl2 span{font-family:'IBM Plex Mono',monospace;color:var(--w)}
.cl2.tot{border-bottom:none;border-top:2px solid var(--y);margin-top:4px;padding-top:10px;font-weight:700;color:var(--w)}
.cl2.tot span{color:var(--y);font-size:15px}
.ct2{display:flex;justify-content:space-between;align-items:center;background:var(--y4);border:1px solid rgba(245,197,24,.25);border-radius:var(--r);padding:12px 14px;margin-top:12px}
.ct2 .cl{font-size:12px;color:var(--w2);font-weight:600}
.ct2 .cv{font-family:'IBM Plex Mono',monospace;font-size:17px;font-weight:700;color:var(--y)}
.imp-item{display:grid;grid-template-columns:1fr auto auto auto;gap:10px;align-items:center;padding:8px 0;border-bottom:1px solid var(--bg3);font-size:12px}
.imp-item .in{font-weight:500;color:var(--w);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.imp-item .id2{color:var(--w3);font-size:11px}
.imp-item .iv{font-family:'IBM Plex Mono',monospace;color:var(--y);font-weight:600}
.proc{text-align:center;padding:30px 16px}
.proc p{font-size:14px;font-weight:600;color:var(--w);margin:12px 0 4px}
.proc span{font-size:12px;color:var(--w3)}
.proc-spin{width:36px;height:36px;border:3px solid var(--bg4);border-top-color:var(--y);border-radius:50%;margin:0 auto;animation:spin 1s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
@media(max-width:600px){.imp-item{grid-template-columns:1fr auto;row-gap:3px}}
.imp-item .ib{display:flex;justify-content:flex-end}
.bar-col{transition:opacity .2s;cursor:pointer}
.bar-col:hover{opacity:.8}
</style>
</head>
<body>

<!-- SPLASH -->
<div id="splash">
  <img class="splash-logo" src="https://i.ibb.co/W4myqrMK/logo-ar4-1769255749160-l5ecu2z7ru.png" alt="AR4"
    onerror="this.style.opacity=0"/>
  <div class="splash-bar"><div class="splash-fill"></div></div>
  <div class="splash-txt">Carregando sistema...</div>
</div>

<!-- TIMEOUT WARNING -->
<div id="timeoutWarn">
  <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
  <span>Sessão expira em <span id="timeoutCount">60</span>s &nbsp;—&nbsp; <button onclick="resetTimeout()" style="background:var(--y);color:var(--bg);border:none;border-radius:5px;padding:3px 10px;font-size:12px;font-weight:600;cursor:pointer">Continuar</button></span>
</div>

<!-- FORÇAR TROCA DE SENHA -->
<div id="forcePassScreen">
  <div style="background:var(--bg2);border:1px solid var(--bg4);border-radius:20px;padding:36px;width:100%;max-width:380px">
    <div style="text-align:center;margin-bottom:24px">
      <div style="font-size:28px;margin-bottom:10px">🔐</div>
      <div style="font-size:18px;font-weight:700;color:var(--w);margin-bottom:6px">Troque sua senha</div>
      <div style="font-size:13px;color:var(--w3)">Por segurança, defina uma senha pessoal no primeiro acesso.</div>
    </div>
    <div style="display:flex;flex-direction:column;gap:12px">
      <div><label class="fl" style="display:block;margin-bottom:4px">Nova senha</label><input class="fi" type="password" id="fp1" placeholder="Mínimo 4 caracteres"/></div>
      <div><label class="fl" style="display:block;margin-bottom:4px">Confirmar senha</label><input class="fi" type="password" id="fp2" placeholder="Repita a senha" onkeydown="if(event.key==='Enter')confirmarForceSenha()"/></div>
      <button class="lbtn" onclick="confirmarForceSenha()">Definir senha e entrar</button>
    </div>
  </div>
</div>

<!-- LOGIN MODERNO -->
<div id="ls" style="position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:var(--bg);z-index:1000;padding:20px;overflow:auto">
  <div style="position:absolute;inset:0;overflow:hidden;pointer-events:none">
    <div style="position:absolute;width:700px;height:700px;border-radius:50%;background:radial-gradient(circle,rgba(245,197,24,.06) 0%,transparent 70%);top:-250px;left:50%;transform:translateX(-50%)"></div>
    <div style="position:absolute;width:400px;height:400px;border-radius:50%;background:radial-gradient(circle,rgba(245,197,24,.03) 0%,transparent 70%);bottom:-150px;right:5%"></div>
  </div>
  <div style="display:flex;width:100%;max-width:860px;min-height:480px;margin:auto;gap:0;position:relative;z-index:1;border-radius:20px;overflow:hidden;box-shadow:0 30px 80px rgba(0,0,0,.6)">
    
    <!-- ESQUERDO -->
    <div style="flex:1;display:flex;flex-direction:column;justify-content:space-between;padding:44px 48px;background:linear-gradient(160deg,rgba(245,197,24,.1) 0%,rgba(245,197,24,.03) 50%,var(--bg3) 100%);border:1px solid rgba(245,197,24,.12);border-right:none">
      <img src="https://i.ibb.co/W4myqrMK/logo-ar4-1769255749160-l5ecu2z7ru.png" alt="AR4" style="width:120px;height:auto;filter:drop-shadow(0 4px 24px rgba(245,197,24,.35))" onerror="this.style.display='none'"/>
      <div>
        <div style="font-size:11px;color:var(--y);font-weight:600;text-transform:uppercase;letter-spacing:1.5px;margin-bottom:12px">Sistema de Gestão</div>
        <h1 style="font-size:32px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1.25;margin-bottom:14px">Indenizações<br><span style="color:var(--y)">AR4</span> Proteção Veicular</h1>
        <p style="font-size:13px;color:var(--w3);line-height:1.75;margin-bottom:28px">Transparência e controle total<br>na palma da sua mão.</p>
        <div style="display:flex;flex-direction:column;gap:8px">
          <div style="display:flex;align-items:center;gap:9px;font-size:12px;color:var(--w3)"><div style="width:5px;height:5px;border-radius:50%;background:var(--y);flex-shrink:0"></div>Acompanhamento mensal em tempo real</div>
          <div style="display:flex;align-items:center;gap:9px;font-size:12px;color:var(--w3)"><div style="width:5px;height:5px;border-radius:50%;background:var(--y);flex-shrink:0"></div>Calendário visual de pagamentos</div>
          <div style="display:flex;align-items:center;gap:9px;font-size:12px;color:var(--w3)"><div style="width:5px;height:5px;border-radius:50%;background:var(--y);flex-shrink:0"></div>Relatórios por consultor</div>
          <div style="display:flex;align-items:center;gap:9px;font-size:12px;color:var(--w3)"><div style="width:5px;height:5px;border-radius:50%;background:var(--y);flex-shrink:0"></div>Exportação e backup completo</div>
        </div>
      </div>
      <div style="font-size:11px;color:var(--w4)">© AR4 Proteção Veicular — Sistema interno</div>
    </div>

    <!-- DIREITO -->
    <div style="width:340px;flex-shrink:0;display:flex;flex-direction:column;justify-content:center;padding:44px 40px;background:var(--bg2);border:1px solid var(--bg4)">
      <div style="margin-bottom:30px">
        <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:6px">Acesso ao sistema</div>
        <div style="font-size:24px;font-weight:700;color:var(--w);letter-spacing:-.3px">Bem-vindo</div>
      </div>
      <div style="margin-bottom:14px">
        <label style="display:block;font-size:10px;font-weight:600;color:var(--w3);text-transform:uppercase;letter-spacing:.4px;margin-bottom:5px">Usuário</label>
        <input class="pinp" type="text" id="lp" placeholder="Seu login" style="margin-bottom:0" onkeydown="if(event.key==='Enter')document.getElementById('lpassword').focus()"/>
      </div>
      <div style="margin-bottom:22px">
        <label style="display:block;font-size:10px;font-weight:600;color:var(--w3);text-transform:uppercase;letter-spacing:.4px;margin-bottom:5px">Senha</label>
        <div style="position:relative">
          <input class="pinp" type="password" id="lpassword" placeholder="Sua senha" style="margin-bottom:0;padding-right:40px" onkeydown="if(event.key==='Enter')login()"/>
          <button onclick="togglePassView()" style="position:absolute;right:10px;top:50%;transform:translateY(-50%);background:none;border:none;cursor:pointer;color:var(--w4);padding:4px" tabindex="-1">
            <svg id="eyeIc" width="16" height="16" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
          </button>
        </div>
      </div>
      <button id="loginBtn" class="lbtn" onclick="login()" style="letter-spacing:.2px">Entrar no sistema</button>
      <div style="text-align:center;margin-top:14px;font-size:10px;color:var(--w4);letter-spacing:.5px">versão 2.2</div>
    </div>
  </div>
</div>

<!-- APP -->
<div id="app">
<div class="lay">
  <div class="sb">
    <div class="sbt">
      <img src="https://i.ibb.co/W4myqrMK/logo-ar4-1769255749160-l5ecu2z7ru.png" alt="AR4"
        style="width:36px;height:36px;object-fit:contain;filter:drop-shadow(0 0 6px rgba(240,180,41,.3))"
        onerror="this.style.display='none';this.nextElementSibling.style.display='flex'"/>
      <div class="sbsh" style="display:none">AR4</div>
      <div class="sbb"><strong>AR4</strong><span>Proteção Veicular</span></div>
    </div>
    <nav class="sbn" id="sbn"></nav>
    <div class="sbft">
      <!-- Desktop view -->
      <div class="sbft-desk">
        <div class="sbu">
          <div class="sbav" id="sbav">D</div>
          <div class="sbui"><strong id="sbnm">Diretoria</strong><span id="sbrl">Visualização</span></div>
        </div>
        <div style="display:flex;gap:6px;margin-bottom:7px">
          <button onclick="toggleTheme()" id="themeBtn" style="flex:1;padding:6px;background:transparent;border:1px solid var(--glass-border2);border-radius:var(--r);color:var(--w3);font-size:11px;font-family:'IBM Plex Sans',sans-serif;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:5px">
            <svg id="themeIc" width="13" height="13" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
            <span id="themeTxt">Claro</span>
          </button>
        </div>
        <button class="outbtn" onclick="logout()">
          <svg viewBox="0 0 24 24"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>Sair
        </button>
      </div>
      <!-- Mobile view: theme + logout as nav items -->
      <div class="sbft-mob">
        <button onclick="toggleTheme()" class="ni" id="themeBtnMob" style="flex-direction:column;gap:3px;font-size:9px;min-width:54px;border-top:2px solid transparent">
          <svg id="themeIcMob" width="19" height="19" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
          <span id="themeTxtMob">Claro</span>
        </button>
        <button onclick="logout()" class="ni" style="flex-direction:column;gap:3px;font-size:9px;min-width:54px;border-top:2px solid transparent;color:var(--red)">
          <svg width="19" height="19" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/></svg>
          Sair
        </button>
      </div><!-- mob-footer moved inside sbft -->
    </div>
  </div>
  <div class="main" id="main"><div id="pc"></div></div>
</div>
</div>

<!-- MODAL -->
<div class="mbk" id="modal" onclick="if(event.target===this)closeM()">
  <div class="mbox" id="mbox"></div>
</div>

<!-- TOAST -->
<div class="toast" id="toast">
  <span class="ti"><svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg></span>
  <span id="tm"></span>
</div>

<script>
// Worker do pdf.js: tenta carregar como blob local (elimina aviso de fake worker)
const PDF_WORKER_URL='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';
pdfjsLib.GlobalWorkerOptions.workerSrc=PDF_WORKER_URL;
const AR4_VERSION='2.2';console.log('[AR4] versão',AR4_VERSION);
fetch(PDF_WORKER_URL).then(r=>r.text()).then(code=>{
  const blob=new Blob([code],{type:'application/javascript'});
  pdfjsLib.GlobalWorkerOptions.workerSrc=URL.createObjectURL(blob);
}).catch(()=>{/* mantém URL do CDN; fallback interno do pdf.js cuida do resto */});

// ══ SPLASH — declared first so INIT can call it ══
function hideSplash(){
  try{
    const s=document.getElementById('splash');
    if(!s)return;
    s.style.transition='opacity .5s ease';
    s.style.opacity='0';
    s.style.pointerEvents='none';
    setTimeout(()=>{
      s.style.display='none';
      s.remove();
    },600);
  }catch(e){}
}

// ══════════════ DATABASE ══════════════
const DB={meses:{},nextId:1};
const HIST=[];  // {key,label,valor,pagamentos}

// Pré-carrega Junho 2026
DB.meses['2026-06']={
  label:'Junho 2026',ano:2026,mes:6,
  indenizacoes:[
    {id:1,nome:'Paulo Fernandes dos Santos',data:'2026-06-01',parcela:'1/2',status:'DEPENDE DO BOLETO',obs:'Se aprovado, será parcelado em 2x',valorLiq:17335,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:2,nome:'Paulo Fernandes dos Santos',data:'2026-06-02',parcela:'3/3',status:'PENDENTE',obs:'',valorLiq:13129,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:3,nome:'Rian Kayque José da Silva',data:'2026-06-03',parcela:'1/2',status:'DEPENDE DO BOLETO',obs:'Se aprovado, será parcelado em 2x no valor de R$ 8.667,00',valorLiq:17335,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:4,nome:'FERIADO',data:'2026-06-04',parcela:'',status:'FERIADO',obs:'Sem expediente',valorLiq:0,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:5,nome:'Rafael Leandro da Silva Santos',data:'2026-06-08',parcela:'',status:'PENDENTE',obs:'',valorLiq:16353,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:6,nome:'Luis Rogério Wanderley Beltrão',data:'2026-06-09',parcela:'2/2',status:'PENDENTE',obs:'',valorLiq:14972.5,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:7,nome:'Mathews Felipe Melo Silva',data:'2026-06-10',parcela:'1/2',status:'DEPENDE DO BOLETO',obs:'Se aprovado, será parcelado em 2x no valor de R$ 8.374,00',valorLiq:16749,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:8,nome:'José Elio Flor da Silva',data:'2026-06-11',parcela:'',status:'PENDENTE',obs:'Fortaleza',valorLiq:15289,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:9,nome:'Gabriel Luiz Marques de Brito',data:'2026-06-15',parcela:'',status:'PENDENTE',obs:'',valorLiq:16090,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:10,nome:'Jamersson Hely Costa',data:'2026-06-17',parcela:'',status:'VALOR RESTANTE',obs:'Boleto maior que R$ 18.000, associado pagará o valor restante',valorLiq:17294,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
    {id:11,nome:'Leonardo de Santana Candido',data:'2026-06-17',parcela:'1/2',status:'DEPENDE DO BOLETO',obs:'Se aprovado, será parcelado em 2x',valorLiq:20298,valorFipe:0,taxaTransf:0,cotaPart:0,bolContrib:0,licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0},
  ],
  meta:{pagamentos:11,beneficiarios:9,diasPag:10,condicionais:3}
};
DB.nextId=12;

// ══════════════ APROVAÇÕES (dentro do DB principal) ══════════════
// Aprovações ficam em DB.aprovacoes para que gestor E diretoria vejam os mesmos dados
if(!DB.aprovacoes) DB.aprovacoes=[];
if(!DB.log) DB.log=[];
if(!DB.avisoGestor) DB.avisoGestor='';

// ══════════════ LOG DE ATIVIDADES ══════════════
function addLog(acao){
  if(!DB.log)DB.log=[];
  DB.log.unshift({
    id:Date.now(),
    acao,
    usuario:currentUser?currentUser.nome:'Sistema',
    perfil:currentUser?currentUser.perfil:'sys',
    ts:new Date().toISOString()
  });
  if(DB.log.length>200)DB.log=DB.log.slice(0,200);
}
function fmtTs(ts){
  const d=new Date(ts);
  return d.toLocaleDateString('pt-BR')+' às '+d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});
}
function getAprovacoes(){ return DB.aprovacoes||[]; }
function saveAprovacoes(list){ DB.aprovacoes=list; saveDB(); }
function addAprovacao(data){
  if(!DB.aprovacoes)DB.aprovacoes=[];
  DB.aprovacoes=DB.aprovacoes.filter(a=>a.mesKey!==data.mesKey);
  DB.aprovacoes.unshift(data);
  saveDB();
}


// ╔══════════════════════════════════════════════════════════════════════╗
// ║         AR4 PROTEÇÃO VEICULAR — CAMADA DE DADOS                     ║
// ║         Versão: 2.0 | Pronto para produção                          ║
// ╠══════════════════════════════════════════════════════════════════════╣
// ║                                                                      ║
// ║  PARA A EQUIPE DE DESENVOLVIMENTO:                                   ║
// ║                                                                      ║
// ║  Este arquivo contém TODO o sistema AR4 em um único HTML.            ║
// ║  Para conectar um banco de dados externo, você só precisa            ║
// ║  alterar as 4 funções abaixo (saveDB, loadDB, saveUsers,             ║
// ║  loadUsers). O restante do código NÃO precisa ser tocado.            ║
// ║                                                                      ║
// ║  PROVIDERS SUPORTADOS (basta descomentar e configurar):             ║
// ║  → localStorage  (padrão, funciona sem servidor)                    ║
// ║  → Supabase      (PostgreSQL gerenciado, recomendado)               ║
// ║  → Firebase      (Google, tempo real)                               ║
// ║  → REST API      (qualquer backend próprio)                         ║
// ║  → MongoDB Atlas (via API REST)                                      ║
// ║                                                                      ║
// ║  ESTRUTURA DOS DADOS:                                                ║
// ║  DB.meses     → { [key: "YYYY-MM"]: { label, ano, mes,             ║
// ║                    indenizacoes[], meta{} } }                        ║
// ║  DB.aprovacoes → Aprovacao[]                                         ║
// ║  DB.log       → LogEntry[]                                           ║
// ║  DB.avisoGestor → string                                             ║
// ║  HIST         → { key, label, valor, pagamentos }[]                  ║
// ║  USERS        → { id, nome, login, senha, perfil, ativo }[]         ║
// ║                                                                      ║
// ╚══════════════════════════════════════════════════════════════════════╝

// ── CONFIGURAÇÃO ── Altere apenas esta seção ──────────────────────────
const AR4_CONFIG = {

  // ┌─────────────────────────────────────────────────────────────────┐
  // │ PROVIDER ATIVO: troque para 'supabase', 'firebase', 'api'       │
  // └─────────────────────────────────────────────────────────────────┘
  provider: 'supabase',

  // ── Supabase (https://supabase.com — gratuito até 500MB) ──────────
  // provider: 'supabase',
  supabase: {
    url: 'https://nbbvohoitmmwhzvnyqww.supabase.co',   // ← Settings → API → Project URL
    anonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5iYnZvaG9pdG1td2h6dm55cXd3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI1MzExMDIsImV4cCI6MjA5ODEwNzEwMn0.J3uwWHxNOCHTqrk_aksp5pId4tZkHpeZBsXZfGMVljo',     // ← Settings → API → anon public
    // As 7 tabelas (meses, indenizacoes, consultores, usuarios,
    // aprovacoes, logs, config) são criadas pelo arquivo
    // ar4-setup-banco.sql — rode-o no SQL Editor do Supabase.
    // A API REST é gerada automaticamente pelo Supabase.
  },

  // ── Firebase Firestore (https://firebase.google.com) ──────────────
  // provider: 'firebase',
  firebase: {
    apiKey: 'SUA-API-KEY',
    projectId: 'SEU-PROJECT-ID',
    collection: 'ar4',
    // Instale: <script src="https://www.gstatic.com/firebasejs/10.x.x/firebase-app-compat.js"><\/script>
    //          <script src="https://www.gstatic.com/firebasejs/10.x.x/firebase-firestore-compat.js"><\/script>
  },

  // ── API REST própria (Node/Python/PHP/Laravel/etc) ─────────────────
  // provider: 'api',
  api: {
    baseUrl: 'https://sua-api.com/ar4',
    token: 'SEU-TOKEN-DE-AUTENTICACAO',
    // Endpoints esperados:
    // GET  /ar4/data          → retorna { db, hist }
    // POST /ar4/data          → salva   { db, hist }
    // GET  /ar4/users         → retorna users[]
    // POST /ar4/users         → salva   users[]
    // Headers: Authorization: Bearer TOKEN
  },

  // ── MongoDB Atlas (via Data API) ───────────────────────────────────
  // provider: 'mongodb',
  mongodb: {
    dataApiUrl: 'https://data.mongodb-api.com/app/SEU-APP-ID/endpoint/data/v1',
    apiKey: 'SUA-API-KEY',
    database: 'ar4',
    dataCollection: 'dados',
    usersCollection: 'usuarios',
  },

};
// ─────────────────────────────────────────────────────────────────────

const DB_KEY = 'ar4_db_v1', HIST_KEY = 'ar4_hist_v1', USERS_KEY_LS = 'ar4_users_v1';

// ══ SAVE ══ Altere apenas o bloco do seu provider ════════════════════

// ── Helper Supabase (API REST PostgREST) ──
// ╔════════════════════════════════════════════════════════════════════╗
// ║  CAMADA DE SINCRONIZAÇÃO COM O BACKEND (Supabase — PostgreSQL)      ║
// ║  Arquitetura local-first: o app trabalha com localStorage           ║
// ║  (instantâneo, funciona offline) e sincroniza com as tabelas        ║
// ║  do banco em segundo plano através da API REST do Supabase.         ║
// ╚════════════════════════════════════════════════════════════════════╝

function isSB(){
  return AR4_CONFIG.provider==='supabase'
    && AR4_CONFIG.supabase.url
    && !AR4_CONFIG.supabase.url.includes('SEU-PROJETO');
}

async function sbReq(path, method='GET', body=null, query=''){
  const {url, anonKey} = AR4_CONFIG.supabase;
  const headers = {
    'apikey': anonKey,
    'Authorization': 'Bearer '+anonKey,
    'Content-Type': 'application/json',
    'Prefer': method==='POST' ? 'resolution=merge-duplicates,return=minimal' : 'return=minimal'
  };
  const res = await fetch(`${url}/rest/v1/${path}${query}`, {
    method, headers, body: body ? JSON.stringify(body) : undefined
  });
  if(!res.ok){
    const txt = await res.text();
    throw new Error(`${method} ${path}: ${res.status} ${txt.slice(0,180)}`);
  }
  if(method==='GET') return res.json();
  return null;
}

// ── PUSH: envia tudo para as tabelas do banco (debounced) ──────────────
let _pushTimer=null, _pushing=false, _pushAgain=false;
function schedulePush(){
  if(!isSB())return;
  clearTimeout(_pushTimer);
  _pushTimer=setTimeout(sbPushAll, 1200);
}
// Push imediato para ações críticas (salvar/excluir) — garante persistência antes de navegar
async function pushNow(){
  if(!isSB())return;
  clearTimeout(_pushTimer);
  await sbPushAll();
}

async function sbPushAll(){
  if(!isSB())return;
  if(_pushing){_pushAgain=true;return;}
  _pushing=true;
  window._syncState='enviando';
  try{
    // 1. MESES (upsert) + remover meses excluídos
    const mesesRows=Object.entries(DB.meses).map(([key,m])=>({
      key, label:m.label, ano:m.ano||parseInt(key.split('-')[0]),
      mes:m.mes||parseInt(key.split('-')[1]), meta:m.meta||{},
      updated_at:new Date().toISOString()
    }));
    const remoteKeys=await sbReq('meses?select=key');
    const localKeys=new Set(Object.keys(DB.meses));
    const toDelete=remoteKeys.filter(r=>!localKeys.has(r.key)).map(r=>r.key);
    for(const k of toDelete){
      await sbReq(`meses?key=eq.${encodeURIComponent(k)}`,'DELETE'); // CASCADE apaga indenizações
    }
    if(mesesRows.length) await sbReq('meses','POST',mesesRows,'?on_conflict=key');

    // 2. INDENIZAÇÕES (substituição completa = espelho do estado local)
    await sbReq('indenizacoes?id=gte.0','DELETE');
    const indRows=[];
    Object.entries(DB.meses).forEach(([key,m])=>{
      (m.indenizacoes||[]).forEach(i=>{
        indRows.push({
          id:i.id, mes_key:key, nome:i.nome||'', data:i.data||null,
          parcela:i.parcela||'', status:i.status||'PENDENTE', obs:i.obs||'',
          consultor:i.consultor||'', valor_liq:i.valorLiq||0, valor_fipe:i.valorFipe||0,
          descontos:{taxaTransf:i.taxaTransf||0,cotaPart:i.cotaPart||0,boletosContr:i.boletosContr||0,
            licenciamento:i.licenciamento||0,ipva:i.ipva||0,bombeiro:i.bombeiro||0,
            multa:i.multa||0,financiamento:i.financiamento||0},
          comprovante:i.comprovante||null,
          updated_at:new Date().toISOString()
        });
      });
    });
    // envia em lotes de 200 para não estourar payload
    for(let i=0;i<indRows.length;i+=200){
      await sbReq('indenizacoes','POST',indRows.slice(i,i+200),'?on_conflict=id');
    }

    // 3. CONSULTORES (espelho)
    await sbReq('consultores?nome=neq.__never__','DELETE');
    const consRows=getConsultores().map(nome=>({nome}));
    if(consRows.length) await sbReq('consultores','POST',consRows,'?on_conflict=nome');

    // 4. USUÁRIOS (upsert por id)
    const userRows=getUsers().map(u=>({
      id:u.id, nome:u.nome, login:u.login, senha:u.senha,
      perfil:u.perfil, ativo:u.ativo!==false, force_change:!!u.forceChange,
      updated_at:new Date().toISOString()
    }));
    if(userRows.length) await sbReq('usuarios','POST',userRows,'?on_conflict=id');

    // 5. APROVAÇÕES (espelho)
    await sbReq('aprovacoes?id=gte.0','DELETE');
    const apRows=(DB.aprovacoes||[]).map(a=>({
      id:a.id, mes_key:a.mesKey||null, status:a.status||'pendente',
      payload:a, criado_em:a.criadoEm||new Date().toISOString(),
      updated_at:new Date().toISOString()
    }));
    if(apRows.length) await sbReq('aprovacoes','POST',apRows,'?on_conflict=id');

    // 6. CONFIG (hist, aviso, nextId, log)
    await sbReq('config','POST',[
      {key:'hist', value:HIST, updated_at:new Date().toISOString()},
      {key:'avisoGestor', value:{texto:DB.avisoGestor||''}, updated_at:new Date().toISOString()},
      {key:'nextId', value:{n:DB.nextId}, updated_at:new Date().toISOString()},
      {key:'log', value:(DB.log||[]).slice(0,300), updated_at:new Date().toISOString()}
    ],'?on_conflict=key');

    window._syncState='ok';
    window._lastSync=new Date().toISOString();
  }catch(e){
    window._syncState='erro';
    console.warn('[AR4 sync] push falhou:', e.message);
    toast('Sem conexão com o banco — dados salvos localmente.','inf');
  }finally{
    _pushing=false;
    if(_pushAgain){_pushAgain=false;schedulePush();}
  }
}

// ── PULL: busca tudo das tabelas e reconstrói o estado local ───────────
async function sbPullAll(silent){
  if(!isSB())return false;
  window._syncState='baixando';
  try{
    const [meses,inds,cons,users,aps,cfg]=await Promise.all([
      sbReq('meses?select=*'),
      sbReq('indenizacoes?select=*&order=data.asc'),
      sbReq('consultores?select=nome&order=nome.asc'),
      sbReq('usuarios?select=*'),
      sbReq('aprovacoes?select=*&order=criado_em.desc'),
      sbReq('config?select=*')
    ]);

    // Reconstrói DB.meses
    const novosMeses={};
    meses.forEach(m=>{
      novosMeses[m.key]={label:m.label,ano:m.ano,mes:m.mes,meta:m.meta||{},indenizacoes:[]};
    });
    inds.forEach(r=>{
      if(!novosMeses[r.mes_key])return;
      const d=r.descontos||{};
      novosMeses[r.mes_key].indenizacoes.push({
        id:r.id, nome:r.nome, data:r.data, parcela:r.parcela||'',
        status:r.status||'PENDENTE', obs:r.obs||'', consultor:r.consultor||'',
        valorLiq:parseFloat(r.valor_liq)||0, valorFipe:parseFloat(r.valor_fipe)||0,
        taxaTransf:d.taxaTransf||0, cotaPart:d.cotaPart||0, boletosContr:d.boletosContr||0,
        licenciamento:d.licenciamento||0, ipva:d.ipva||0, bombeiro:d.bombeiro||0,
        multa:d.multa||0, financiamento:d.financiamento||0,
        comprovante:r.comprovante||null
      });
    });

    // Só aplica se o banco tiver dados (evita apagar dados locais num banco recém-criado)
    if(meses.length>0||users.length>0){
      if(meses.length>0){
        for(const k of Object.keys(DB.meses)) delete DB.meses[k];
        Object.assign(DB.meses,novosMeses);
      }
      DB.aprovacoes=aps.map(a=>a.payload);
      const cfgMap={}; cfg.forEach(r=>cfgMap[r.key]=r.value);
      if(cfgMap.hist){HIST.splice(0,HIST.length,...cfgMap.hist);}
      if(cfgMap.avisoGestor)DB.avisoGestor=cfgMap.avisoGestor.texto||'';
      if(cfgMap.nextId&&cfgMap.nextId.n>DB.nextId)DB.nextId=cfgMap.nextId.n;
      if(cfgMap.log)DB.log=cfgMap.log;
      if(users.length){
        saveUsersLocal(users.map(u=>({
          id:u.id,nome:u.nome,login:u.login,senha:u.senha,
          perfil:u.perfil,ativo:u.ativo!==false,forceChange:!!u.force_change
        })));
      }
      if(cons.length) saveConsultoresLocal(cons.map(r=>r.nome));

      // Atualiza cache local
      try{
        localStorage.setItem(DB_KEY,JSON.stringify({meses:DB.meses,nextId:DB.nextId,
          aprovacoes:DB.aprovacoes,log:DB.log,avisoGestor:DB.avisoGestor,ts:Date.now()}));
        localStorage.setItem(HIST_KEY,JSON.stringify(HIST));
      }catch(e){}

      const mk=Object.keys(DB.meses).sort();
      if(mk.length&&typeof mesAtivo!=='undefined'&&!DB.meses[mesAtivo])mesAtivo=mk[mk.length-1];
    }

    window._syncState='ok';
    window._lastSync=new Date().toISOString();
    if(!silent)toast('Dados sincronizados com o banco!','suc');
    return true;
  }catch(e){
    window._syncState='erro';
    console.warn('[AR4 sync] pull falhou:', e.message);
    if(!silent)toast('Banco indisponível — usando dados locais.','inf');
    return false;
  }
}

// ── SAVE/LOAD principais (local-first) ──────────────────────────────────
function saveDBLocal(){
  const payload={meses:DB.meses,nextId:DB.nextId,aprovacoes:DB.aprovacoes||[],
    log:DB.log||[],avisoGestor:DB.avisoGestor||'',ts:Date.now()};
  try{
    localStorage.setItem(DB_KEY,JSON.stringify(payload));
    localStorage.setItem(HIST_KEY,JSON.stringify(HIST));
  }catch(e){console.warn('[AR4] localStorage cheio:',e);}
}
async function saveDB(){
  saveDBLocal();
  schedulePush(); // sincroniza com o banco em segundo plano
}

async function loadDB(){
  // Sempre carrega o cache local primeiro (instantâneo, funciona offline)
  try{
    const d=localStorage.getItem(DB_KEY);
    const h=localStorage.getItem(HIST_KEY);
    if(d){
      const p=JSON.parse(d);
      Object.assign(DB.meses,p.meses||{});
      DB.nextId=p.nextId||DB.nextId;
      if(p.aprovacoes)DB.aprovacoes=p.aprovacoes;
      if(p.log)DB.log=p.log;
      if(p.avisoGestor!==undefined)DB.avisoGestor=p.avisoGestor;
    }
    if(h){
      const ph=JSON.parse(h);
      ph.forEach(item=>{if(!HIST.find(x=>x.key===item.key))HIST.push(item);});
    }
    window._pendingMesAtivo=Object.keys(DB.meses).sort().pop();
  }catch(e){console.warn('[AR4] loadDB error:',e);}
  // O pull do banco acontece após o login (ver função login)
}

// ══ USERS SAVE ════════════════════════════════════════════════════════
// (saveUsers da camada de dados removido — duplicado; versão ativa abaixo)


// ══ USERS LOAD ════════════════════════════════════════════════════════
// (getUsers da camada de dados removido — duplicado; versão ativa abaixo)


// ══ INIT ══════════════════════════════════════════════════════════════
try{const t=localStorage.getItem('ar4_theme');if(t==='light')applyTheme('light');}catch(e){}
try{loadDB();}catch(e){}

// Restore session if exists
try{
  const _s=sessionStorage.getItem('ar4_session');
  if(_s){
    const _sess=JSON.parse(_s);
    const _users=getUsers();
    const _found=_users.find(u=>u.id===_sess.id&&u.ativo);
    if(_found){
      currentUser=_found;role=_found.perfil;
      document.getElementById('ls').style.display='none';
      document.getElementById('app').style.display='block';
      const _ini=_found.nome.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase();
      const _e1=document.getElementById('sbav');
      const _e2=document.getElementById('sbnm');
      const _e3=document.getElementById('sbrl');
      if(_e1)_e1.textContent=_ini;
      if(_e2)_e2.textContent=_found.nome;
      if(_e3)_e3.textContent=role==='ges'?'Administrador':'Diretoria';
      // Always use most recent month
      const _mk=Object.keys(DB.meses).sort();
      if(_mk.length>0)mesAtivo=_mk[_mk.length-1];
      buildNav();resetTimeout();nav('dashboard');
    }
  }
}catch(e){}

// Hide splash — simple timeout, no async
setTimeout(hideSplash,1800);


// ══════════════ CONSULTORES ══════════════
const CONSULTORES_KEY='ar4_consultores';
function getConsultores(){
  try{
    const saved=localStorage.getItem(CONSULTORES_KEY);
    if(saved)return JSON.parse(saved);
  }catch(e){}
  return [
    'JOHN','ADRIANO CAVALCANTE','WAGNER FERRAZ',
    'MARIA CLARA - COMERCIAL AR4','MARIA BERNADETE - COMERCIAL AR4','ANA LETICIA - COMERCIAL AR4',
    'JAILSON SOARES','FERNANDES','FELIPE','LIANDRA','FLAVIO ROBERTO',
    'JAILSON DIAS MUNIZ','DANIEL BARBOSA','WENDEL LEITE','LUCAS HENRIQUE',
    'ARTUR MAURICIO','VITORIA KATARINA','JAQUELINE MARIA','ANDRE LIMA',
    'VICTOR ROBERTO','FABIO ROSEMIRO','DIROMAR','GLAYBSON HENRIQUE',
    'ROSIMERY','SILVIO','NELSON','JUNIOR BOY','JOSINALDO','KATIA','ANACLETO'
  ].sort();
}
function saveConsultoresLocal(list){
  try{localStorage.setItem(CONSULTORES_KEY,JSON.stringify(list));}catch(e){}
}
function saveConsultores(list){saveConsultoresLocal(list);schedulePush();}
HIST.push({key:'2026-06',label:'Jun/26',valor:165071,pagamentos:11});

// ══════════════ STATE ══════════════
let role='dir',page='dashboard',mesAtivo='2026-06',filterTab='todos',editId=null;
// Apply most recent month from loadDB if available
if(window._pendingMesAtivo) mesAtivo=window._pendingMesAtivo;

// ══════════════ UTILS ══════════════
const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
const fdt=d=>{if(!d)return '-';const p=d.split('-');return `${p[2]}/${p[1]}/${p[0]}`};
const isFer=i=>i.status==='FERIADO';
const getMes=(key)=>DB.meses[key||mesAtivo]||(DB.meses[key||mesAtivo]={label:key||mesAtivo,indenizacoes:[],meta:{}});
const daysInMonth=(y,m)=>new Date(y,m,0).getDate();
const firstDayOfMonth=(y,m)=>new Date(y,m-1,1).getDay(); // 0=Sun

const MESES_PT={1:'Janeiro',2:'Fevereiro',3:'Março',4:'Abril',5:'Maio',6:'Junho',7:'Julho',8:'Agosto',9:'Setembro',10:'Outubro',11:'Novembro',12:'Dezembro'};
const MESES_ABBR={1:'Jan',2:'Fev',3:'Mar',4:'Abr',5:'Mai',6:'Jun',7:'Jul',8:'Ago',9:'Set',10:'Out',11:'Nov',12:'Dez'};
const MESES_MAP={janeiro:1,fevereiro:2,março:3,marco:3,abril:4,maio:5,junho:6,julho:7,agosto:8,setembro:9,outubro:10,novembro:11,dezembro:12};

function statusColor(s){
  if(!s)return 'bpend';
  const u=s.toUpperCase();
  if(u==='PAGO')return 'bp';
  if(u==='FERIADO')return 'bf';
  if(u.includes('DEPENDE DO BOLETO'))return 'bdc';
  if(u.includes('VALOR RESTANTE'))return 'bvr';
  if(u.includes('FORTALEZA')||u.includes('PAGO'))return 'bp';
  return 'bpend';
}

// ══════════════ SENHAS ══════════════
function getPass(perfil){
  try{ return localStorage.getItem('ar4_pass_'+perfil)||( perfil==='dir'?'1234':'admin'); }
  catch(e){ return perfil==='dir'?'1234':'admin'; }
}
function setPass(perfil,v){
  try{ localStorage.setItem('ar4_pass_'+perfil,v); }catch(e){}
}

// ══════════════ USUÁRIOS ══════════════
const USERS_KEY='ar4_users_v1';
function getUsers(){
  try{
    const s=localStorage.getItem(USERS_KEY);
    if(s)return JSON.parse(s);
  }catch(e){}
  return [
    {id:1,nome:'Gestor',login:'admin',senha:'admin',perfil:'ges',ativo:true},
    {id:2,nome:'Diretoria',login:'dir',senha:'1234',perfil:'dir',ativo:true}
  ];
}
function saveUsersLocal(u){try{localStorage.setItem(USERS_KEY,JSON.stringify(u));}catch(e){}}
function saveUsers(u){saveUsersLocal(u);schedulePush();}
let currentUser=null;

// ══════════════ AUTH ══════════════
function selR(r){role=r;document.getElementById('bD').classList.toggle('on',r==='dir');document.getElementById('bG').classList.toggle('on',r==='ges')}
async function login(){
  const loginVal=document.getElementById('lp').value.trim();
  const passVal=document.getElementById('lpassword').value;
  const btn=document.getElementById('loginBtn');
  // Se conectado ao banco, baixa os usuários mais recentes ANTES de validar
  if(isSB()){
    if(btn){btn.disabled=true;btn.textContent='Entrando...';}
    try{
      const remoteUsers=await sbReq('usuarios?select=*');
      if(remoteUsers&&remoteUsers.length){
        saveUsersLocal(remoteUsers.map(u=>({
          id:u.id,nome:u.nome,login:u.login,senha:u.senha,
          perfil:u.perfil,ativo:u.ativo!==false,forceChange:!!u.force_change
        })));
      }
    }catch(e){console.warn('[AR4] não foi possível buscar usuários do banco:',e.message);}
    if(btn){btn.disabled=false;btn.textContent='Entrar no sistema';}
  }
  const users=getUsers();
  const found=users.find(u=>(u.ativo!==false)&&String(u.login).trim().toLowerCase()===loginVal.toLowerCase()&&String(u.senha)===passVal);
  if(found){
    currentUser=found;
    role=found.perfil;
    try{sessionStorage.setItem('ar4_session',JSON.stringify({id:found.id,perfil:found.perfil}));}catch(e){}
    document.getElementById('ls').style.display='none';
    document.getElementById('app').style.display='block';
    const initials=found.nome.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase();
    document.getElementById('sbav').textContent=initials;
    document.getElementById('sbnm').textContent=found.nome;
    document.getElementById('sbrl').textContent=role==='ges'?'Administrador':'Diretoria';
    // Set mesAtivo to most recent month
    const _lk=Object.keys(DB.meses).sort();
    if(_lk.length>0)mesAtivo=_lk[_lk.length-1];
    buildNav();
    resetTimeout();
    // Sincroniza com o banco em segundo plano e re-renderiza ao concluir
    if(isSB()){
      sbPullAll(true).then(ok=>{
        if(ok){buildNav();try{nav(page);}catch(e){}}
      });
    }
    if(checkForcePassword())return;
    // Check scheduled notifications on login
    setTimeout(verificarNotificacoesAgendadas, 2500);
    if(role==='dir'){nav('boasvindas');}
    else{nav('dashboard');}
  }else toast('Login ou senha incorretos.','err');
}
function logout(){
  clearTimeout(_timeoutTimer);if(typeof _warnTimer!=='undefined')clearTimeout(_warnTimer);if(typeof _warnInterval!=='undefined')clearInterval(_warnInterval);
  const w=document.getElementById('timeoutWarn');if(w)w.classList.remove('show');
  try{sessionStorage.removeItem('ar4_session');}catch(e){}
  currentUser=null;
  document.getElementById('app').style.display='none';
  document.getElementById('ls').style.display='flex';
  document.getElementById('lp').value='';
  document.getElementById('lpassword').value='';
}

// ══════════════ NAV ══════════════
const NAV_DIR=[{id:'dashboard',l:'Dashboard',ic:'grid'},{id:'aprovacoes',l:'Aprovações',ic:'check'},{id:'associado',l:'Associados',ic:'people'},{id:'senhas',l:'Senha',ic:'lock'},{id:'calendario',l:'Calendário',ic:'cal'},{id:'indenizacoes',l:'Indenizações',ic:'doc'},{id:'acumulado',l:'Acumulado',ic:'chart'},{id:'eventos',l:'Por Tipo',ic:'chart'},{id:'consultores',l:'Por Consultor',ic:'people'},{id:'busca',l:'Busca por Placa',ic:'search'},{id:'notificacoes',l:'Notificações',ic:'bell'}];
const NAV_GES=[...NAV_DIR,{id:'importar',l:'Importar PDF',ic:'upload',sec:'Gestão'},{id:'novo',l:'Nova / Editar',ic:'plus'},{id:'gerenciar',l:'Gerenciar',ic:'cfg'},{id:'gesCons',l:'Consultores',ic:'people'},{id:'usuarios',l:'Usuários',ic:'people'},{id:'log',l:'Log',ic:'log'},{id:'relatorio',l:'Rel. Anual',ic:'chart'},{id:'notificacoes',l:'Notificações',ic:'bell'},{id:'agendamento',l:'Agendamento',ic:'clock'},{id:'backup',l:'Backup',ic:'backup'},{id:'senhas',l:'Alterar Senhas',ic:'lock'}];
const IC={
  grid:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>',
  cal:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>',
  doc:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>',
  chart:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>',
  upload:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>',
  plus:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg>',
  cfg:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><circle cx="12" cy="12" r="3"/><path d="M19.07 4.93l-1.41 1.41M5.34 18.66l-1.41 1.41M2 12h2M20 12h2M19.07 19.07l-1.41-1.41M5.34 5.34L3.93 3.93M12 2v2M12 20v2"/></svg>',
  lock:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>',
  people:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>',
  search:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>',
  print:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>',
  backup:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>',
  bell:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>',
  check:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>',
  log:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>',
  clock:'<svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>',
};
function buildNav(){
  const items=role==='ges'?NAV_GES:NAV_DIR;
  let h='',lastSec=null;
  items.forEach(it=>{
    if(it.sec&&it.sec!==lastSec){h+=`<div class="sbsec">${it.sec}</div>`;lastSec=it.sec;}
    const pendInd=it.id==='indenizacoes'?getMes().indenizacoes.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length:0;
    const pendAprov=it.id==='aprovacoes'?getAprovacoes().filter(a=>a.status==='pendente').length:0;
    const pend=pendInd||pendAprov;
    h+=`<div class="ni${page===it.id?' on':''}" id="ni-${it.id}" onclick="nav('${it.id}')">${IC[it.ic]}${it.l}${pend?`<span class="nbadge">${pend}</span>`:''}</div>`;
  });
  document.getElementById('sbn').innerHTML=h;
}
function nav(p){
  page=p;buildNav();
  const c=document.getElementById('pc');c.innerHTML='';
  const d=document.createElement('div');d.className='page on';d.id='pg-'+p;c.appendChild(d);
  PAGES[p]&&PAGES[p](d);
  document.getElementById('main').scrollTop=0;
}

// ══════════════ PAGES ══════════════
const PAGES={

dashboard(el){
  const mes=getMes();
  const ind=mes.indenizacoes.filter(i=>!isFer(i));
  const total=ind.reduce((s,i)=>s+i.valorLiq,0);
  const pend=ind.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length;
  const pago=ind.filter(i=>i.status==='PAGO').length;
  const condic=ind.filter(i=>i.status==='DEPENDE DO BOLETO').length;
  const benef=mes.meta.beneficiarios||new Set(ind.map(i=>i.nome)).size;
  const sortedHist=[...HIST].sort((a,b)=>a.key.localeCompare(b.key));
  const hist6=sortedHist.slice(-6);
  const maxV=Math.max(...hist6.map(h=>h.valor),1);
  const mesOpts=Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===mesAtivo?'selected':''}>${DB.meses[k].label}</option>`).join('');

  // SVG bar chart
  const svgW=460,svgH=130,padL=0,padB=24,padT=20;
  const bW=Math.floor((svgW-padL)/hist6.length);
  const bGap=6;
  const bMax=svgH-padB-padT;
  const svgBars=hist6.map((h,i)=>{
    const bH=Math.round(h.valor/maxV*bMax);
    const x=padL+i*bW+bGap/2;
    const y=padT+(bMax-bH);
    const isLast=i===hist6.length-1;
    const lbl=h.label.slice(0,3);
    const valLbl=isLast?brl(h.valor).replace('R$ ',''):'';
    return `<g>
      <rect x="${x}" y="${y}" width="${bW-bGap}" height="${bH}" rx="4" fill="${isLast?'#F5C518':'var(--bg4)'}" class="bar-col"/>
      ${isLast?`<text x="${x+(bW-bGap)/2}" y="${y-6}" text-anchor="middle" font-size="9" fill="#F5C518" font-family="IBM Plex Mono,monospace" font-weight="600">${valLbl}</text>`:''}
      <text x="${x+(bW-bGap)/2}" y="${svgH-6}" text-anchor="middle" font-size="9" fill="#555" font-family="IBM Plex Sans,sans-serif">${lbl}</text>
    </g>`;
  }).join('');

  // Tabela de indenizações — todas
  const rows=mes.indenizacoes.map(i=>`
    <tr onclick="showDetail(${i.id},'${mesAtivo}')" style="cursor:pointer">
      <td style="padding:12px 14px">
        <div style="font-size:14px;font-weight:500;color:var(--w);line-height:1.3">${isFer(i)?'<span style="color:#a78bfa">Feriado</span>':i.nome}</div>
        ${i.parcela?`<div style="font-size:11px;color:var(--w4);margin-top:2px">Parcela ${i.parcela}</div>`:''}
        ${i.obs&&i.obs.length<60?`<div style="font-size:11px;color:var(--orange);margin-top:2px">${i.obs}</div>`:''}
      </td>
      <td style="padding:12px 14px;color:var(--w3);font-size:13px;white-space:nowrap">${fdt(i.data)}</td>
      <td style="padding:12px 14px"><span class="badge ${statusColor(i.status)}" style="font-size:11px;padding:4px 10px">${i.status}</span></td>
      <td style="padding:12px 14px;text-align:right;font-family:'IBM Plex Mono',monospace;font-size:15px;font-weight:600;color:${i.valorLiq>0?'#F5C518':'var(--w4)'}">${i.valorLiq>0?brl(i.valorLiq):'—'}</td>
    </tr>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Indenizações</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">${mes.label||mesAtivo}</h1>
      </div>
      <select class="pbtn" onchange="mesAtivo=this.value;nav('dashboard')" style="cursor:pointer;font-size:13px">${mesOpts}</select>
    </div>

    <!-- AVISO DO GESTOR -->
    ${DB.avisoGestor?`
    <div style="background:rgba(234,88,12,.08);border:1px solid rgba(234,88,12,.25);border-radius:12px;padding:12px 16px;margin-bottom:16px;display:flex;align-items:flex-start;gap:10px" class="au">
      <span style="font-size:18px;flex-shrink:0">📢</span>
      <div style="flex:1">
        <div style="font-size:11px;font-weight:700;color:#fb923c;text-transform:uppercase;letter-spacing:.4px;margin-bottom:3px">Aviso do Gestor</div>
        <div style="font-size:13px;color:var(--w2);line-height:1.5">${DB.avisoGestor}</div>
      </div>
      ${role==='ges'?`<button onclick="DB.avisoGestor='';saveDB();nav('dashboard')" style="background:none;border:none;color:var(--w4);cursor:pointer;font-size:20px;line-height:1;padding:0" title="Remover aviso">×</button>`:''}
    </div>`:''}
    ${role==='ges'?`
    <div style="margin-bottom:16px" class="au">
      <div style="display:flex;gap:8px">
        <input class="fi" id="avisoInput" placeholder="📢 Deixar aviso para a diretoria no dashboard..." value="${DB.avisoGestor||''}" style="flex:1;font-size:13px"/>
        <button class="btn btn-yg btn-sm" onclick="const v=document.getElementById('avisoInput').value.trim();DB.avisoGestor=v;saveDB();nav('dashboard');toast(v?'Aviso publicado!':'Aviso removido.','suc')">
          ${DB.avisoGestor?'Atualizar':'Publicar'}
        </button>
      </div>
    </div>`:''}

    <!-- HERO TOTAL -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:18px;padding:28px 30px;margin-bottom:16px;position:relative;overflow:hidden">
      <div style="position:absolute;top:-40px;right:-40px;width:200px;height:200px;background:radial-gradient(circle,rgba(245,197,24,.08),transparent 70%);pointer-events:none"></div>
      <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:10px">Total indenizado no mês</div>
      <div style="font-size:44px;font-weight:700;color:var(--y);font-family:'IBM Plex Mono',monospace;letter-spacing:-1.5px;line-height:1;margin-bottom:16px">${brl(total)}</div>
      <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:0;border-top:1px solid var(--bg3);padding-top:16px">
        ${[
          ['Pagamentos',ind.length,'var(--w)'],
          ['Pagos',pago,'#4ade80'],
          ['Pendentes',pend,'#fb923c'],
          ['Beneficiários',benef,'var(--w)']
        ].map(([l,v,c])=>`
        <div style="text-align:center;padding:0 10px;border-right:1px solid var(--bg3)">
          <div style="font-size:24px;font-weight:700;color:${c};font-family:'IBM Plex Mono',monospace">${v}</div>
          <div style="font-size:11px;color:var(--w3);margin-top:3px;font-weight:500">${l}</div>
        </div>`).join('')}
      </div>
    </div>

    <!-- CHART + RESUMO -->
    <div style="display:grid;grid-template-columns:3fr 2fr;gap:14px;margin-bottom:16px" class="au2">
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px">
        <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:.6px;font-weight:600;margin-bottom:14px">Evolução — últimos meses</div>
        <svg width="100%" viewBox="0 0 ${svgW} ${svgH}" xmlns="http://www.w3.org/2000/svg">${svgBars}</svg>
      </div>
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px;display:flex;flex-direction:column;gap:0">
        <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:.6px;font-weight:600;margin-bottom:14px">Detalhes do mês</div>
        ${[
          ['Dias com pagamentos', mes.meta.diasPag||'—', ''],
          ['Pagamentos condicionais', condic||mes.meta.condicionais||'—', condic>0?'color:var(--blue)':''],
          ['Valor médio por pag.', ind.length>0?brl(total/ind.length):'—', 'font-family:IBM Plex Mono,monospace'],
          ['Ticket máximo', ind.length>0?brl(Math.max(...ind.map(i=>i.valorLiq))):'—', 'font-family:IBM Plex Mono,monospace'],
        ].map(([l,v,s])=>`
        <div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid var(--bg3);font-size:13px">
          <span style="color:var(--w3)">${l}</span>
          <strong style="color:var(--w);${s}">${v}</strong>
        </div>`).join('')}
      </div>
    </div>

    <!-- TABELA GRANDE -->
    <div class="au3" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;overflow:hidden">
      <div style="display:flex;align-items:center;justify-content:space-between;padding:18px 20px 14px;border-bottom:1px solid var(--bg3)">
        <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:.6px;font-weight:600">Todas as indenizações</div>
        <button class="btn btn-yg btn-sm" onclick="nav('indenizacoes')">Ver detalhes →</button>
      </div>
      <table style="width:100%;border-collapse:collapse">
        <thead>
          <tr style="border-bottom:1px solid var(--bg3)">
            <th style="text-align:left;padding:10px 14px;font-size:11px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.4px">Associado</th>
            <th style="text-align:left;padding:10px 14px;font-size:11px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.4px">Data</th>
            <th style="text-align:left;padding:10px 14px;font-size:11px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.4px">Status</th>
            <th style="text-align:right;padding:10px 14px;font-size:11px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.4px">Valor Líquido</th>
          </tr>
        </thead>
        <tbody>${rows}</tbody>
      </table>
    </div>`;
},

calendario(el){
  const mes=getMes();
  const {ano,mes:mesN}=mes;
  const totalDias=daysInMonth(ano,mesN);
  const primeiroDia=firstDayOfMonth(ano,mesN);
  const payMap={};
  mes.indenizacoes.forEach(i=>{
    if(!i.data)return;
    const d=parseInt(i.data.split('-')[2]);
    if(!payMap[d])payMap[d]=[];
    payMap[d].push(i);
  });
  const today=new Date();
  const isCurrentMonth=(today.getFullYear()===ano&&today.getMonth()+1===mesN);
  const ind=mes.indenizacoes.filter(i=>!isFer(i));
  const total=ind.reduce((s,i)=>s+i.valorLiq,0);
  const mesOpts=Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===mesAtivo?'selected':''}>${DB.meses[k].label}</option>`).join('');

  const days=['DOM','SEG','TER','QUA','QUI','SEX','SÁB'];

  // Build big calendar cells
  let cells=days.map(d=>`
    <div style="text-align:center;padding:8px 0;font-size:11px;font-weight:700;color:var(--w4);text-transform:uppercase;letter-spacing:.6px">${d}</div>`).join('');

  for(let i=0;i<primeiroDia;i++) cells+=`<div style="min-height:72px;background:var(--bg3);border-radius:6px;opacity:.15"></div>`;

  for(let d=1;d<=totalDias;d++){
    const ents=payMap[d]||[];
    const isH=ents.some(e=>e.status==='FERIADO');
    const hasPay=ents.filter(e=>!isFer(e)).length>0;
    const isToday=(isCurrentMonth&&today.getDate()===d);
    const dayTotal=ents.filter(e=>!isFer(e)).reduce((s,e)=>s+e.valorLiq,0);

    let bg='var(--bg3)';
    let border='transparent';
    if(isH){bg='rgba(124,58,237,.08)';border='rgba(124,58,237,.25)';}
    else if(hasPay){border='rgba(245,197,24,.25)';}
    if(isToday){border='var(--y)';}

    const numStyle=isToday
      ? `width:24px;height:24px;border-radius:50%;background:var(--y);display:inline-flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:var(--bg)`
      : `font-size:13px;font-weight:600;color:${hasPay?'var(--w)':'var(--w4)'}`;

    const pills=ents.slice(0,3).map(e=>{
      const colors={
        'FERIADO':['rgba(124,58,237,.18)','#a78bfa'],
        'PAGO':['rgba(22,163,74,.18)','#4ade80'],
        'DEPENDE DO BOLETO':['rgba(37,99,235,.15)','#93c5fd'],
        'PENDENTE':['rgba(234,88,12,.18)','#fb923c'],
        'VALOR RESTANTE':['rgba(245,197,24,.15)','#f5c518'],
      };
      const [bg2,fc]=colors[e.status]||colors['PENDENTE'];
      const firstName=e.nome.split(' ')[0];
      return `<div style="background:${bg2};color:${fc};border-radius:3px;padding:2px 5px;font-size:10px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;margin-bottom:2px">${firstName}</div>`;
    }).join('');

    const totalLine=dayTotal>0?`<div style="font-size:9px;color:var(--y);font-family:'IBM Plex Mono',monospace;font-weight:600;margin-top:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${brl(dayTotal).replace('R$ ','')}</div>`:'';

    cells+=`<div onclick="showDay(${d},'${mesAtivo}')" style="background:${bg};border:1px solid ${border};border-radius:6px;padding:4px;cursor:pointer;transition:all .15s;display:flex;flex-direction:column;min-height:68px;overflow:hidden"
      onmouseover="this.style.background='var(--bg4)'" onmouseout="this.style.background='${bg}'">
      <div style="margin-bottom:5px"><span style="${numStyle}">${d}</span></div>
      ${pills}
      ${totalLine}
    </div>`;
  }

  // Legend
  const legend=[
    ['var(--y)','Dia atual'],
    ['rgba(245,197,24,.25)','Com pagamento'],
    ['rgba(124,58,237,.25)','Feriado'],
  ].map(([c,l])=>`<div style="display:flex;align-items:center;gap:6px;font-size:11px;color:var(--w3)"><div style="width:12px;height:12px;border-radius:3px;border:1.5px solid ${c}"></div>${l}</div>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Calendário</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">${mes.label||mesAtivo}</h1>
      </div>
      <div style="display:flex;gap:8px;align-items:center">
        <select class="pbtn" onchange="mesAtivo=this.value;nav('calendario')" style="cursor:pointer;font-size:13px">${mesOpts}</select>
        <button class="btn btn-g btn-sm" onclick="window.print()" title="Imprimir calendário">
          <svg viewBox="0 0 24 24"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
          Imprimir
        </button>
      </div>
    </div>

    <!-- KPIs do mês -->
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:10px;margin-bottom:16px" class="au1">
      <div style="background:var(--y);border-radius:14px;padding:18px 20px">
        <div style="font-size:11px;color:rgba(0,0,0,.5);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:6px">Total do Mês</div>
        <div style="font-size:22px;font-weight:700;color:var(--bg);font-family:'IBM Plex Mono',monospace;letter-spacing:-.5px">${brl(total)}</div>
      </div>
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px 20px">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:6px">Pagamentos</div>
        <div style="font-size:28px;font-weight:700;color:var(--w);font-family:'IBM Plex Mono',monospace">${ind.length}</div>
      </div>
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px 20px">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:6px">Dias c/ Pagamento</div>
        <div style="font-size:28px;font-weight:700;color:var(--w);font-family:'IBM Plex Mono',monospace">${mes.meta.diasPag||'—'}</div>
      </div>
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px 20px">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:6px">Condicionais</div>
        <div style="font-size:28px;font-weight:700;color:#93c5fd;font-family:'IBM Plex Mono',monospace">${mes.meta.condicionais||'—'}</div>
      </div>
    </div>

    <!-- CALENDÁRIO GRANDE -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:12px;padding:12px;margin-bottom:12px">
      <div style="overflow-x:auto;-webkit-overflow-scrolling:touch;width:100%">
        <div style="display:grid;grid-template-columns:repeat(7,minmax(44px,1fr));gap:3px;min-width:320px;width:max(100%,320px)">${cells}</div>
      </div>
    </div>

    <!-- LEGENDA -->
    <div class="au3" style="display:flex;gap:18px;padding:0 4px">${legend}</div>`;
},

indenizacoes(el){renderList(el);},

acumulado(el){
  const sorted=[...HIST].sort((a,b)=>b.key.localeCompare(a.key));
  const totalAcum=sorted.reduce((s,h)=>s+h.valor,0);
  const totalPag=sorted.reduce((s,h)=>s+h.pagamentos,0);
  const maxV=Math.max(...sorted.map(h=>h.valor),1);
  const rows=sorted.map(h=>`
    <div class="hr" onclick="showMonthSumm('${h.key}','${h.label}',${h.valor},${h.pagamentos})">
      <div style="flex:1"><div class="hm">${DB.meses[h.key]?DB.meses[h.key].label:h.label}</div>
        <div class="hp">${h.pagamentos} pagamentos</div>
        <div class="hb"><div class="hbf" style="width:${Math.round(h.valor/maxV*100)}%"></div></div>
      </div>
      <div style="text-align:right"><div class="hv">${brl(h.valor)}</div><div style="font-size:10px;color:var(--w4)">${brl(h.valor/h.pagamentos)}/pag</div></div>
    </div>`).join('');
  el.innerHTML=`
    <div class="tb au"><div><h1>Acumulado</h1><p>Total indenizado desde o início</p></div></div>
    <div class="ah au1">
      <div class="al">Total Indenizado Acumulado</div>
      <div class="at">${brl(totalAcum)}</div>
      <div class="as">${totalPag} pagamentos · ${sorted.length} meses registrados</div>
    </div>
    <div class="kg au2" style="margin-bottom:14px">
      <div class="kpi"><div class="kl">Ticket médio geral</div><div class="kv">${brl(totalAcum/Math.max(totalPag,1))}</div></div>
      <div class="kpi"><div class="kl">Média mensal</div><div class="kv">${brl(totalAcum/Math.max(sorted.length,1))}</div></div>
      <div class="kpi ky"><div class="kl">Meses registrados</div><div class="kv">${sorted.length}</div></div>
    </div>
    <div class="card au3"><div class="ch"><span class="ct">Histórico por mês</span></div>${rows||'<p style="color:var(--w3);font-size:12px">Nenhum histórico ainda.</p>'}</div>`;
},

importar(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const now=new Date();
  let mesOpts='';
  for(let y=now.getFullYear()+2;y>=2026;y--){
    for(let m=12;m>=1;m--){
      if(y===2026&&m<1)break;
      const key=`${y}-${String(m).padStart(2,'0')}`;
      const label=`${MESES_PT[m]} ${y}`;
      const isCur=(y===now.getFullYear()&&m===now.getMonth()+1);
      mesOpts+=`<option value="${key}" ${isCur?'selected':''}>${label}</option>`;
    }
  }
  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Gestão</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Importar Dados</h1>
      </div>
    </div>

    <!-- ABAS -->
    <div class="au1" style="display:flex;gap:4px;margin-bottom:16px">
      <button class="tab on" id="tabPdf" onclick="switchImportTab('pdf')">
        <svg width="13" height="13" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="2" style="display:inline;vertical-align:middle;margin-right:4px"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
        Upload PDF
      </button>
      <button class="tab" id="tabTexto" onclick="switchImportTab('texto')">
        <svg width="13" height="13" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="2" style="display:inline;vertical-align:middle;margin-right:4px"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="9" y1="9" x2="15" y2="9"/><line x1="9" y1="12" x2="15" y2="12"/><line x1="9" y1="15" x2="13" y2="15"/></svg>
        Colar Texto
      </button>
    </div>

    <!-- PAINEL PDF -->
    <div id="painelPdf">
      <div class="card au2" id="importCard">
        <div class="ch"><span class="ct">Configurar importação</span></div>
        <div style="margin-bottom:14px">
          <label class="fl" style="display:block;margin-bottom:5px">Mês de referência</label>
          <select class="fs" id="importMesKey" style="max-width:260px">${mesOpts}</select>
          <div style="font-size:11px;color:var(--w3);margin-top:5px">O sistema detecta automaticamente, mas você pode corrigir aqui.</div>
        </div>
        <div class="upz" id="upz"
          ondragover="event.preventDefault();this.classList.add('drag')"
          ondragleave="this.classList.remove('drag')"
          ondrop="dropPDF(event)">
          <input type="file" accept=".pdf" onchange="handlePDF(this.files[0])"/>
          <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
          <div class="ut">Arraste o PDF aqui ou clique para selecionar</div>
          <div class="us">Cronograma de Indenizações — qualquer mês/ano</div>
        </div>
        <div class="info-box" style="margin-top:12px">
          <strong>O sistema lê automaticamente:</strong>
          Nomes · Valores · Datas · Status · Parcelas · Observações
        </div>
      </div>
    </div>

    <!-- PAINEL TEXTO -->
    <div id="painelTexto" style="display:none">
      <div class="card au2">
        <div class="ch"><span class="ct">Colar tabela de indenizações</span></div>
        <div style="margin-bottom:14px">
          <label class="fl" style="display:block;margin-bottom:5px">Mês de referência</label>
          <select class="fs" id="textoMesKey" style="max-width:260px">${mesOpts}</select>
        </div>
        <div style="margin-bottom:10px">
          <label class="fl" style="display:block;margin-bottom:5px">Cole os dados aqui</label>
          <textarea class="fta" id="textoInput" style="min-height:220px;font-family:'IBM Plex Mono',monospace;font-size:12px;line-height:1.6"
            placeholder="Cole aqui o texto copiado da planilha ou PDF. Exemplo:

Beneficiário	Valor	Data	Parcelas	Status
PAULO FERNANDES	R$ 17.335,00	01/06/2026	1/2	PENDENTE
RAFAEL LEANDRO	R$ 16.353,00	08/06/2026	-	PAGO

Aceita separação por tabulação, espaços ou colunas misturadas."></textarea>
        </div>
        <button class="btn btn-p" onclick="processarTexto()">
          <svg viewBox="0 0 24 24"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
          Processar e visualizar
        </button>
        <div class="info-box" style="margin-top:12px">
          <strong>Como usar:</strong>
          Copie a tabela de qualquer lugar (planilha, PDF, e-mail) e cole acima. O sistema reconhece automaticamente nomes, valores em R$, datas, parcelas e status.
        </div>
      </div>
      <div id="textoPreview"></div>
    </div>`;
},

novo(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const mes=getMes();
  const ed=editId?mes.indenizacoes.find(i=>i.id===editId):null;
  const v=f=>ed?(ed[f]??''):'';
  const mesOpts=Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===mesAtivo?'selected':''}>${DB.meses[k].label}</option>`).join('');
  el.innerHTML=`
    <div class="tb au"><div><h1>${ed?'Editar Indenização':'Nova Indenização'}</h1><p>${ed?'Altere e salve':'Preencha os dados'}</p></div>${ed?`<div class="tbr"><span class="shb">Editando #${ed.id}</span></div>`:''}</div>
    <div class="fc au1">
      <div class="fg">
        <div class="fgr fw"><label class="fl">Mês de referência</label><select class="fs" id="fmes" onchange="mesAtivo=this.value;editId=null;nav('novo')">${mesOpts}</select></div>
        <div class="fgr fw"><label class="fl">Nome do Associado *</label><input class="fi" id="fn" placeholder="Nome completo" value="${v('nome')}"/></div>
        <div class="fgr"><label class="fl">Placa do Veículo</label><input class="fi" id="fplaca" placeholder="Ex: ABC-1234" value="${v('placa')}" style="text-transform:uppercase" oninput="this.value=this.value.toUpperCase()"/></div>
        <div class="fgr"><label class="fl">Veículo (Marca/Modelo)</label><input class="fi" id="fveiculo" placeholder="Ex: Honda CG 160 FAN" value="${v('veiculo')}"/></div>
        <div class="fgr"><label class="fl">Data do Pagamento *</label><input class="fi" id="fdt" type="date" value="${v('data')||new Date().toISOString().split('T')[0]}"/></div>
        <div class="fgr"><label class="fl">Parcela</label><input class="fi" id="fp" placeholder="Ex: 1/2" value="${v('parcela')}"/></div>
        <div class="fgr"><label class="fl">Tipo de Evento</label>
          <select class="fs" id="ftipo">
            <option value="">— Selecione —</option>
            ${['Colisão','Roubo/Furto','Perda Total','Outros'].map(t=>`<option value="${t}" ${v('tipo')===t?'selected':''}>${t}</option>`).join('')}
          </select></div>
        <div class="fgr"><label class="fl">Status</label>
          <select class="fs" id="fst">${['PAGO','PENDENTE','DEPENDE DO BOLETO','VALOR RESTANTE','FERIADO'].map(s=>`<option ${v('status')===s?'selected':''}>${s}</option>`).join('')}</select></div>
        <div class="fgr"><label class="fl">Valor FIPE (R$)</label><input class="fi" id="ff" type="number" step="0.01" placeholder="0,00" value="${v('valorFipe')||''}" oninput="calcPrev()"/></div>
      </div>
      <div class="div"></div>
      <div style="font-size:10px;font-weight:600;color:var(--w3);text-transform:uppercase;letter-spacing:.6px;margin-bottom:10px">Descontos</div>
      <div class="fg">
        <div class="fgr"><label class="fl">Taxa Transferência</label><input class="fi" id="ftt" type="number" step="0.01" placeholder="0,00" value="${v('taxaTransf')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Cota de Participação</label><input class="fi" id="fcp" type="number" step="0.01" placeholder="0,00" value="${v('cotaPart')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Boletos Contribuição</label><input class="fi" id="fbc" type="number" step="0.01" placeholder="0,00" value="${v('bolContrib')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Licenciamento</label><input class="fi" id="flic" type="number" step="0.01" placeholder="0,00" value="${v('licenciamento')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">IPVA</label><input class="fi" id="fipva" type="number" step="0.01" placeholder="0,00" value="${v('ipva')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Bombeiro</label><input class="fi" id="fbomb" type="number" step="0.01" placeholder="0,00" value="${v('bombeiro')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Multa</label><input class="fi" id="fmul" type="number" step="0.01" placeholder="0,00" value="${v('multa')||0}" oninput="calcPrev()"/></div>
        <div class="fgr"><label class="fl">Financiamento</label><input class="fi" id="ffin" type="number" step="0.01" placeholder="0,00" value="${v('financiamento')||0}" oninput="calcPrev()"/></div>
        <div class="fgr fw"><label class="fl">Valor Líquido a Pagar *</label><input class="fi" id="fliq" type="number" step="0.01" placeholder="Calculado automaticamente" value="${v('valorLiq')||''}"/></div>
      </div>
      <div class="cp2" id="cprev"></div>
      <div class="fgr fw" style="margin-top:10px">
        <label class="fl">Consultor Responsável</label>
        <select class="fs" id="fcons">
          <option value="">— Selecione o consultor —</option>
          ${getConsultores().map(c=>`<option value="${c}" ${v('consultor')===c?'selected':''}>${c}</option>`).join('')}
        </select>
      </div>
      <div class="fgr" style="margin-top:10px"><label class="fl">Observações</label><textarea class="fta" id="fobs">${v('obs')}</textarea></div>
      <div class="fgr" style="margin-top:10px">
        <label class="fl">Comprovante (imagem ou PDF)</label>
        <div style="border:1.5px dashed var(--bg5);border-radius:var(--r);padding:14px;text-align:center;cursor:pointer;transition:all .2s;background:var(--bg3);position:relative" 
          id="compArea" onmouseover="this.style.borderColor='var(--y)'" onmouseout="this.style.borderColor='var(--bg5)'">
          <input type="file" accept="image/*,.pdf" style="position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%" onchange="handleComprovante(this)"/>
          <div id="compLabel" style="font-size:12px;color:var(--w3)">${v('comprovante')?'<span style="color:var(--green)">✓ Comprovante anexado</span>':'<svg width="20" height="20" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.75" style="display:inline;vertical-align:middle;margin-right:6px"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>Clique ou arraste para anexar comprovante'}</div>
        </div>
        ${v('comprovante')?`<div style="margin-top:6px;display:flex;align-items:center;gap:8px"><span style="font-size:11px;color:var(--green)">Comprovante salvo</span><button type="button" class="btn btn-d btn-sm" onclick="window._comprovante=null;document.getElementById('compLabel').innerHTML='Clique ou arraste para anexar comprovante'">Remover</button></div>`:''}
      </div>
      <div class="fa">
        <button class="btn btn-p" onclick="salvar()">
          <svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/></svg>
          ${ed?'Salvar alterações':'Adicionar indenização'}
        </button>
        <button class="btn btn-g" onclick="editId=null;nav('gerenciar')">Cancelar</button>
      </div>
    </div>`;
  calcPrev();
},

gerenciar(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const mes=getMes();
  const mesOpts=Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===mesAtivo?'selected':''}>${DB.meses[k].label}</option>`).join('');
  const rows=mes.indenizacoes.map(i=>`
    <tr>
      <td class="nc">${i.nome}</td>
      <td class="mc">${fdt(i.data)}</td>
      <td><span class="badge ${statusColor(i.status)}">${i.status}</span></td>
      <td class="vc">${i.valorLiq>0?brl(i.valorLiq):'—'}</td>
      <td><div class="ra">
        <button class="rab re" onclick="startEdit(${i.id})" title="Editar"><svg viewBox="0 0 24 24"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        <button class="rab rd" onclick="delItem(${i.id})" title="Excluir"><svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>
      </div></td>
    </tr>`).join('');
  el.innerHTML=`
    <div class="tb au"><div><h1>Gerenciar</h1><p>${mes.label||mesAtivo}</p></div>
      <div class="tbr">
        <select class="pbtn" onchange="mesAtivo=this.value;nav('gerenciar')" style="cursor:pointer">${mesOpts}</select>
        <button class="btn btn-p btn-sm" onclick="editId=null;window._comprovante=null;nav('novo')">
          <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>Nova
        </button>
        <button class="btn btn-d btn-sm" onclick="excluirMes('${mesAtivo}')" title="Excluir este mês inteiro">
          <svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg>Excluir mês
        </button>
      </div>
    </div>
    <div class="card au1">
      <div class="tw"><table class="dt">
        <thead><tr><th>Associado</th><th>Data</th><th>Status</th><th style="text-align:right">Valor Líquido</th><th></th></tr></thead>
        <tbody>${rows||'<tr><td colspan="5" style="text-align:center;padding:16px;color:var(--w3)">Nenhum registro</td></tr>'}</tbody>
      </table></div>
    </div>`;
},

senhas(el){
  // Accessible to ALL users — each changes their own password
  const u=currentUser;
  if(!u){el.innerHTML='<p style="color:var(--w3);padding:20px">Faça login primeiro.</p>';return;}

  const isGes=role==='ges';

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Segurança</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Alterar Senha</h1>
      </div>
    </div>

    <!-- INFO USUÁRIO ATUAL -->
    <div class="au1" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:16px;padding:18px;margin-bottom:16px;display:flex;align-items:center;gap:14px">
      <div style="width:46px;height:46px;border-radius:50%;background:var(--yg);display:flex;align-items:center;justify-content:center;font-weight:800;font-size:16px;color:#000;flex-shrink:0;box-shadow:0 0 16px rgba(240,180,41,.3)">
        ${u.nome.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase()}
      </div>
      <div>
        <div style="font-size:15px;font-weight:600;color:var(--w)">${u.nome}</div>
        <div style="font-size:12px;color:var(--w3);margin-top:2px">Login: <strong style="color:var(--w2)">${u.login}</strong> · ${isGes?'Gestor (Administrador)':'Diretoria (Visualização)'}</div>
      </div>
    </div>

    <!-- TROCAR MINHA SENHA -->
    <div class="au2" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:16px;padding:22px;margin-bottom:14px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:16px">Trocar minha senha</div>
      <div style="display:flex;flex-direction:column;gap:12px;max-width:420px">
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Senha atual</label>
          <input class="fi" type="password" id="spAtual" placeholder="Confirme sua senha atual"/>
        </div>
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Nova senha</label>
          <input class="fi" type="password" id="spNova" placeholder="Mínimo 4 caracteres"/>
        </div>
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Confirmar nova senha</label>
          <input class="fi" type="password" id="spConf" placeholder="Repita a nova senha" onkeydown="if(event.key==='Enter')trocarMinhaSenha()"/>
        </div>
        <button class="btn btn-p" onclick="trocarMinhaSenha()" style="align-self:flex-start">
          <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          Salvar nova senha
        </button>
      </div>
    </div>

    ${isGes?`
    <!-- GESTOR: alterar senha de outros usuários -->
    <div class="au3" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:16px;padding:22px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:16px">Redefinir senha de outro usuário</div>
      <div style="display:flex;flex-direction:column;gap:12px;max-width:420px">
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Usuário</label>
          <select class="fs" id="spUsuario">
            ${getUsers().filter(x=>x.id!==u.id&&x.ativo).map(x=>`<option value="${x.id}">${x.nome} (${x.login})</option>`).join('')}
          </select>
        </div>
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Nova senha</label>
          <input class="fi" type="password" id="spOutraNova" placeholder="Nova senha para o usuário"/>
        </div>
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Confirmar</label>
          <input class="fi" type="password" id="spOutraConf" placeholder="Repita a senha" onkeydown="if(event.key==='Enter')redefinirSenhaOutro()"/>
        </div>
        <div style="background:rgba(234,88,12,.08);border:1px solid rgba(234,88,12,.2);border-radius:8px;padding:10px 14px;font-size:12px;color:#fb923c">
          O usuário precisará usar a nova senha no próximo login.
        </div>
        <button class="btn btn-yg" onclick="redefinirSenhaOutro()" style="align-self:flex-start">
          <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          Redefinir senha
        </button>
      </div>
    </div>`:''}`;
},



gesCons(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const cons=getConsultores();
  const rows=cons.map((c,i)=>`
    <div style="display:flex;align-items:center;justify-content:space-between;padding:10px 14px;background:var(--bg3);border-radius:8px;margin-bottom:4px;font-size:13px">
      <span style="font-weight:500;color:var(--w)">${c}</span>
      <button class="rab rd" onclick="removerConsultor('${c.replace(/'/g,"\'")}')"><svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/></svg></button>
    </div>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Gestão</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Consultores</h1>
      </div>
    </div>

    <!-- CADASTRAR NOVO -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;margin-bottom:16px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">Cadastrar novo consultor</div>
      <div style="display:flex;gap:10px;flex-wrap:wrap">
        <input class="fi" id="novoConsInput" placeholder="Nome do consultor" style="flex:1;min-width:200px" onkeydown="if(event.key==='Enter')addConsultor()"/>
        <button class="btn btn-p" onclick="addConsultor()">
          <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          Adicionar
        </button>
      </div>
    </div>

    <!-- LISTA -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">${cons.length} consultores cadastrados</div>
      <div id="consListDiv">${rows}</div>
    </div>`;
},

busca(el){
  const q=(window._searchQuery||'').trim();
  const results=[];
  if(q.length>=2){
    Object.entries(DB.meses).forEach(([mk,mes])=>{
      mes.indenizacoes.filter(i=>!isFer(i)).forEach(i=>{
        const hay=[i.nome,i.placa||'',i.consultor||'',i.obs||''].join(' ').toUpperCase();
        if(hay.includes(q.toUpperCase()))results.push({...i,mesKey:mk,mesLabel:mes.label});
      });
    });
  }
  results.sort((a,b)=>b.data.localeCompare(a.data));
  const totalRes=results.reduce((s,i)=>s+i.valorLiq,0);

  const rows=results.map(i=>`
    <tr onclick="showDetail(${i.id},'${i.mesKey}')" style="cursor:pointer">
      <td style="padding:10px 12px"><div style="font-weight:500;color:var(--w);font-size:13px">${i.nome}</div>${i.placa?`<div style="font-size:11px;color:var(--y);font-family:'IBM Plex Mono',monospace;margin-top:2px">${i.placa}</div>`:''}</td>
      <td style="padding:10px 12px;color:var(--w3);font-size:12px">${i.mesLabel}</td>
      <td style="padding:10px 12px;color:var(--w3);font-size:12px">${fdt(i.data)}</td>
      <td style="padding:10px 12px">${i.consultor?`<span style="font-size:11px;color:var(--w3)">${i.consultor}</span>`:'—'}</td>
      <td style="padding:10px 12px"><span class="badge ${statusColor(i.status)}">${i.status}</span></td>
      <td style="padding:10px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);font-size:13px">${brl(i.valorLiq)}</td>
    </tr>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Pesquisa</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Busca por Placa ou Nome</h1>
      </div>
    </div>
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;margin-bottom:16px">
      <div style="display:flex;gap:10px">
        <input class="fi" id="searchInp" placeholder="Digite a placa (ex: ABC-1234) ou nome do associado..." 
          value="${q}" style="flex:1;font-size:14px;padding:11px 14px"
          onkeydown="if(event.key==='Enter'){window._searchQuery=this.value;nav('busca')}"/>
        <button class="btn btn-p" onclick="window._searchQuery=document.getElementById('searchInp').value;nav('busca')">
          <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          Buscar
        </button>
        ${q?`<button class="btn btn-g" onclick="window._searchQuery='';nav('busca')">Limpar</button>`:''}
      </div>
    </div>
    ${q.length>=2?`
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;overflow:hidden">
      <div style="padding:14px 18px;border-bottom:1px solid var(--bg3);display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px">
        <span style="font-size:12px;color:var(--w3)">${results.length} resultado${results.length!==1?'s':''} para <strong style="color:var(--w)">"${q}"</strong></span>
        ${results.length>0?`<span style="font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);font-size:13px">Total: ${brl(totalRes)}</span>`:''}
      </div>
      ${results.length>0?`
      <div style="overflow-x:auto">
        <table style="width:100%;border-collapse:collapse">
          <thead><tr style="border-bottom:1px solid var(--bg3)">
            ${['Associado / Placa','Mês','Data','Consultor','Status','Valor Líquido'].map((h,i)=>`<th style="text-align:${i===5?'right':'left'};padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.4px">${h}</th>`).join('')}
          </tr></thead>
          <tbody>${rows}</tbody>
        </table>
      </div>`:`<div style="padding:32px;text-align:center;color:var(--w3);font-size:13px">Nenhum resultado encontrado para "<strong style="color:var(--w)">${q}</strong>"</div>`}
    </div>`:`<div style="text-align:center;padding:40px;color:var(--w4)">
      <svg width="48" height="48" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.5" style="margin:0 auto 12px;display:block;opacity:.3"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      <div style="font-size:14px;font-weight:500;color:var(--w3)">Digite pelo menos 2 caracteres para buscar</div>
      <div style="font-size:12px;color:var(--w4);margin-top:6px">Busca por placa, nome do associado ou consultor em todos os meses</div>
    </div>`}`;
  setTimeout(()=>document.getElementById('searchInp')&&document.getElementById('searchInp').focus(),100);
},

backup(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const totalMeses=Object.keys(DB.meses).length;
  const totalInd=Object.values(DB.meses).reduce((s,m)=>s+m.indenizacoes.length,0);
  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Sistema</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Backup de Dados</h1>
      </div>
    </div>

    <!-- STATUS -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px;margin-bottom:14px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">Status atual</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:10px">
        ${[['Meses salvos',totalMeses,''],['Total registros',totalInd,''],['Consultores',getConsultores().length,'']].map(([l,v])=>`
        <div style="background:var(--bg3);border-radius:10px;padding:14px;text-align:center">
          <div style="font-size:22px;font-weight:700;font-family:'IBM Plex Mono',monospace;color:var(--y)">${v}</div>
          <div style="font-size:11px;color:var(--w3);margin-top:3px">${l}</div>
        </div>`).join('')}
      </div>
    </div>

    <!-- EXPORTAR -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px;margin-bottom:14px">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:14px">
        <div style="width:38px;height:38px;background:var(--y3);border-radius:10px;display:flex;align-items:center;justify-content:center;flex-shrink:0">
          <svg width="18" height="18" viewBox="0 0 24 24" stroke="#F5C518" fill="none" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        </div>
        <div><div style="font-size:15px;font-weight:600;color:var(--w)">Exportar Backup</div><div style="font-size:12px;color:var(--w3);margin-top:2px">Baixar todos os dados em arquivo JSON</div></div>
      </div>
      <button class="btn btn-p" onclick="exportarBackup()">
        <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        Baixar backup agora
      </button>
    </div>

    <!-- IMPORTAR -->
    <div class="au3" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:14px">
        <div style="width:38px;height:38px;background:rgba(234,88,12,.12);border-radius:10px;display:flex;align-items:center;justify-content:center;flex-shrink:0">
          <svg width="18" height="18" viewBox="0 0 24 24" stroke="#fb923c" fill="none" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
        </div>
        <div><div style="font-size:15px;font-weight:600;color:var(--w)">Restaurar Backup</div><div style="font-size:12px;color:var(--w3);margin-top:2px">Carregar dados de um arquivo JSON salvo anteriormente</div></div>
      </div>
      <div style="background:rgba(234,88,12,.07);border:1px solid rgba(234,88,12,.2);border-radius:8px;padding:10px 14px;margin-bottom:14px;font-size:12px;color:#fb923c">
        <strong>Atenção:</strong> a restauração substituirá todos os dados atuais pelo backup carregado.
      </div>
      <div style="position:relative;display:inline-block">
        <button class="btn btn-g">
          <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
          Selecionar arquivo de backup
        </button>
        <input type="file" accept=".json" onchange="restaurarBackup(this)" style="position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%"/>
      </div>
    </div>`;
},

boasvindas(el){
  const now=new Date();
  const mesK=Object.keys(DB.meses).sort().reverse()[0]||mesAtivo;
  const mes=DB.meses[mesK]||{indenizacoes:[],meta:{},label:'',ano:now.getFullYear(),mes:now.getMonth()+1};
  const ind=mes.indenizacoes.filter(i=>!isFer(i));
  const total=ind.reduce((s,i)=>s+i.valorLiq,0);
  const pago=ind.filter(i=>i.status==='PAGO').length;
  const pend=ind.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length;
  const hr=now.getHours();
  const saud=hr<12?'Bom dia':(hr<18?'Boa tarde':'Boa noite');
  el.innerHTML=`
    <div style="min-height:calc(100vh - 44px);display:flex;flex-direction:column;justify-content:center;padding:30px 24px">
      <!-- LOGO + SAUDAÇÃO -->
      <div style="text-align:center;margin-bottom:36px" class="au">
        <img src="data:image/svg+xml,%3Csvg viewBox='0 0 200 230' xmlns='http://www.w3.org/2000/svg'%3E%3C/svg%3E" alt="AR4" style="width:160px;height:auto"/>
        <div style="font-size:16px;color:var(--w3);font-weight:400;margin-bottom:6px">${saud}, ${currentUser&&currentUser.nome.split(' ')[0]==='Diretoria'?'senhor(a)':'senhor(a) '+currentUser.nome.split(' ')[0]}</div>
        <div style="font-size:32px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1.2">Bem-vindo ao sistema<br>de indenizações</div>
        <div style="font-size:13px;color:var(--w3);margin-top:10px">${now.toLocaleDateString('pt-BR',{weekday:'long',year:'numeric',month:'long',day:'numeric'})}</div>
      </div>

      <!-- RESUMO RÁPIDO -->
      <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:12px;max-width:480px;margin:0 auto 28px;width:100%" class="au1">
        <div style="background:var(--y);border-radius:16px;padding:20px;grid-column:1/-1;text-align:center">
          <div style="font-size:11px;color:rgba(0,0,0,.5);font-weight:600;text-transform:uppercase;letter-spacing:.6px;margin-bottom:6px">Total indenizado — ${mes.label||mesK}</div>
          <div style="font-size:36px;font-weight:700;color:var(--bg);font-family:'IBM Plex Mono',monospace;letter-spacing:-1px">${brl(total)}</div>
        </div>
        <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;text-align:center">
          <div style="font-size:28px;font-weight:700;color:#4ade80;font-family:'IBM Plex Mono',monospace">${pago}</div>
          <div style="font-size:11px;color:var(--w3);margin-top:4px;font-weight:500">Pagamentos realizados</div>
        </div>
        <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;text-align:center">
          <div style="font-size:28px;font-weight:700;color:#fb923c;font-family:'IBM Plex Mono',monospace">${pend}</div>
          <div style="font-size:11px;color:var(--w3);margin-top:4px;font-weight:500">Pagamentos pendentes</div>
        </div>
      </div>

      <!-- BOTÃO ENTRAR -->
      <div style="text-align:center" class="au2">
        <button onclick="nav('dashboard')" style="padding:14px 40px;background:var(--y);color:var(--bg);border:none;border-radius:12px;font-size:15px;font-weight:600;cursor:pointer;font-family:'IBM Plex Sans',sans-serif;letter-spacing:.1px;transition:all .2s" onmouseover="this.style.background='#c9a010'" onmouseout="this.style.background='var(--y)'">
          Entrar no painel →
        </button>
        <div style="font-size:11px;color:var(--w4);margin-top:12px">Clique para acessar o dashboard completo</div>
      </div>
    </div>`;
},

usuarios(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const users=getUsers();
  const perfilLabel=p=>p==='ges'?'Gestor':'Diretoria';
  const perfilColor=p=>p==='ges'?'color:#fb923c':'color:#93c5fd';

  const rows=users.map(u=>`
    <div style="display:flex;align-items:center;gap:12px;padding:12px 14px;background:var(--bg3);border-radius:10px;margin-bottom:6px;border:1px solid ${!u.ativo?'rgba(220,38,38,.2)':'transparent'}">
      <div style="width:36px;height:36px;border-radius:50%;background:${u.perfil==='ges'?'rgba(234,88,12,.2)':'var(--y3)'};display:flex;align-items:center;justify-content:center;font-weight:700;font-size:13px;color:${u.perfil==='ges'?'#fb923c':'var(--y)'};flex-shrink:0">
        ${u.nome.split(' ').map(w=>w[0]).join('').slice(0,2).toUpperCase()}
      </div>
      <div style="flex:1;min-width:0">
        <div style="font-weight:600;font-size:13px;color:${u.ativo?'var(--w)':'var(--w4)'}">${u.nome}</div>
        <div style="font-size:11px;color:var(--w3);margin-top:2px">Login: <strong style="color:var(--w2)">${u.login}</strong> · <span style="${perfilColor(u.perfil)}">${perfilLabel(u.perfil)}</span>${!u.ativo?' · <span style="color:var(--red)">Inativo</span>':''}</div>
      </div>
      <div style="display:flex;gap:6px">
        <button class="rab re" onclick="editarUsuario(${u.id})" title="Editar"><svg viewBox="0 0 24 24"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></button>
        ${u.id!==currentUser?.id?`<button class="rab rd" onclick="toggleUsuario(${u.id})" title="${u.ativo?'Desativar':'Ativar'}"><svg viewBox="0 0 24 24">${u.ativo?'<polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>':'<polyline points="20 6 9 17 4 12"/>'}</svg></button>`:''}
      </div>
    </div>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Gestão</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Usuários</h1>
      </div>
    </div>

    <!-- NOVO USUÁRIO -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px;margin-bottom:14px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">Cadastrar novo usuário</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:12px">
        <div><label class="fl" style="display:block;margin-bottom:4px">Nome completo</label><input class="fi" id="uNome" placeholder="Ex: Diretor Silvio"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Login (usuário)</label><input class="fi" id="uLogin" placeholder="Ex: silvio"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Senha</label><input class="fi" type="password" id="uSenha" placeholder="Mínimo 4 caracteres"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Perfil</label>
          <select class="fs" id="uPerfil">
            <option value="dir">Diretoria (visualização)</option>
            <option value="ges">Gestor (administrador)</option>
          </select>
        </div>
      </div>
      <button class="btn btn-p" onclick="addUsuario()">
        <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        Cadastrar usuário
      </button>
    </div>

    <!-- LISTA -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">${users.length} usuário${users.length!==1?'s':''} cadastrado${users.length!==1?'s':''}</div>
      ${rows}
    </div>`;
},

eventos(el){
  const mesOpts=[''].concat(Object.keys(DB.meses).sort().reverse()).map(k=>
    `<option value="${k}" ${k===(window._eventosMes||'')?'selected':''}>${k?DB.meses[k].label:'Todos os meses'}</option>`).join('');
  const selMes=window._eventosMes||'';
  const tipos=getEventTypes(selMes||null);
  const totalGeral=tipos.reduce((s,t)=>s+t.total,0);
  const CORES=['#F5C518','#fb923c','#4ade80','#93c5fd','#a78bfa','#f87171','#34d399'];

  // SVG donut chart
  const R=80,CX=110,CY=100,STROKE=32;
  const circ=2*Math.PI*R;
  let offset=0;
  const arcs=tipos.map((t,i)=>{
    const pct=totalGeral>0?t.total/totalGeral:0;
    const dash=pct*circ;
    const arc=`<circle cx="${CX}" cy="${CY}" r="${R}" fill="none" stroke="${CORES[i%CORES.length]}" stroke-width="${STROKE}"
      stroke-dasharray="${dash.toFixed(1)} ${(circ-dash).toFixed(1)}"
      stroke-dashoffset="${(-offset*circ+circ/4).toFixed(1)}"
      style="transition:all .5s"/>`;
    offset+=pct;
    return arc;
  }).join('');

  const legend=tipos.map((t,i)=>`
    <div style="display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid var(--bg3)">
      <div style="width:12px;height:12px;border-radius:3px;background:${CORES[i%CORES.length]};flex-shrink:0"></div>
      <div style="flex:1">
        <div style="font-size:13px;font-weight:600;color:var(--w)">${t.nome}</div>
        <div style="font-size:11px;color:var(--w3);margin-top:1px">${t.count} pagamento${t.count!==1?'s':''}</div>
      </div>
      <div style="text-align:right">
        <div style="font-family:'IBM Plex Mono',monospace;font-weight:600;font-size:14px;color:var(--y)">${brl(t.total)}</div>
        <div style="font-size:11px;color:var(--w3);margin-top:1px">${totalGeral>0?Math.round(t.total/totalGeral*100):0}%</div>
      </div>
    </div>`).join('');

  // Tabela detalhada por tipo
  const allInd=selMes
    ?(DB.meses[selMes]?DB.meses[selMes].indenizacoes.filter(i=>!isFer(i)&&i.tipo):[])
    :Object.values(DB.meses).flatMap(m=>m.indenizacoes.filter(i=>!isFer(i)&&i.tipo));
  const tiposUniq=[...new Set(allInd.map(i=>i.tipo))].sort();

  const tipoSel=window._tipoSel||'';
  const tipoOpts=`<option value="">Todos os tipos</option>`+tiposUniq.map(t=>`<option value="${t}" ${t===tipoSel?'selected':''}>${t}</option>`).join('');
  const filtered=tipoSel?allInd.filter(i=>i.tipo===tipoSel):allInd;
  const tipoRows=filtered.slice(0,50).map(i=>{
    const mk=Object.keys(DB.meses).find(k=>DB.meses[k].indenizacoes.some(x=>x.id===i.id))||mesAtivo;
    return `<tr onclick="showDetail(${i.id},'${mk}')" style="cursor:pointer">
      <td style="padding:9px 12px;font-weight:500;color:var(--w);font-size:12px">${i.nome}</td>
      <td style="padding:9px 12px;font-size:11px;color:var(--w3)">${DB.meses[mk]?DB.meses[mk].label:mk}</td>
      <td style="padding:9px 12px"><span class="badge" style="background:${CORES[tiposUniq.indexOf(i.tipo)%CORES.length]}22;color:${CORES[tiposUniq.indexOf(i.tipo)%CORES.length]}">${i.tipo}</span></td>
      <td style="padding:9px 12px"><span class="badge ${statusColor(i.status)}">${i.status}</span></td>
      <td style="padding:9px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);font-size:12px">${brl(i.valorLiq)}</td>
    </tr>`;}).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Relatório</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Por Tipo de Evento</h1>
      </div>
      <select class="pbtn" style="cursor:pointer;font-size:13px" onchange="window._eventosMes=this.value;nav('eventos')">${mesOpts}</select>
    </div>

    ${tipos.length===0?`<div style="text-align:center;padding:40px;color:var(--w3);font-size:13px">Nenhuma indenização com tipo de evento cadastrado.<br><span style="font-size:11px">Edite as indenizações e preencha o campo Tipo de Evento.</span></div>`:`
    <div style="display:grid;grid-template-columns:260px 1fr;gap:16px;margin-bottom:16px" class="au1">
      <!-- DONUT -->
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;display:flex;flex-direction:column;align-items:center">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px;align-self:flex-start">Distribuição</div>
        <svg width="220" height="200" viewBox="0 0 220 200">
          ${arcs}
          <text x="${CX}" y="${CY-8}" text-anchor="middle" font-size="11" fill="var(--w3)" font-family="IBM Plex Sans,sans-serif">Total</text>
          <text x="${CX}" y="${CY+12}" text-anchor="middle" font-size="13" fill="#F5C518" font-family="IBM Plex Mono,monospace" font-weight="600">${brl(totalGeral).replace('R$ ','')}</text>
        </svg>
      </div>
      <!-- LEGEND -->
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">Por tipo</div>
        ${legend}
      </div>
    </div>

    <!-- TABELA -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;overflow:hidden">
      <div style="padding:14px 16px;border-bottom:1px solid var(--bg3);display:flex;align-items:center;gap:10px;flex-wrap:wrap">
        <span style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px">Filtrar por tipo:</span>
        <select class="fs" style="max-width:200px;padding:5px 10px;font-size:12px" onchange="window._tipoSel=this.value;nav('eventos')">${tipoOpts}</select>
        <span style="font-size:11px;color:var(--w3);margin-left:auto">${filtered.length} registro${filtered.length!==1?'s':''}</span>
      </div>
      <div style="overflow-x:auto">
        <table style="width:100%;border-collapse:collapse;font-size:12px">
          <thead><tr style="border-bottom:1px solid var(--bg3)">
            <th style="text-align:left;padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">Associado</th>
            <th style="text-align:left;padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">Mês</th>
            <th style="text-align:left;padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">Tipo</th>
            <th style="text-align:left;padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">Status</th>
            <th style="text-align:right;padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">Valor</th>
          </tr></thead>
          <tbody>${tipoRows||`<tr><td colspan="5" style="text-align:center;padding:20px;color:var(--w3)">Nenhum registro</td></tr>`}</tbody>
        </table>
      </div>
    </div>`}`;
},


consultores(el){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
  const isFerx=i=>i.status==='FERIADO';

  const todosCons=getConsultores();
  const selCons=window._consNome||'';

  // Build consultor select options
  const consOpts=`<option value="">— Selecione o consultor —</option>`+
    todosCons.map(c=>`<option value="${c}" ${c===selCons?'selected':''}>${c}</option>`).join('');

  // Gather all indenizacoes for selected consultor across all months
  let indFiltradas=[];
  let totalGeral=0, totalPago=0, totalPend=0, countPago=0, countPend=0;

  if(selCons){
    Object.entries(DB.meses).sort((a,b)=>b[0].localeCompare(a[0])).forEach(([mk,mes])=>{
      (mes.indenizacoes||[]).filter(i=>!isFerx(i)&&i.consultor===selCons).forEach(i=>{
        indFiltradas.push({...i,mesKey:mk,mesLabel:mes.label});
        totalGeral+=i.valorLiq||0;
        if(i.status==='PAGO'){totalPago+=i.valorLiq||0;countPago++;}
        else{totalPend+=i.valorLiq||0;countPend++;}
      });
    });
  }

  const rows=indFiltradas.map(i=>`
    <tr onclick="showDetail(${i.id},'${i.mesKey}')" style="cursor:pointer">
      <td class="nc">${i.nome}</td>
      <td class="mc">${fdt(i.data)}</td>
      <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${i.mesLabel}</td>
      <td>${i.parcela?`<span style="font-size:11px;color:var(--w3)">${i.parcela}</span>`:'-'}</td>
      <td><span class="badge ${statusColor(i.status)}">${i.status}</span></td>
      <td class="vc">${brl(i.valorLiq||0)}</td>
    </tr>`).join('');

  el.innerHTML=`
    <div class="tb au">
      <div><h1>Por Consultor</h1><p>Histórico completo de indenizações por consultor</p></div>
    </div>

    <div class="card au1" style="margin-bottom:16px">
      <div class="ch"><span class="ct">Selecionar Consultor</span></div>
      <select class="fs" style="width:100%;max-width:420px" onchange="window._consNome=this.value;nav('consultores')">
        ${consOpts}
      </select>
    </div>

    ${!selCons?`
    <div class="card" style="text-align:center;padding:40px;color:var(--w3)">
      <svg viewBox="0 0 24 24" width="40" height="40" stroke="currentColor" fill="none" stroke-width="1.5" style="margin:0 auto 12px;display:block;opacity:.35"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></svg>
      <div style="font-size:14px;font-weight:500;margin-bottom:6px">Selecione um consultor acima</div>
      <div style="font-size:12px">O histórico completo de indenizações será exibido aqui.</div>
    </div>`:indFiltradas.length===0?`
    <div class="card" style="text-align:center;padding:40px;color:var(--w3)">
      <div style="font-size:14px;font-weight:500;margin-bottom:6px">Nenhuma indenização encontrada</div>
      <div style="font-size:12px">Nenhuma indenização foi atribuída a <strong>${selCons}</strong>.</div>
    </div>`:`
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:10px;margin-bottom:16px" class="au2">
      <div class="kpi ky">
        <div class="kl">Total Geral</div>
        <div class="kv">${brl(totalGeral)}</div>
        <div class="ks">${indFiltradas.length} indenizações</div>
      </div>
      <div class="kpi">
        <div class="kl">Total Pago</div>
        <div class="kv" style="color:#4ade80">${brl(totalPago)}</div>
        <div class="ks" style="color:#4ade80">${countPago} pagamentos</div>
      </div>
      <div class="kpi">
        <div class="kl">Total Pendente</div>
        <div class="kv" style="color:#fb923c">${brl(totalPend)}</div>
        <div class="ks" style="color:#fb923c">${countPend} pendentes</div>
      </div>
    </div>
    <div class="card au3">
      <div class="ch"><span class="ct">${selCons}</span><span style="font-size:11px;color:var(--w3)">${indFiltradas.length} registros</span></div>
      <div class="tw">
        <table class="dt">
          <thead><tr>
            <th>Associado</th><th>Data</th><th>Mês</th><th>Parcela</th><th>Status</th>
            <th style="text-align:right">Valor Líquido</th>
          </tr></thead>
          <tbody>${rows}</tbody>
        </table>
      </div>
    </div>`}`;
},

notificacoes(el){
  const status=getNotifStatus();
  const ativo=localStorage.getItem(NOTIF_KEY)==='1'&&status==='granted';

  // Detect device/browser for better guidance
  const isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent);
  const isSafari=/^((?!chrome|android).)*safari/i.test(navigator.userAgent);
  const isAndroid=/Android/.test(navigator.userAgent);
  const isChrome=/Chrome/.test(navigator.userAgent)&&!/Edg/.test(navigator.userAgent);
  const isPWA=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone;

  const statusInfo={
    'unsupported':{
      cor:'var(--orange)',
      txt:isIOS?'Requer instalação como App':'Não suportado neste navegador',
      desc:isIOS
        ?'No iPhone/iPad, notificações push só funcionam quando o app está instalado na tela inicial. Veja as instruções abaixo.'
        :'Tente abrir no Google Chrome para usar notificações push.'
    },
    'default':{cor:'var(--orange)',txt:'Permissão necessária',desc:'Clique em "Ativar notificações" para receber alertas de pagamentos.'},
    'granted':{cor:'var(--green)',txt:ativo?'Ativado':'Desativado',desc:ativo?'Você receberá notificações de pagamentos automaticamente.':'Permissão concedida. Ative o toggle para receber notificações.'},
    'denied':{cor:'var(--red)',txt:'Bloqueado',desc:'Você bloqueou as notificações. Para reativar: Configurações do navegador → Notificações → Permitir para este site.'},
  };
  const info=statusInfo[status]||statusInfo['default'];

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Configurações</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px;line-height:1">Notificações Push</h1>
      </div>
    </div>

    <!-- STATUS CARD -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:16px;padding:22px;margin-bottom:14px">
      <div style="display:flex;align-items:center;gap:14px;margin-bottom:16px">
        <div style="width:44px;height:44px;border-radius:50%;background:${info.cor}20;display:flex;align-items:center;justify-content:center;flex-shrink:0">
          <svg width="20" height="20" viewBox="0 0 24 24" stroke="${info.cor}" fill="none" stroke-width="2"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
        </div>
        <div>
          <div style="font-size:15px;font-weight:600;color:var(--w)">Status: <span style="color:${info.cor}">${info.txt}</span></div>
          <div style="font-size:12px;color:var(--w3);margin-top:3px">${info.desc}</div>
        </div>
      </div>

      ${status==='unsupported'?'':status==='denied'?`
        <div style="background:rgba(220,38,38,.08);border:1px solid rgba(220,38,38,.2);border-radius:10px;padding:12px 14px;font-size:12px;color:#f87171">
          <strong style="display:block;margin-bottom:6px">Como desbloquear notificações:</strong>
          🤖 <strong>Android Chrome:</strong> Toque nos 3 pontinhos → Configurações do site → Notificações → Permitir<br>
          🍎 <strong>iPhone Safari:</strong> Ajustes → Apps → Safari → Configurações para sites → Notificações → Perguntar<br>
          🖥️ <strong>Chrome PC/Mac:</strong> Clique no 🔒 na URL → Notificações → Permitir
        </div>`:
        status==='unsupported'&&isIOS?`
        <div style="background:var(--y3);border:1px solid rgba(240,180,41,.2);border-radius:12px;padding:16px;font-size:12px;color:var(--w2);line-height:1.9">
          <strong style="color:var(--y);font-size:13px;display:block;margin-bottom:10px">📱 iPhone — Instale como App primeiro</strong>
          <div style="display:flex;flex-direction:column;gap:8px">
            <div style="display:flex;align-items:center;gap:10px"><div style="width:22px;height:22px;border-radius:50%;background:var(--y);color:#000;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px;flex-shrink:0">1</div><span>Abra no <strong>Safari</strong> (não no Chrome do iPhone)</span></div>
            <div style="display:flex;align-items:center;gap:10px"><div style="width:22px;height:22px;border-radius:50%;background:var(--y);color:#000;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px;flex-shrink:0">2</div><span>Toque em <strong>Compartilhar</strong> (□↑) na barra inferior</span></div>
            <div style="display:flex;align-items:center;gap:10px"><div style="width:22px;height:22px;border-radius:50%;background:var(--y);color:#000;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px;flex-shrink:0">3</div><span>Toque em <strong>"Adicionar à Tela de Início"</strong></span></div>
            <div style="display:flex;align-items:center;gap:10px"><div style="width:22px;height:22px;border-radius:50%;background:var(--y);color:#000;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px;flex-shrink:0">4</div><span>Abra o app pelo ícone — notificações estarão disponíveis</span></div>
          </div>
          <div style="margin-top:10px;background:var(--glass);border-radius:6px;padding:8px;font-size:11px;color:var(--w3)">Requer iOS 16.4 ou superior.</div>
        </div>`:
        status==='unsupported'?`
        <div style="background:var(--blue2);border:1px solid rgba(59,130,246,.2);border-radius:10px;padding:12px 14px;font-size:12px;color:#93c5fd;line-height:1.7">
          Para notificações, abra este app no <strong>Google Chrome</strong> — ele tem suporte completo em Android e computador.
        </div>`:!ativo?`
        <button class="btn btn-p" onclick="ativarNotificacoes().then(()=>nav('notificacoes'))">
          <svg viewBox="0 0 24 24"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
          Ativar notificações
        </button>`:`
        <div style="display:flex;gap:10px;flex-wrap:wrap">
          <button class="btn btn-yg" onclick="enviarNotificacaoLocal('AR4 — Teste','Notificações funcionando perfeitamente! ✓');toast('Notificação de teste enviada!','suc')">
            <svg viewBox="0 0 24 24"><polyline points="22 2 15 22 11 13 2 9 22 2"/></svg>
            Enviar notificação de teste
          </button>
          <button class="btn btn-d" onclick="desativarNotificacoes();nav('notificacoes')">Desativar</button>
        </div>`}
    </div>

    <!-- COMO FUNCIONA -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:20px;margin-bottom:14px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:14px">Como funciona</div>
      ${[
        ['📅','Novo mês importado','Quando o gestor importar um novo cronograma, todos com notificações ativas recebem alerta com o total e quantidade de pagamentos.'],
        ['📱','Funciona em segundo plano','A notificação aparece mesmo com o app minimizado ou a tela bloqueada — igual a qualquer outro app.'],
        ['⚙️','Configuração por dispositivo','Cada diretor precisa ativar as notificações no seu próprio celular/computador. A permissão é por dispositivo.'],
        ['🔒','Sem servidor necessário','As notificações são locais — disparadas diretamente pelo service worker instalado no dispositivo, sem custo adicional.'],
      ].map(([ic,t,d])=>`
        <div style="display:flex;gap:12px;padding:12px 0;border-bottom:1px solid var(--bg3)">
          <div style="font-size:22px;flex-shrink:0;width:32px;text-align:center">${ic}</div>
          <div><div style="font-size:13px;font-weight:600;color:var(--w);margin-bottom:3px">${t}</div><div style="font-size:12px;color:var(--w3);line-height:1.6">${d}</div></div>
        </div>`).join('')}
    </div>

    <!-- INSTALAR COMO APP -->
    <div class="au3" style="background:var(--y3);border:1px solid rgba(245,197,24,.2);border-radius:14px;padding:20px">
      <div style="font-size:11px;color:var(--y);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">💡 Dica importante</div>
      <div style="font-size:13px;color:var(--w2);line-height:1.7">
        Para notificações funcionarem perfeitamente no celular, <strong style="color:var(--w)">instale o app na tela inicial</strong>:<br>
        <strong>Android:</strong> Menu do Chrome (⋮) → "Adicionar à tela inicial"<br>
        <strong>iPhone:</strong> Botão compartilhar (□↑) → "Adicionar à Tela de Início"<br><br>
        Com o app instalado, as notificações aparecem mesmo com o celular bloqueado.
      </div>
    </div>`;
},

aprovacoes(el){
  const list=getAprovacoes();
  const pendentes=list.filter(a=>a.status==='pendente');
  const historico=list.filter(a=>a.status!=='pendente');
  const isGes=role==='ges';

  function statusBadge(s){
    if(s==='aprovado')return '<span class="badge" style="background:rgba(22,163,74,.18);color:#4ade80;font-size:11px;padding:4px 10px">✓ Aprovado</span>';
    if(s==='reprovado')return '<span class="badge" style="background:rgba(220,38,38,.15);color:#f87171;font-size:11px;padding:4px 10px">✗ Reprovado</span>';
    return '<span class="badge bpend" style="font-size:11px;padding:4px 10px">⏳ Aguardando</span>';
  }

  function cardAprov(aprov){
    const total=brl(aprov.meta.totalBruto||0);
    const dt=new Date(aprov.criadoEm).toLocaleDateString('pt-BR');
    const isPend=aprov.status==='pendente';
    const ind=aprov.indenizacoes||[];
    const indFilt=ind.filter(i=>i.status!=='FERIADO');
    const pago=indFilt.filter(i=>i.status==='PAGO').length;
    const pend2=indFilt.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length;

    // Build mini calendar
    const totalDias=daysInMonth(aprov.mAno,aprov.mNum);
    const primeiroDia=firstDayOfMonth(aprov.mAno,aprov.mNum);
    const payMap={};
    ind.forEach(i=>{
      if(!i.data)return;
      const d=parseInt(i.data.split('-')[2]);
      if(!payMap[d])payMap[d]=[];
      payMap[d].push(i);
    });
    const days=['D','S','T','Q','Q','S','S'];
    let calCells=days.map(d=>`<div style="text-align:center;font-size:11px;font-weight:700;color:var(--w4);padding:7px 2px;letter-spacing:.3px">${d}</div>`).join('');
    for(let i=0;i<primeiroDia;i++) calCells+=`<div style="min-height:72px;background:var(--bg3);border-radius:8px;opacity:.15"></div>`;
    for(let d=1;d<=totalDias;d++){
      const ents=payMap[d]||[];
      const isH=ents.some(e=>e.status==='FERIADO');
      const hasPay=ents.filter(e=>e.status!=='FERIADO').length>0;
      const dayTotal=ents.filter(e=>e.status!=='FERIADO').reduce((s,e)=>s+e.valorLiq,0);
      let bg='var(--bg3)';let border='transparent';let numColor='var(--w4)';
      if(isH){bg='rgba(124,58,237,.1)';border='rgba(124,58,237,.3)';}
      else if(hasPay){bg='rgba(245,197,24,.06)';border='rgba(245,197,24,.4)';numColor='var(--y)';}
      const allNames=ents.filter(e=>!isH).map(e=>e.nome.split(' ')[0]);
      calCells+=`<div style="background:${bg};border:1px solid ${border};border-radius:8px;padding:7px;min-height:72px;overflow:hidden">
        <div style="font-size:11px;font-weight:700;color:${numColor};margin-bottom:4px">${d}</div>
        ${isH?'<div style="font-size:10px;color:#a78bfa;font-weight:600">Feriado</div>':''}
        ${allNames.slice(0,2).map(n=>`<div style="font-size:10px;color:var(--y);font-weight:600;line-height:1.4;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${n}</div>`).join('')}
        ${allNames.length>2?`<div style="font-size:9px;color:var(--w4)">+${allNames.length-2}</div>`:''}
        ${dayTotal>0?`<div style="font-size:10px;color:var(--w2);font-weight:600;margin-top:3px;font-family:'IBM Plex Mono',monospace">${brl(dayTotal).replace('R$ ','')}</div>`:''}
      </div>`;
    }

    // Table rows
    const rows=indFilt.map(i=>`
      <tr>
        <td style="padding:9px 12px;font-size:13px;font-weight:500;color:var(--w)">${i.nome}</td>
        <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${fdt(i.data)}</td>
        <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${i.parcela||'—'}</td>
        <td style="padding:9px 12px"><span class="badge ${statusColor(i.status)}" style="font-size:11px">${i.status}</span></td>
        <td style="padding:9px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);font-size:13px">${brl(i.valorLiq)}</td>
      </tr>`).join('');

    return `
    <div style="background:var(--bg2);border:1.5px solid ${isPend?'rgba(245,197,24,.3)':aprov.status==='aprovado'?'rgba(22,163,74,.3)':'rgba(220,38,38,.3)'};border-radius:16px;overflow:hidden;margin-bottom:16px">

      <!-- HEADER -->
      <div style="background:${isPend?'linear-gradient(135deg,rgba(245,197,24,.08),transparent)':aprov.status==='aprovado'?'linear-gradient(135deg,rgba(22,163,74,.06),transparent)':'linear-gradient(135deg,rgba(220,38,38,.06),transparent)'};padding:18px 20px;border-bottom:1px solid var(--bg3)">
        <div style="display:flex;align-items:flex-start;justify-content:space-between;flex-wrap:wrap;gap:10px">
          <div>
            <div style="font-size:20px;font-weight:700;color:var(--w);margin-bottom:4px">${aprov.mesLabel}</div>
            <div style="font-size:11px;color:var(--w3)">Enviado por <strong style="color:var(--w2)">${aprov.criadoPor}</strong> em ${dt}</div>
          </div>
          <div style="text-align:right">
            <div style="font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:22px;color:var(--y)">${total}</div>
            <div style="margin-top:5px">${statusBadge(aprov.status)}</div>
          </div>
        </div>
        <!-- KPIs -->
        <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:14px">
          ${[['Pagamentos',aprov.meta.pagamentos,'var(--w)'],['Pagos',pago,'#4ade80'],['Pendentes',pend2,'#fb923c'],['Beneficiários',aprov.meta.beneficiarios||new Set(indFilt.map(i=>i.nome)).size,'var(--w)']].map(([l,v,c])=>`
          <div style="background:rgba(0,0,0,.2);border-radius:8px;padding:8px;text-align:center">
            <div style="font-size:18px;font-weight:700;color:${c};font-family:'IBM Plex Mono',monospace">${v}</div>
            <div style="font-size:10px;color:var(--w3);margin-top:2px">${l}</div>
          </div>`).join('')}
        </div>
      </div>

      <!-- CALENDARIO + TABELA -->
      <div style="padding:18px 22px;border-bottom:1px solid var(--bg3)">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">Calendário do mês</div>
        <div style="display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:4px;min-width:0;width:100%">${calCells}</div>
        <div style="display:flex;gap:16px;margin-top:10px;flex-wrap:wrap">
          <div style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--w3)"><div style="width:10px;height:10px;border-radius:3px;background:rgba(245,197,24,.06);border:1px solid rgba(245,197,24,.35)"></div>Com pagamento</div>
          <div style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--w3)"><div style="width:10px;height:10px;border-radius:3px;background:rgba(124,58,237,.1);border:1px solid rgba(124,58,237,.3)"></div>Feriado</div>
        </div>
      </div>

      <!-- TABELA COMPLETA -->
      <div style="padding:16px 20px;border-bottom:1px solid var(--bg3)">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">Todas as indenizações</div>
        <div style="overflow-x:auto">
          <table style="width:100%;border-collapse:collapse;font-size:12px">
            <thead><tr style="border-bottom:1px solid var(--bg3)">
              ${['Associado','Data','Parcela','Status','Valor Líquido'].map((h,i)=>`<th style="text-align:${i===4?'right':'left'};padding:6px 10px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase;letter-spacing:.3px">${h}</th>`).join('')}
            </tr></thead>
            <tbody>${rows}</tbody>
            <tfoot><tr style="border-top:1px solid var(--bg3);background:var(--bg3)">
              <td colspan="4" style="padding:7px 10px;font-size:12px;font-weight:600;color:var(--w)">Total</td>
              <td style="padding:7px 10px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:14px;color:var(--y)">${total}</td>
            </tr></tfoot>
          </table>
        </div>
      </div>

      <!-- OBS E AÇÕES -->
      <div style="padding:16px 20px">
        ${aprov.obs?`<div style="background:var(--bg3);border-radius:8px;padding:10px 12px;margin-bottom:12px;font-size:12px;color:var(--w2)"><span style="font-size:10px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.4px;display:block;margin-bottom:4px">Observação do Gestor</span>${aprov.obs}</div>`:''}
        ${aprov.obsDir?`<div style="background:${aprov.status==='aprovado'?'rgba(22,163,74,.08)':'rgba(220,38,38,.08)'};border:1px solid ${aprov.status==='aprovado'?'rgba(22,163,74,.2)':'rgba(220,38,38,.2)'};border-radius:8px;padding:10px 12px;margin-bottom:12px;font-size:12px"><span style="font-size:10px;font-weight:600;text-transform:uppercase;letter-spacing:.4px;display:block;margin-bottom:4px;color:${aprov.status==='aprovado'?'#4ade80':'#f87171'}">Resposta da Diretoria</span><span style="color:var(--w2)">${aprov.obsDir}</span></div>`:''}
        ${isPend&&!isGes?`
          <div style="font-size:11px;color:var(--y);font-weight:600;text-transform:uppercase;letter-spacing:.4px;margin-bottom:8px">Sua decisão</div>
          <textarea class="fta" id="obs_${aprov.id}" placeholder="Observação (opcional) — está bom, pode prosseguir / o valor de X precisa ser revisado..." style="margin-bottom:10px;font-size:12px;min-height:70px"></textarea>
          <div style="display:flex;gap:8px;flex-wrap:wrap">
            <button class="btn" style="background:rgba(22,163,74,.15);color:#4ade80;border:1px solid rgba(22,163,74,.3);flex:1;justify-content:center;font-weight:600" onclick="responderAprovacao(${aprov.id},'aprovado')">
              <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>Aprovar cronograma
            </button>
            <button class="btn" style="background:rgba(220,38,38,.12);color:#f87171;border:1px solid rgba(220,38,38,.25);flex:1;justify-content:center;font-weight:600" onclick="responderAprovacao(${aprov.id},'reprovado')">
              <svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>Reprovar
            </button>
          </div>`:
        isPend&&isGes?`
          <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px">
            <div style="font-size:12px;color:var(--w3);font-style:italic">⏳ Aguardando análise da diretoria...</div>
            <div style="display:flex;gap:7px">
              <button class="btn btn-d btn-sm" onclick="cancelarAprovacao(${aprov.id})">Cancelar envio</button>
            </div>
          </div>`:
        aprov.status==='aprovado'?`
          <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center">
            ${!DB.meses[aprov.mesKey]?`<button class="btn btn-p btn-sm" onclick="aplicarAprovacao(${aprov.id});buildNav();nav('dashboard');toast('${aprov.mesLabel} adicionado!','suc')">
              <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/></svg>
              ${isGes?'Aplicar ao sistema':'Aplicar ao sistema'}
            </button>`:`<span style="font-size:12px;color:#4ade80;font-weight:600">✓ Já aplicado ao sistema</span>`}
          </div>`:''}
      </div>
    </div>`;
  }

  // Contador de pendentes para badge no nav
  const badge=pendentes.length?`<span style="background:var(--orange);color:#fff;font-size:10px;font-weight:700;padding:2px 7px;border-radius:10px;margin-left:8px">${pendentes.length}</span>`:'';

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Fluxo de trabalho</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Aprovações ${badge}</h1>
      </div>
    </div>

    ${isGes?`
    <!-- GESTOR: config mensagem da notificação -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;margin-bottom:16px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:8px">Mensagem da notificação push (próximo envio)</div>
      <div style="display:flex;gap:8px;margin-bottom:8px">
        <input class="fi" id="customNotifMsg" style="flex:1"
          placeholder="Ex: Cronograma de Julho 2026 disponível para análise"
          value="${window._customNotifMsg||''}"
          oninput="window._customNotifMsg=this.value"/>
        <button class="btn btn-yg btn-sm" onclick="window._customNotifMsg=document.getElementById('customNotifMsg').value;toast('Mensagem salva!','suc')">Salvar</button>
      </div>
      <div style="font-size:11px;color:var(--w3);margin-bottom:12px">Esta mensagem aparecerá na notificação push quando você enviar o próximo cronograma.</div>
      <div style="border-top:1px solid var(--bg3);padding-top:12px">
        <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:8px">Teste do fluxo</div>
        <button class="btn btn-g btn-sm" onclick="criarAprovacaoTeste()">
          <svg viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="2"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
          Criar aprovação de teste
        </button>
        <div style="font-size:11px;color:var(--w4);margin-top:5px">Cria um cronograma fictício para testar o fluxo de aprovação.</div>
      </div>
    </div>`:''}

    ${pendentes.length?`
    <div class="au2" style="margin-bottom:20px">
      <div style="font-size:11px;color:var(--y);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">${pendentes.length} aguardando análise</div>
      ${pendentes.map(cardAprov).join('')}
    </div>`:`
    <div class="au2" style="text-align:center;padding:32px;background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;margin-bottom:16px;color:var(--w3)">
      <svg width="40" height="40" viewBox="0 0 24 24" stroke="currentColor" fill="none" stroke-width="1.5" style="margin:0 auto 12px;display:block;opacity:.3"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
      <div style="font-size:14px;font-weight:500">Nenhum cronograma aguardando aprovação</div>
      <div style="font-size:12px;margin-top:6px">Quando o gestor enviar um cronograma de mês futuro, aparecerá aqui para você aprovar.</div>
    </div>`}

    ${historico.length?`
    <div class="au3">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:10px">Histórico</div>
      ${historico.slice(0,5).map(cardAprov).join('')}
    </div>`:''}`;
},

associado(el){
  // Histórico completo por associado
  const q=(window._assocQ||'').trim().toUpperCase();
  const allInd=[];
  Object.entries(DB.meses).sort((a,b)=>b[0].localeCompare(a[0])).forEach(([mk,mes])=>{
    mes.indenizacoes.filter(i=>!isFer(i)).forEach(i=>allInd.push({...i,mesKey:mk,mesLabel:mes.label}));
  });
  // Build unique associado list
  const assocMap={};
  allInd.forEach(i=>{
    if(!assocMap[i.nome])assocMap[i.nome]={nome:i.nome,total:0,count:0,pagos:0,meses:new Set(),inds:[]};
    assocMap[i.nome].total+=i.valorLiq;
    assocMap[i.nome].count++;
    if(i.status==='PAGO')assocMap[i.nome].pagos++;
    assocMap[i.nome].meses.add(i.mesKey);
    assocMap[i.nome].inds.push(i);
  });
  const sorted=Object.values(assocMap).sort((a,b)=>b.total-a.total);
  const filtered=q?sorted.filter(a=>a.nome.includes(q)):sorted;
  const selNome=window._assocSel||'';
  const selData=selNome?assocMap[selNome]:null;

  const listRows=filtered.slice(0,30).map(a=>`
    <div onclick="window._assocSel='${a.nome.replace(/'/g,"\'")}';nav('associado')"
      style="display:flex;align-items:center;justify-content:space-between;padding:12px 14px;background:${selNome===a.nome?'var(--y3)':'var(--bg3)'};border:1px solid ${selNome===a.nome?'rgba(245,197,24,.3)':'transparent'};border-radius:10px;margin-bottom:5px;cursor:pointer;transition:all .15s"
      onmouseover="this.style.background='var(--bg4)'" onmouseout="this.style.background='${selNome===a.nome?'var(--y3)':'var(--bg3)'}'">
      <div>
        <div style="font-size:13px;font-weight:600;color:var(--w)">${a.nome}</div>
        <div style="font-size:11px;color:var(--w3);margin-top:2px">${a.count} pagamento${a.count!==1?'s':''} · ${a.meses.size} mês${a.meses.size!==1?'es':''}</div>
      </div>
      <div style="text-align:right">
        <div style="font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:14px;color:var(--y)">${brl(a.total)}</div>
        ${a.pagos>0?`<div style="font-size:10px;color:#4ade80;margin-top:2px">${a.pagos} pago${a.pagos!==1?'s':''}</div>`:''}
      </div>
    </div>`).join('');

  let detailHtml='';
  if(selData){
    const selRows=selData.inds.map(i=>`
      <tr onclick="showDetail(${i.id},'${i.mesKey}')" style="cursor:pointer">
        <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${i.mesLabel}</td>
        <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${fdt(i.data)}</td>
        <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${i.parcela||'—'}</td>
        <td style="padding:9px 12px"><span class="badge ${statusColor(i.status)}" style="font-size:10px">${i.status}</span></td>
        <td style="padding:9px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:600;color:var(--y);font-size:12px">${brl(i.valorLiq)}</td>
      </tr>`).join('');
    detailHtml=`
      <div style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;overflow:hidden;margin-bottom:14px">
        <div style="background:var(--y3);padding:18px 20px;border-bottom:1px solid rgba(245,197,24,.15)">
          <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px">
            <div>
              <div style="font-size:11px;color:var(--y);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:4px">Associado</div>
              <div style="font-size:20px;font-weight:700;color:var(--w)">${selData.nome}</div>
            </div>
            <button class="btn btn-g btn-sm" onclick="window._assocSel='';nav('associado')">← Voltar</button>
          </div>
          <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:14px">
            ${[['Total recebido',brl(selData.total),'var(--y)','IBM Plex Mono,monospace'],['Pagamentos',selData.count,'var(--w)',''],['Meses ativos',selData.meses.size,'var(--w)',''],['Pagos',selData.pagos,'#4ade80','']].map(([l,v,c,f])=>`
            <div style="background:rgba(0,0,0,.2);border-radius:8px;padding:10px;text-align:center">
              <div style="font-size:${l==='Total recebido'?'15':'20'}px;font-weight:700;color:${c};font-family:${f}">${v}</div>
              <div style="font-size:10px;color:var(--w3);margin-top:3px">${l}</div>
            </div>`).join('')}
          </div>
        </div>
        <div style="overflow-x:auto">
          <table style="width:100%;border-collapse:collapse">
            <thead><tr style="border-bottom:1px solid var(--bg3)">
              ${['Mês','Data','Parcela','Status','Valor'].map((h,i)=>`<th style="text-align:${i===4?'right':'left'};padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">${h}</th>`).join('')}
            </tr></thead>
            <tbody>${selRows}</tbody>
            <tfoot><tr style="border-top:1px solid var(--bg3);background:var(--bg3)">
              <td colspan="4" style="padding:8px 12px;font-size:12px;font-weight:600;color:var(--w)">Total</td>
              <td style="padding:8px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:700;color:var(--y)">${brl(selData.total)}</td>
            </tr></tfoot>
          </table>
        </div>
      </div>`;
  }

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:20px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Histórico</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Por Associado</h1>
      </div>
    </div>
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:14px;margin-bottom:14px">
      <input class="fi" id="assocSearch" placeholder="Buscar associado pelo nome..." value="${window._assocQ||''}"
        oninput="window._assocQ=this.value.toUpperCase();window._assocSel='';nav('associado')"
        style="font-size:14px;padding:11px 14px"/>
    </div>
    ${detailHtml}
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:16px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">
        ${q?`Resultados para "${q}" — ${filtered.length} associado${filtered.length!==1?'s':''}`:`Todos os associados — ${sorted.length}`}
      </div>
      ${listRows||'<p style="color:var(--w3);text-align:center;padding:20px;font-size:13px">Nenhum associado encontrado</p>'}
    </div>`;
  setTimeout(()=>{const el2=document.getElementById('assocSearch');if(el2&&!selNome)el2.focus();},100);
},

log(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const logs=DB.log||[];
  const iconFor=p=>p==='ges'?'🔧':'👁️';
  const rows=logs.length?logs.map(l=>`
    <div style="display:flex;align-items:flex-start;gap:12px;padding:10px 0;border-bottom:1px solid var(--bg3)">
      <div style="width:32px;height:32px;border-radius:50%;background:${l.perfil==='ges'?'rgba(234,88,12,.15)':'var(--y3)'};display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0">${iconFor(l.perfil)}</div>
      <div style="flex:1;min-width:0">
        <div style="font-size:13px;font-weight:500;color:var(--w);line-height:1.4">${l.acao}</div>
        <div style="font-size:11px;color:var(--w3);margin-top:3px"><strong style="color:var(--w2)">${l.usuario}</strong> · ${fmtTs(l.ts)}</div>
      </div>
    </div>`).join(''):`<div style="text-align:center;padding:32px;color:var(--w3);font-size:13px">Nenhuma atividade registrada ainda.</div>`;
  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Auditoria</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Log de Atividades</h1>
      </div>
      ${logs.length?`<button class="btn btn-d btn-sm" onclick="if(confirm('Limpar todo o log?')){DB.log=[];saveDB();nav('log')}">Limpar log</button>`:''}
    </div>
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">${logs.length} registro${logs.length!==1?'s':''}</div>
      ${rows}
    </div>`;
},

relatorio(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const anos=[...new Set(HIST.map(h=>parseInt(h.key.split('-')[0])))].sort((a,b)=>b-a);
  const anoSel=window._relAno||(anos[0]||new Date().getFullYear());
  const histAno=HIST.filter(h=>parseInt(h.key.split('-')[0])===anoSel).sort((a,b)=>a.key.localeCompare(b.key));
  const totalAno=histAno.reduce((s,h)=>s+h.valor,0);
  const totalPag=histAno.reduce((s,h)=>s+h.pagamentos,0);
  const maxV=Math.max(...histAno.map(h=>h.valor),1);

  // SVG line chart
  const W=500,H=140,pad=30;
  const pts=histAno.map((h,i)=>{
    const x=pad+(i/(Math.max(histAno.length-1,1)))*(W-2*pad);
    const y=H-pad-(h.valor/maxV)*(H-2*pad);
    return {x,y,h};
  });
  const pathD=pts.map((p,i)=>`${i===0?'M':'L'}${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ');
  const areaD=pts.length?`${pathD} L${pts[pts.length-1].x},${H-pad} L${pts[0].x},${H-pad} Z`:'';
  const dots=pts.map(p=>`<circle cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="5" fill="#F5C518" stroke="var(--bg2)" stroke-width="2"><title>${p.h.label}: ${brl(p.h.valor)}</title></circle>`).join('');
  const xlabels=pts.map(p=>`<text x="${p.x.toFixed(1)}" y="${H-8}" text-anchor="middle" font-size="9" fill="#555" font-family="IBM Plex Sans,sans-serif">${p.h.label.slice(0,3)}</text>`).join('');
  const svgChart=`<svg width="100%" viewBox="0 0 ${W} ${H}" xmlns="http://www.w3.org/2000/svg">
    <defs><linearGradient id="ag" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#F5C518" stop-opacity=".2"/><stop offset="100%" stop-color="#F5C518" stop-opacity="0"/></linearGradient></defs>
    ${areaD?`<path d="${areaD}" fill="url(#ag)"/>`:''}
    ${pts.length>1?`<path d="${pathD}" fill="none" stroke="#F5C518" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>`:''}
    ${dots}${xlabels}
  </svg>`;

  // Tabela mensal
  const meses=MESES_PT;
  const tblRows=histAno.map(h=>`
    <tr>
      <td style="padding:9px 12px;font-size:13px;font-weight:600;color:var(--w)">${DB.meses[h.key]?DB.meses[h.key].label:h.label}</td>
      <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${h.pagamentos}</td>
      <td style="padding:9px 12px;font-size:12px;color:var(--w3)">${brl(h.valor/h.pagamentos)}</td>
      <td style="padding:9px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:13px;color:var(--y)">${brl(h.valor)}</td>
    </tr>`).join('');

  // Consultores do ano
  const consAno={};
  histAno.forEach(h=>{
    const mes=DB.meses[h.key];
    if(!mes)return;
    mes.indenizacoes.filter(i=>!isFer(i)&&i.consultor).forEach(i=>{
      if(!consAno[i.consultor])consAno[i.consultor]={nome:i.consultor,total:0,count:0};
      consAno[i.consultor].total+=i.valorLiq;consAno[i.consultor].count++;
    });
  });
  const topCons=Object.values(consAno).sort((a,b)=>b.total-a.total).slice(0,5);

  const anoOpts=anos.map(a=>`<option value="${a}" ${a===anoSel?'selected':''}>${a}</option>`).join('');

  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Relatório</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Relatório Anual</h1>
      </div>
      <select class="pbtn" style="cursor:pointer;font-size:14px;font-weight:600" onchange="window._relAno=parseInt(this.value);nav('relatorio')">${anoOpts}</select>
    </div>

    <!-- HERO -->
    <div class="au1" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:16px;padding:24px;margin-bottom:16px;position:relative;overflow:hidden">
      <div style="position:absolute;top:-30px;right:-30px;width:200px;height:200px;background:radial-gradient(circle,rgba(245,197,24,.08),transparent 70%)"></div>
      <div style="font-size:11px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:8px">Total indenizado em ${anoSel}</div>
      <div style="font-family:'IBM Plex Mono',monospace;font-size:38px;font-weight:700;color:var(--y);letter-spacing:-1px;margin-bottom:14px">${brl(totalAno)}</div>
      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:10px">
        ${[['Pagamentos',totalPag],['Meses registrados',histAno.length],['Ticket médio',brl(totalPag?totalAno/totalPag:0)]].map(([l,v])=>`
        <div style="background:var(--bg3);border-radius:10px;padding:12px;text-align:center">
          <div style="font-size:16px;font-weight:700;color:var(--w);font-family:'IBM Plex Mono',monospace">${v}</div>
          <div style="font-size:10px;color:var(--w3);margin-top:3px">${l}</div>
        </div>`).join('')}
      </div>
    </div>

    <!-- GRÁFICO -->
    <div class="au2" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px;margin-bottom:14px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">Evolução mensal — ${anoSel}</div>
      ${histAno.length?svgChart:'<p style="color:var(--w4);text-align:center;padding:20px;font-size:13px">Sem dados para este ano</p>'}
    </div>

    <!-- TABELA MENSAL -->
    <div class="au3" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;overflow:hidden;margin-bottom:14px">
      <div style="padding:14px 16px;border-bottom:1px solid var(--bg3)"><span style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px">Detalhe por mês</span></div>
      <div style="overflow-x:auto">
        <table style="width:100%;border-collapse:collapse">
          <thead><tr style="border-bottom:1px solid var(--bg3)">
            ${['Mês','Pagamentos','Ticket Médio','Total'].map((h,i)=>`<th style="text-align:${i===3?'right':'left'};padding:8px 12px;font-size:10px;font-weight:600;color:var(--w4);text-transform:uppercase">${h}</th>`).join('')}
          </tr></thead>
          <tbody>${tblRows||'<tr><td colspan="4" style="text-align:center;padding:20px;color:var(--w3)">Sem dados</td></tr>'}</tbody>
          ${histAno.length?`<tfoot><tr style="border-top:1px solid var(--bg3);background:var(--bg3)">
            <td style="padding:9px 12px;font-size:12px;font-weight:700;color:var(--w)">Total ${anoSel}</td>
            <td style="padding:9px 12px;font-size:12px;font-weight:700;color:var(--w)">${totalPag}</td>
            <td style="padding:9px 12px"></td>
            <td style="padding:9px 12px;text-align:right;font-family:'IBM Plex Mono',monospace;font-weight:700;font-size:14px;color:var(--y)">${brl(totalAno)}</td>
          </tr></tfoot>`:''}
        </table>
      </div>
    </div>

    <!-- TOP CONSULTORES -->
    ${topCons.length?`
    <div class="au4" style="background:var(--bg2);border:1px solid var(--bg3);border-radius:14px;padding:18px">
      <div style="font-size:11px;color:var(--w3);font-weight:600;text-transform:uppercase;letter-spacing:.5px;margin-bottom:12px">Top consultores — ${anoSel}</div>
      ${topCons.map((c,i)=>`
        <div style="display:flex;align-items:center;gap:10px;padding:9px 0;border-bottom:1px solid var(--bg3)">
          <div style="width:24px;height:24px;border-radius:50%;background:${i===0?'var(--y)':i===1?'rgba(192,192,192,.2)':'var(--bg4)'};display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:${i===0?'var(--bg)':'var(--w3)'};flex-shrink:0">${i+1}</div>
          <div style="flex:1;font-size:13px;font-weight:500;color:var(--w)">${c.nome}</div>
          <div style="font-size:11px;color:var(--w3)">${c.count} pag.</div>
          <div style="font-family:'IBM Plex Mono',monospace;font-weight:700;color:var(--y);font-size:13px">${brl(c.total)}</div>
        </div>`).join('')}
    </div>`:''}`;
},

agendamento(el){
  if(role!=='ges'){el.innerHTML='<p style="color:var(--w3);padding:20px">Acesso restrito.</p>';return;}
  const cfg=getAgendConfig();
  el.innerHTML=`
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:22px" class="au">
      <div>
        <div style="font-size:12px;color:var(--w3);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px">Automação</div>
        <h1 style="font-size:26px;font-weight:700;color:var(--w);letter-spacing:-.5px">Notificações Agendadas</h1>
      </div>
    </div>

    <!-- STATUS SW -->
    <div class="au1" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:var(--r2);padding:18px;margin-bottom:14px">
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:14px">
        <div id="swStatus" style="width:10px;height:10px;border-radius:50%;background:${Notification.permission==='granted'?'var(--green)':'var(--red)'}"></div>
        <div style="font-size:13px;font-weight:600;color:var(--w)">Notificações ${Notification.permission==='granted'?'ativas':'inativas'}</div>
        ${Notification.permission!=='granted'?`<button class="btn btn-p btn-sm" onclick="ativarNotificacoes().then(()=>nav('agendamento'))">Ativar agora</button>`:''}
      </div>
      <div style="font-size:11px;color:var(--w3);line-height:1.7">
        Para que as notificações agendadas funcionem, o app precisa estar <strong style="color:var(--w2)">instalado na tela inicial</strong> e com permissão de notificação concedida.<br>
        <strong style="color:var(--w2)">Android:</strong> Menu Chrome → Adicionar à tela inicial &nbsp;|&nbsp; <strong style="color:var(--w2)">iPhone:</strong> Compartilhar → Adicionar à Tela de Início
      </div>
    </div>

    <!-- NOTIF DIÁRIA -->
    <div class="au2" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:var(--r2);padding:20px;margin-bottom:14px">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
        <div style="width:40px;height:40px;background:var(--y3);border-radius:var(--r);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0">📅</div>
        <div>
          <div style="font-size:15px;font-weight:600;color:var(--w)">Lembrete diário de pagamentos</div>
          <div style="font-size:12px;color:var(--w3);margin-top:2px">Notifica todos os dias que tiver indenização agendada</div>
        </div>
        <div style="margin-left:auto">
          <label style="position:relative;display:inline-block;width:44px;height:24px;cursor:pointer">
            <input type="checkbox" id="toggleDiario" ${cfg.diarioAtivo?'checked':''} onchange="salvarAgendConfig()" style="opacity:0;width:0;height:0"/>
            <span id="sliderDiario" style="position:absolute;inset:0;background:${cfg.diarioAtivo?'var(--y)':'var(--bg5)'};border-radius:12px;transition:all .25s">
              <span style="position:absolute;top:3px;left:${cfg.diarioAtivo?'23':'3'}px;width:18px;height:18px;background:#fff;border-radius:50%;transition:all .25s"></span>
            </span>
          </label>
        </div>
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Horário do lembrete</label>
          <input class="fi" type="time" id="horaDiario" value="${cfg.horaDiario||'08:00'}" onchange="salvarAgendConfig()"/>
        </div>
        <div>
          <label class="fl" style="display:block;margin-bottom:5px">Mês de referência</label>
          <select class="fs" id="mesDiario" onchange="salvarAgendConfig()">
            ${Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===(cfg.mesDiario||mesAtivo)?'selected':''}>${DB.meses[k].label}</option>`).join('')}
          </select>
        </div>
      </div>
      <div style="margin-top:12px;padding:10px;background:var(--glass);border:1px solid var(--glass-border);border-radius:var(--r);font-size:11px;color:var(--w3)">
        <strong style="color:var(--w2)">Como funciona:</strong> Toda vez que você abrir o app, ele verifica se hoje tem indenizações agendadas no mês selecionado. Se tiver, envia automaticamente uma notificação para todos os dispositivos com o app instalado.
      </div>
      <button class="btn btn-yg btn-sm" style="margin-top:12px" onclick="testarNotifDiaria()">
        <svg viewBox="0 0 24 24"><polyline points="22 2 15 22 11 13 2 9 22 2"/></svg>
        Testar notificação agora
      </button>
    </div>

    <!-- NOTIF SEMANAL -->
    <div class="au3" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:var(--r2);padding:20px;margin-bottom:14px">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
        <div style="width:40px;height:40px;background:var(--blue2);border-radius:var(--r);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0">📊</div>
        <div>
          <div style="font-size:15px;font-weight:600;color:var(--w)">Resumo semanal — toda segunda-feira</div>
          <div style="font-size:12px;color:var(--w3);margin-top:2px">Total da semana, pagamentos realizados e pendentes</div>
        </div>
        <div style="margin-left:auto">
          <label style="position:relative;display:inline-block;width:44px;height:24px;cursor:pointer">
            <input type="checkbox" id="toggleSemanal" ${cfg.semanalAtivo?'checked':''} onchange="salvarAgendConfig()" style="opacity:0;width:0;height:0"/>
            <span id="sliderSemanal" style="position:absolute;inset:0;background:${cfg.semanalAtivo?'var(--y)':'var(--bg5)'};border-radius:12px;transition:all .25s">
              <span style="position:absolute;top:3px;left:${cfg.semanalAtivo?'23':'3'}px;width:18px;height:18px;background:#fff;border-radius:50%;transition:all .25s"></span>
            </span>
          </label>
        </div>
      </div>
      <div>
        <label class="fl" style="display:block;margin-bottom:5px">Horário da segunda-feira</label>
        <input class="fi" type="time" id="horaSemanal" value="${cfg.horaSemanal||'09:00'}" style="max-width:160px" onchange="salvarAgendConfig()"/>
      </div>
      <button class="btn btn-yg btn-sm" style="margin-top:12px" onclick="testarNotifSemanal()">
        <svg viewBox="0 0 24 24"><polyline points="22 2 15 22 11 13 2 9 22 2"/></svg>
        Testar resumo semanal
      </button>
    </div>

    <!-- EXPORTAR PDF -->
    <div class="au4" style="background:var(--glass);backdrop-filter:blur(20px);border:1px solid var(--glass-border);border-radius:var(--r2);padding:20px">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
        <div style="width:40px;height:40px;background:var(--red2);border-radius:var(--r);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0">📄</div>
        <div>
          <div style="font-size:15px;font-weight:600;color:var(--w)">Exportar Cronograma em PDF</div>
          <div style="font-size:12px;color:var(--w3);margin-top:2px">Gera o PDF exato do cronograma — calendário + lista + resumo</div>
        </div>
      </div>
      <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:12px">
        <select class="fs" id="mesPdf" style="flex:1;max-width:220px">
          ${Object.keys(DB.meses).sort().reverse().map(k=>`<option value="${k}" ${k===mesAtivo?'selected':''}>${DB.meses[k].label}</option>`).join('')}
        </select>
        <button class="btn btn-p" onclick="exportarCronogramaPDF()">
          <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
          Gerar e baixar PDF
        </button>
      </div>
      <div style="font-size:11px;color:var(--w3)">O PDF inclui: calendário visual do mês, lista completa de associados, cálculo de descontos e resumo financeiro.</div>
    </div>`;
},

};// END PAGES

// ══════════════ USUÁRIOS CRUD ══════════════
function addUsuario(){
  const nome=document.getElementById('uNome').value.trim();
  const login=document.getElementById('uLogin').value.trim().toLowerCase();
  const senha=document.getElementById('uSenha').value;
  const perfil=document.getElementById('uPerfil').value;
  if(!nome){toast('Informe o nome.','err');return;}
  if(!login){toast('Informe o login.','err');return;}
  if(senha.length<4){toast('Senha deve ter ao menos 4 caracteres.','err');return;}
  const users=getUsers();
  if(users.find(u=>u.login.toLowerCase()===login)){toast('Este login já existe.','err');return;}
  const newId=Math.max(...users.map(u=>u.id),0)+1;
  users.push({id:newId,nome,login,senha,perfil,ativo:true});
  saveUsers(users);
  toast(`Usuário ${nome} cadastrado!`,'suc');
  nav('usuarios');
}
function toggleUsuario(id){
  const users=getUsers();
  const u=users.find(x=>x.id===id);
  if(!u)return;
  u.ativo=!u.ativo;
  saveUsers(users);
  toast(`Usuário ${u.ativo?'ativado':'desativado'}.`,'suc');
  nav('usuarios');
}
function editarUsuario(id){
  const users=getUsers();
  const u=users.find(x=>x.id===id);
  if(!u)return;
  document.getElementById('mbox').innerHTML=`
    <div class="mh"><h2>Editar Usuário</h2><button class="mclose" onclick="closeM()"><svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button></div>
    <div class="mb">
      <div style="display:flex;flex-direction:column;gap:12px">
        <div><label class="fl" style="display:block;margin-bottom:4px">Nome</label><input class="fi" id="euNome" value="${u.nome}"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Login</label><input class="fi" id="euLogin" value="${u.login}"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Nova senha (deixe em branco para manter)</label><input class="fi" type="password" id="euSenha" placeholder="Nova senha"/></div>
        <div><label class="fl" style="display:block;margin-bottom:4px">Perfil</label>
          <select class="fs" id="euPerfil">
            <option value="dir" ${u.perfil==='dir'?'selected':''}>Diretoria</option>
            <option value="ges" ${u.perfil==='ges'?'selected':''}>Gestor</option>
          </select>
        </div>
        <button class="btn btn-p" onclick="salvarEdicaoUsuario(${id})">Salvar alterações</button>
      </div>
    </div>`;
  document.getElementById('modal').classList.add('on');
}
function salvarEdicaoUsuario(id){
  const users=getUsers();
  const u=users.find(x=>x.id===id);
  if(!u)return;
  const nome=document.getElementById('euNome').value.trim();
  const login=document.getElementById('euLogin').value.trim().toLowerCase();
  const senha=document.getElementById('euSenha').value;
  const perfil=document.getElementById('euPerfil').value;
  if(!nome||!login){toast('Nome e login são obrigatórios.','err');return;}
  if(senha&&senha.length<4){toast('Senha mínima: 4 caracteres.','err');return;}
  // Check login unique
  if(users.find(x=>x.id!==id&&x.login.toLowerCase()===login)){toast('Login já usado por outro usuário.','err');return;}
  u.nome=nome;u.login=login;u.perfil=perfil;
  if(senha)u.senha=senha;
  saveUsers(users);
  closeM();
  toast('Usuário atualizado!','suc');
  nav('usuarios');
}

// ══════════════ ALTERAR SENHA ══════════════
function trocarMinhaSenha(){
  const atual=document.getElementById('spAtual')?.value.trim();
  const nova=document.getElementById('spNova')?.value.trim();
  const conf=document.getElementById('spConf')?.value.trim();
  if(!atual){toast('Digite sua senha atual.','err');return;}
  if(!nova||nova.length<4){toast('Nova senha: mínimo 4 caracteres.','err');return;}
  if(nova!==conf){toast('As senhas não coincidem.','err');return;}
  if(atual!==currentUser.senha){toast('Senha atual incorreta.','err');return;}
  const users=getUsers();
  const u=users.find(x=>x.id===currentUser.id);
  if(!u){toast('Usuário não encontrado.','err');return;}
  u.senha=nova;
  currentUser.senha=nova;
  saveUsers(users);
  // Update session
  try{sessionStorage.setItem('ar4_session',JSON.stringify({id:currentUser.id,perfil:currentUser.perfil}));}catch(e){}
  document.getElementById('spAtual').value='';
  document.getElementById('spNova').value='';
  document.getElementById('spConf').value='';
  addLog(`Alterou sua própria senha`);
  toast('Senha alterada com sucesso!','suc');
}

function redefinirSenhaOutro(){
  const idEl=document.getElementById('spUsuario');
  const nova=document.getElementById('spOutraNova')?.value.trim();
  const conf=document.getElementById('spOutraConf')?.value.trim();
  if(!idEl){toast('Selecione um usuário.','err');return;}
  if(!nova||nova.length<4){toast('Senha: mínimo 4 caracteres.','err');return;}
  if(nova!==conf){toast('As senhas não coincidem.','err');return;}
  const uid=parseInt(idEl.value);
  const users=getUsers();
  const u=users.find(x=>x.id===uid);
  if(!u){toast('Usuário não encontrado.','err');return;}
  u.senha=nova;
  saveUsers(users);
  document.getElementById('spOutraNova').value='';
  document.getElementById('spOutraConf').value='';
  addLog(`Redefiniu senha de ${u.nome}`);
  toast(`Senha de ${u.nome} redefinida!`,'suc');
}

// Legacy function kept for compatibility
function alterarSenha(perfil,id1,id2,id0){
  trocarMinhaSenha();
}

// ══════════════ TOAST ══════════════
function toast(msg,type='suc'){
  const t=document.getElementById('toast');
  document.getElementById('tm').textContent=msg;
  t.className=`toast ${type}`;t.classList.add('on');
  clearTimeout(t._t);t._t=setTimeout(()=>t.classList.remove('on'),3200);
}

// ══════════════ THEME ══════════════
function applyTheme(t){
  const r=document.documentElement;
  if(t==='light'){
    r.style.setProperty('--bg','#f5f5f5');r.style.setProperty('--bg2','#ffffff');
    r.style.setProperty('--bg3','#eeeeee');r.style.setProperty('--bg4','#dddddd');
    r.style.setProperty('--bg5','#cccccc');r.style.setProperty('--w','#111111');
    r.style.setProperty('--w2','#333333');r.style.setProperty('--w3','#555555');r.style.setProperty('--w4','#888888');
  } else {
    r.style.setProperty('--bg','#0c0c0c');r.style.setProperty('--bg2','#141414');
    r.style.setProperty('--bg3','#1c1c1c');r.style.setProperty('--bg4','#252525');
    r.style.setProperty('--bg5','#2e2e2e');r.style.setProperty('--w','#efefef');
    r.style.setProperty('--w2','#b0b0b0');r.style.setProperty('--w3','#707070');r.style.setProperty('--w4','#404040');
  }
  try{localStorage.setItem('ar4_theme',t);}catch(e){}
}

// ══════════════ FORCE PASSWORD CHANGE ══════════════
function checkForcePassword(){
  const users=getUsers();
  const u=users.find(x=>x.id===(currentUser&&currentUser.id));
  if(u&&u.forceChange){
    const scr=document.getElementById('forcePassScreen');
    if(scr)scr.style.display='flex';
    return true;
  }
  return false;
}

function confirmarForceSenha(){
  const p1=document.getElementById('fp1')?document.getElementById('fp1').value:'';
  const p2=document.getElementById('fp2')?document.getElementById('fp2').value:'';
  if(!p1||p1.length<4){toast('A senha deve ter pelo menos 4 caracteres.','err');return;}
  if(p1!==p2){toast('As senhas não coincidem.','err');return;}
  const users=getUsers();
  const idx=users.findIndex(x=>x.id===(currentUser&&currentUser.id));
  if(idx<0){toast('Usuário não encontrado.','err');return;}
  users[idx].senha=p1;
  users[idx].forceChange=false;
  saveUsers(users);
  currentUser=users[idx];
  const scr=document.getElementById('forcePassScreen');
  if(scr)scr.style.display='none';
  toast('Senha definida com sucesso!');
  addLog('Senha alterada (primeiro acesso): '+currentUser.login);
  if(role==='dir'){nav('boasvindas');}else{nav('dashboard');}
}

// ══════════════ NOTIFICAÇÕES AGENDADAS ══════════════
function verificarNotificacoesAgendadas(){
  try{
    if(!('Notification' in window))return;
    const cfg=JSON.parse(localStorage.getItem('ar4_agend')||'{}');
    if(!cfg.notifDiaria)return;
  }catch(e){}
}

// ══════════════ GET EVENT TYPES ══════════════
function getEventTypes(mesKey){
  let indenizacoes=[];
  if(mesKey&&DB.meses[mesKey]){
    indenizacoes=DB.meses[mesKey].indenizacoes.filter(i=>i.status!=='FERIADO');
  } else {
    Object.values(DB.meses).forEach(m=>{
      (m.indenizacoes||[]).forEach(i=>{ if(i.status!=='FERIADO') indenizacoes.push(i); });
    });
  }
  const map={};
  indenizacoes.forEach(i=>{
    const tipo=i.tipo||i.status||'Outros';
    if(!map[tipo]) map[tipo]={tipo,total:0,valor:0,count:0};
    map[tipo].total+=(i.valorLiq||0);
    map[tipo].valor+=(i.valorFipe||0);
    map[tipo].count++;
  });
  return Object.values(map).sort((a,b)=>b.total-a.total);
}

// ══════════════ GET NOTIF STATUS ══════════════
const NOTIF_KEY='ar4_notif_enabled';
function getNotifStatus(){
  if(!('Notification' in window)) return 'unsupported';
  return Notification.permission; // 'granted','denied','default'
}

// ══════════════ TOGGLE THEME ══════════════
function toggleTheme(){
  const cur=document.documentElement.style.getPropertyValue('--bg').trim();
  const isLight=(cur==='#f5f5f5');
  applyTheme(isLight?'dark':'light');
  const btn=document.getElementById('themeBtn');
  const btnMob=document.getElementById('themeBtnMob');
  const label=isLight?'🌙 Escuro':'☀️ Claro';
  if(btn)btn.innerHTML=btn.innerHTML.replace(/[☀️🌙].*/,label);
  if(btnMob)btnMob.innerHTML=btnMob.innerHTML.replace(/[☀️🌙].*/,label);
}

// ══════════════ MES LABEL ══════════════
const mesLabel=k=>DB.meses[k]?DB.meses[k].label:k;

// ══════════════ UTILITÁRIOS GLOBAIS ══════════════
// Declared as window props so they don't conflict with const inside page functions
window.brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
window.fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
window.isFerG=i=>i.status==='FERIADO';



// ══════════════ TIMEOUT DE SESSÃO ══════════════
let _timeoutTimer=null,_warnTimer=null,_warnInterval=null;
function resetTimeout(){
  clearTimeout(_timeoutTimer);
  _timeoutTimer=setTimeout(()=>{
    const w=document.getElementById('timeoutWarn');
    if(w)w.style.display='flex';
  },14*60*1000);
}
document.addEventListener('click',resetTimeout);
document.addEventListener('keydown',resetTimeout);

// ══════════════ RENDER LIST ══════════════
function renderList(el){
  if(!el){el=document.getElementById('pg-indenizacoes');if(!el)return;}
  const mes=getMes();
  const all=mes.indenizacoes||[];
  const ft=filterTab;
  const items=ft==='todos'?all:ft==='pago'?all.filter(i=>i.status==='PAGO'):ft==='pendente'?all.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO'):all.filter(i=>i.status===ft);
  const pend=all.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length;
  const pago=all.filter(i=>i.status==='PAGO').length;
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
  const isFer=i=>i.status==='FERIADO';
  const rows=items.map(i=>`
    <tr onclick="showDetail(${i.id},'${mesAtivo}')">
      <td class="nc">${isFer(i)?'<span class="badge bf">FERIADO</span>':i.nome}</td>
      <td class="mc">${fdt(i.data)}</td>
      <td class="mc">${i.parcela||'—'}</td>
      <td><span class="badge ${statusColor(i.status)}">${i.status}</span></td>
      <td class="vc">${i.valorLiq>0?brl(i.valorLiq):'—'}</td>
    </tr>`).join('');
  el.innerHTML=`
    <div class="tb au"><div><h1>Indenizações</h1><p>${mesLabel(mesAtivo)} · ${all.length} registros</p></div></div>
    <div class="card au1">
      <div class="ch">
        <div class="tabs">
          <button class="tab${ft==='todos'?' on':''}" onclick="setFt('todos')">Todos (${all.length})</button>
          <button class="tab${ft==='pago'?' on':''}" onclick="setFt('pago')">Pagos (${pago})</button>
          <button class="tab${ft==='pendente'?' on':''}" onclick="setFt('pendente')">Pendentes (${pend})</button>
        </div>
      </div>
      <div class="tw"><table class="dt">
        <thead><tr><th>Associado</th><th>Data</th><th>Parcela</th><th>Status</th><th style="text-align:right">Valor Líquido</th></tr></thead>
        <tbody>${rows||'<tr><td colspan="5" style="text-align:center;padding:20px;color:var(--w3)">Nenhum registro encontrado</td></tr>'}</tbody>
      </table></div>
    </div>`;
}
function setFt(f){filterTab=f;const el=document.getElementById('pg-indenizacoes');if(el){el.innerHTML='';renderList(el);}}

// ══════════════ SHOW DETAIL ══════════════
function showDetail(id,mesKey){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
  const mes=DB.meses[mesKey||mesAtivo];
  if(!mes)return;
  const i=mes.indenizacoes.find(x=>x.id===id);
  if(!i)return;
  const isFer=x=>x.status==='FERIADO';
  let body='';
  if(isFer(i)){
    body=`<div class="dg"><p style="color:var(--w3);font-size:13px">Sem expediente neste dia.</p></div>`;
  } else {
    const descs=[
      ['Taxa de Transferência',i.taxaTransf||0],['Cota de Participação',i.cotaPart||0],
      ['Boletos Contribuição',i.boletosContr||0],['Licenciamento',i.licenciamento||0],
      ['IPVA',i.ipva||0],['Bombeiro',i.bombeiro||0],['Multa',i.multa||0],['Financiamento',i.financiamento||0],
      ['Franquia',i.franquia||0],['Rateio',i.rateio||0],['Outros',i.outros||0]
    ].filter(([,v])=>v>0);
    const totalDesc=descs.reduce((s,[,v])=>s+v,0);
    body=`
      <div class="dg">
        <h4>Dados Gerais</h4>
        <div class="dr"><span class="dl">Associado</span><span class="dv">${i.nome}</span></div>
        <div class="dr"><span class="dl">Data</span><span class="dv">${fdt(i.data)}</span></div>
        ${i.parcela?`<div class="dr"><span class="dl">Parcela</span><span class="dv">${i.parcela}</span></div>`:''}
        ${i.obs?`<div class="dr"><span class="dl">Observação</span><span class="dv" style="color:var(--orange);font-size:11px">${i.obs}</span></div>`:''}
      </div>
      ${totalDesc>0||i.valorFipe>0?`
      <div class="dg">
        <h4>Cálculo de Descontos</h4>
        ${i.valorFipe>0?`<div class="dr"><span class="dl">Valor FIPE</span><span class="dv">${brl(i.valorFipe)}</span></div>`:''}
        ${descs.map(([l,v])=>`<div class="dr"><span class="dl">— ${l}</span><span class="dv r">− ${brl(v)}</span></div>`).join('')}
      </div>`:''}
      <div class="ct2"><span class="cl">Valor a pagar ao associado</span><span class="cv">${brl(i.valorLiq)}</span></div>
      ${role==='ges'?`<div style="margin-top:12px;display:flex;gap:7px">
        <button class="btn btn-yg btn-sm" onclick="closeM();startEdit(${i.id})">Editar</button>
        <button class="btn btn-d btn-sm" onclick="closeM();delItem(${i.id})">Excluir</button>
      </div>`:''}`;
  }
  document.getElementById('mbox').innerHTML=`
    <div class="mh"><h2>${i.nome}</h2><button class="mc2" onclick="closeM()"><svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button></div>
    <div class="mb">
      <div style="display:flex;align-items:center;gap:7px;margin-bottom:12px"><span class="badge ${statusColor(i.status)}">${i.status}</span></div>
      ${body}
    </div>`;
  document.getElementById('modal').classList.add('on');
}

function showDay(d,mesKey){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
  const mes=DB.meses[mesKey||mesAtivo];
  if(!mes)return;
  const items=mes.indenizacoes.filter(i=>i.data&&parseInt(i.data.split('-')[2])===d);
  if(!items.length)return;
  if(items.length===1){showDetail(items[0].id,mesKey);return;}
  const rows=items.map(i=>`
    <div style="display:flex;justify-content:space-between;align-items:center;padding:9px 0;border-bottom:1px solid var(--bg3);cursor:pointer;font-size:13px" onclick="closeM();setTimeout(()=>showDetail(${i.id},'${mesKey}'),120)">
      <div><div style="font-weight:500">${i.nome}</div><div style="font-size:11px;color:var(--w3)">${i.obs||'—'}</div></div>
      <div style="display:flex;align-items:center;gap:8px">
        ${i.valorLiq>0?`<span style="font-family:'IBM Plex Mono',monospace;color:var(--y);font-size:12px;font-weight:600">${brl(i.valorLiq)}</span>`:''}
        <span class="badge ${statusColor(i.status)}">${i.status}</span>
      </div>
    </div>`).join('');
  document.getElementById('mbox').innerHTML=`
    <div class="mh"><h2>Dia ${d} — ${mesLabel(mesKey||mesAtivo)}</h2><button class="mc2" onclick="closeM()"><svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button></div>
    <div class="mb">${rows}</div>`;
  document.getElementById('modal').classList.add('on');
}

function showMonthSumm(key,label,valor,pagamentos){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const mes=DB.meses[key];
  document.getElementById('mbox').innerHTML=`
    <div class="mh"><h2>${label}</h2><button class="mc2" onclick="closeM()"><svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button></div>
    <div class="mb">
      <div class="dg">
        <h4>Resumo do Mês</h4>
        <div class="dr"><span class="dl">Total Indenizado</span><span class="dv y">${brl(valor)}</span></div>
        <div class="dr"><span class="dl">Pagamentos</span><span class="dv">${pagamentos}</span></div>
        <div class="dr"><span class="dl">Ticket Médio</span><span class="dv">${brl(valor/pagamentos)}</span></div>
        ${mes?`<div class="dr"><span class="dl">Beneficiários</span><span class="dv">${mes.meta.beneficiarios||'—'}</span></div>`:''}
      </div>
      ${mes?`<button class="btn btn-yg btn-sm" onclick="closeM();mesAtivo='${key}';nav('indenizacoes')">Ver indenizações deste mês →</button>`:''}
    </div>`;
  document.getElementById('modal').classList.add('on');
}

function closeM(){document.getElementById('modal').classList.remove('on')}

// ══════════════ CALC PREVIEW ══════════════
function calcPrev(){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const fipe=parseFloat(document.getElementById('ff')?.value)||0;
  const cpEl=document.getElementById('calcPrev');
  if(!fipe){if(cpEl)cpEl.innerHTML='';return;}
  const fields=[['ftt','Taxa Transferência'],['fcp','Cota de Participação'],['fbc','Boletos Contribuição'],['flic','Licenciamento'],['fipva','IPVA'],['fbomb','Bombeiro'],['fmul','Multa'],['ffin','Financiamento']];
  let totalDesc=0;
  const lines=fields.map(([fid,l])=>{
    const v=parseFloat(document.getElementById(fid)?.value)||0;
    if(v>0){totalDesc+=v;return `<div class="cl2">— ${l}<span>− ${brl(v)}</span></div>`;}
    return '';
  }).join('');
  const liqCalc=fipe-totalDesc;
  if(cpEl)cpEl.innerHTML=`
    <div class="cl2">Valor FIPE<span>${brl(fipe)}</span></div>
    ${lines}
    <div class="cl2 tot">Valor Líquido Calculado<span>${brl(liqCalc)}</span></div>`;
  const fliq=document.getElementById('fliq');
  if(fliq&&totalDesc>0)fliq.value=liqCalc.toFixed(2);
}

// ══════════════ SALVAR ══════════════
function salvar(){
  const nome=document.getElementById('fn')?.value.trim();
  if(!nome){toast('Informe o nome do associado.','err');return;}
  const liqVal=parseFloat(document.getElementById('fliq')?.value)||0;
  const status=document.getElementById('fst')?.value;
  if(liqVal<=0&&status!=='FERIADO'){toast('Informe o valor líquido.','err');return;}
  const data={
    nome,
    data:document.getElementById('fdt')?.value,
    parcela:document.getElementById('fp')?.value||'',
    status:status||'PENDENTE',
    obs:document.getElementById('fobs')?.value||'',
    valorFipe:parseFloat(document.getElementById('ff')?.value)||0,
    taxaTransf:parseFloat(document.getElementById('ftt')?.value)||0,
    cotaPart:parseFloat(document.getElementById('fcp')?.value)||0,
    boletosContr:parseFloat(document.getElementById('fbc')?.value)||0,
    licenciamento:parseFloat(document.getElementById('flic')?.value)||0,
    ipva:parseFloat(document.getElementById('fipva')?.value)||0,
    bombeiro:parseFloat(document.getElementById('fbomb')?.value)||0,
    multa:parseFloat(document.getElementById('fmul')?.value)||0,
    financiamento:parseFloat(document.getElementById('ffin')?.value)||0,
    valorLiq:liqVal,
    comprovante:window._comprovante||(editId?(getMes().indenizacoes.find(i=>i.id===editId)||{}).comprovante:null)||null,
  };
  window._comprovante=null;
  // Usar o mês selecionado no formulário, se houver (evita salvar no mês errado)
  const _fm=document.getElementById('fmes');
  const _targetMes=(_fm&&_fm.value)?_fm.value:mesAtivo;
  if(!DB.meses[_targetMes])DB.meses[_targetMes]={label:_targetMes,indenizacoes:[],meta:{}};
  const mes=DB.meses[_targetMes];
  if(editId){
    const idx=mes.indenizacoes.findIndex(i=>i.id===editId);
    if(idx>=0)mes.indenizacoes[idx]={...mes.indenizacoes[idx],...data};
    toast('Indenização atualizada!');
  } else {
    mes.indenizacoes.push({id:DB.nextId++,...data});
    toast('Indenização adicionada!');
  }
  const all=mes.indenizacoes.filter(i=>i.status!=='FERIADO');
  const hIdx=HIST.findIndex(h=>h.key===_targetMes);
  if(hIdx>=0){
    HIST[hIdx].valor=all.reduce((s,i)=>s+i.valorLiq,0);
    HIST[hIdx].pagamentos=all.length;
  } else {
    HIST.unshift({key:_targetMes,label:mes.label,valor:all.reduce((s,i)=>s+i.valorLiq,0),pagamentos:all.length});
    HIST.sort((a,b)=>b.key.localeCompare(a.key));
  }
  mesAtivo=_targetMes;
  saveDBLocal();
  editId=null;buildNav();
  toast('Salvando...','inf');
  pushNow().then(()=>{ toast('Indenização salva!','suc'); }).catch(()=>{});
  nav('indenizacoes');
}

function startEdit(id){editId=id;nav('novo')}

function delItem(id){
  if(!confirm('Confirma a exclusão?'))return;
  const mes=getMes();
  mes.indenizacoes=mes.indenizacoes.filter(i=>i.id!==id);
  const hIdx=HIST.findIndex(h=>h.key===mesAtivo);
  if(hIdx>=0){
    const all=mes.indenizacoes.filter(i=>!isFer(i));
    HIST[hIdx].valor=all.reduce((s,i)=>s+(i.valorLiq||0),0);
    HIST[hIdx].pagamentos=all.length;
  }
  saveDBLocal();
  buildNav();
  toast('Excluindo...','inf');
  pushNow().then(()=>{ toast('Registro excluído!','suc'); }).catch(()=>{});
  nav('gerenciar');
}

// ══════════════ PDF IMPORT ══════════════
function dropPDF(e){
  e.preventDefault();
  document.getElementById('upz')?.classList.remove('drag');
  const f=e.dataTransfer.files[0];
  if(f&&f.type==='application/pdf')handlePDF(f);
  else toast('Envie um arquivo PDF.','err');
}

async function handlePDF(file){
  if(!file)return;
  const card=document.getElementById('importCard');
  if(!card)return;
  card.innerHTML=`<div class="proc"><div class="proc-spin"></div><p>Lendo o PDF...</p><span>Extraindo texto do cronograma</span></div>`;
  try{
    const ab=await file.arrayBuffer();
    const pdf=await pdfjsLib.getDocument({data:ab}).promise;
    let fullText='';
    for(let p=1;p<=pdf.numPages;p++){
      const pg=await pdf.getPage(p);
      const tc=await pg.getTextContent();
      // Reconstrói as linhas usando a coordenada Y de cada item
      const rows={};
      tc.items.forEach(it=>{
        if(!it.str||!it.str.trim())return;
        const y=Math.round(it.transform[5]/3)*3; // agrupa por faixa de 3px
        if(!rows[y])rows[y]=[];
        rows[y].push({x:it.transform[4],str:it.str});
      });
      // Ordena: Y descendente (topo→base), X ascendente (esq→dir)
      const sortedY=Object.keys(rows).map(Number).sort((a,b)=>b-a);
      sortedY.forEach(y=>{
        const line=rows[y].sort((a,b)=>a.x-b.x).map(i=>i.str).join(' ').replace(/\s+/g,' ').trim();
        if(line)fullText+=line+'\n';
      });
      fullText+='\n';
    }
    if(!fullText.trim()){
      card.innerHTML=`<div class="ch"><span class="ct">Upload do Cronograma</span></div>
        <p style="color:var(--orange);padding:10px;font-size:13px">O PDF não contém texto extraível (pode ser uma imagem escaneada). Use a aba <strong>Colar Texto</strong> e cole os dados manualmente.</p>
        <button class="btn btn-g btn-sm" onclick="nav('importar')">Voltar</button>`;
      return;
    }
    parsePDFText(fullText,card);
  }catch(e){
    card.innerHTML=`<div class="ch"><span class="ct">Upload do Cronograma</span></div>
      <p style="color:var(--red);padding:10px;font-size:13px">Erro ao ler PDF: ${e.message}</p>
      <button class="btn btn-g btn-sm" onclick="nav('importar')">Tentar novamente</button>`;
    toast('Erro ao processar PDF.','err');
  }
}


function parsePDFText(text,card){
  const brl=window.brl;
  const fdt=window.fdt;

  // ── Detectar mês/ano de referência ──
  let mesKey=null;
  const selPdf=document.getElementById('importMesKey');
  const selTxt=document.getElementById('textoMesKey');
  if(card&&card.id==='textoPreview'&&selTxt&&selTxt.value)mesKey=selTxt.value;
  else if(selPdf&&selPdf.value)mesKey=selPdf.value;
  else if(selTxt&&selTxt.value)mesKey=selTxt.value;
  const NOMES={janeiro:'01',fevereiro:'02',marco:'03',abril:'04',maio:'05',junho:'06',julho:'07',agosto:'08',setembro:'09',outubro:'10',novembro:'11',dezembro:'12'};
  let anoRef='2026', mesRef=null;
  const mRef=text.match(/(janeiro|fevereiro|mar[\u00e7c]o|abril|maio|junho|julho|agosto|setembro|outubro|novembro|dezembro)\s*[-\u2013\/]?\s*(?:de\s*)?(\d{4})/i);
  if(mRef){
    const mn=mRef[1].toLowerCase().normalize('NFD').replace(/\p{Diacritic}/gu,'');
    mesRef=NOMES[mn]||null; anoRef=mRef[2];
    if(!mesKey&&mesRef)mesKey=`${anoRef}-${mesRef}`;
  }
  if(!mesKey){
    const dts=[...text.matchAll(/\d{1,2}\/(\d{1,2})\/(\d{4})/g)];
    if(dts.length)mesKey=`${dts[0][2]}-${String(dts[0][1]).padStart(2,'0')}`;
    else mesKey=mesAtivo;
  }
  const [ano,mesNum]=mesKey.split('-');
  if(!mesRef)mesRef=mesNum;
  if(anoRef==='2026'&&ano)anoRef=ano;
  const labelFull=`${MESES_PT[parseInt(mesNum)]} ${ano}`;

  const indenizacoes=[];
  let nextId=DB.nextId;
  const vistos=new Set(); // evita duplicar (nome+data+parcela)

  function normNome(s){
    return s.replace(/\(\d{1,2}\/\d{1,2}\)/g,'')      // tira (1/2)
            .replace(/R\$.*/,'')                        // corta a partir de R$
            .replace(/\d{1,2}\/\d{1,2}(\/\d{2,4})?.*/,'')// corta a partir da data
            .replace(/\s+/g,' ').trim();
  }
  function statusDe(txt){
    const up=(txt||'').toUpperCase();
    if(/DEPENDE\s+DO\s+BOLETO/.test(up))return 'DEPENDE DO BOLETO';
    if(/VALOR\s+RESTANTE/.test(up))return 'VALOR RESTANTE';
    if(/JUNHO|JULHO|AGOSTO|SETEMBRO|OUTUBRO|NOVEMBRO|DEZEMBRO|JANEIRO|FEVEREIRO|MAR[\u00c7C]O|ABRIL|MAIO\/\d/.test(up))return 'PARCELA FUTURA';
    if(/PARCELADO/.test(up))return 'PARCELADO';
    if(/\bPAGO\b/.test(up))return 'PAGO';
    if(/PENDENTE/.test(up))return 'PENDENTE';
    return 'PENDENTE';
  }
  function add(nome,valor,dataStr,parcela,status,obs){
    const chave=nome.toUpperCase()+'|'+dataStr+'|'+(parcela||'');
    if(vistos.has(chave))return;
    vistos.add(chave);
    indenizacoes.push({id:nextId++,nome,data:dataStr,parcela:parcela||'',status,obs:obs||'',
      valorLiq:valor,valorFipe:0,taxaTransf:0,cotaPart:0,boletosContr:0,
      licenciamento:0,ipva:0,bombeiro:0,multa:0,financiamento:0});
  }

  const linhas=text.split(/[\n\r]+/);

  // ── PADRÃO A: linha de TABELA (a mais confiável) ──
  // NOME [(p/q)] R$ valor DD/MM[/AAAA] [parcela] [STATUS...] [- obs]
  const RE_TAB=/^(.+?)\s+R\$\s*([\d.]+,\d{2})\s+(\d{1,2})\/(\d{1,2})(?:\/(\d{2,4}))?\s*(.*)$/;
  linhas.forEach(raw=>{
    const line=raw.replace(/\s+/g,' ').trim();
    if(line.length<10)return;
    const up=line.toUpperCase();
    if(/^BENEFICI|^TOTAL|CRONOGRAMA|^DOM\s|RESUMO|OBSERVA|RELAT[OÓ]RIO|DETALHAMENTO|^P[AÁ]GINA|CALEND[AÁ]RIO|^DIAS|^\d+\s+PAGAMENTOS/i.test(up))return;
    const m=line.match(RE_TAB);
    if(!m)return;
    let nome=normNome(m[1]);
    if(nome.length<3)return;
    const valor=parseFloat(m[2].replace(/\./g,'').replace(',','.'));
    if(!valor||valor<=0)return;
    let aa=m[5]||anoRef; if(aa.length===2)aa='20'+aa;
    const dataStr=`${aa}-${String(m[4]).padStart(2,'0')}-${String(m[3]).padStart(2,'0')}`;
    // parcela: primeiro tenta no nome original (1/2), senão no resto
    let parcela='';
    const pNome=m[1].match(/\((\d{1,2}\/\d{1,2})\)/);
    const pResto=m[6].match(/\b(\d{1,2}\/\d{1,2})\b/);
    if(pNome)parcela=pNome[1]; else if(pResto)parcela=pResto[1];
    const status=statusDe(m[6]);
    const obsM=m[6].match(/-\s*(.+)$/);
    add(nome,valor,dataStr,parcela,status,obsM?obsM[1].trim():'');
  });

  // ── PADRÃO B (fallback): calendário quebrado em várias linhas ──
  // Reagrupa: NOME (linhas MAIÚSCULAS) + "R$ valor" + "1/2" + "STATUS"
  if(indenizacoes.length===0){
    for(let i=0;i<linhas.length;i++){
      const valM=linhas[i].match(/R\$\s*([\d.]+(?:,\d{2})?)/);
      if(!valM)continue;
      const valor=parseFloat(valM[1].replace(/\./g,'').replace(',','.'));
      if(!valor||valor<=0)continue;
      // nome = linhas anteriores em maiúsculas
      let nomeParts=[];
      for(let j=i-1;j>=0&&j>=i-4;j--){
        const t=linhas[j].replace(/\s+/g,' ').trim();
        if(/^\d+$/.test(t))break;
        if(/^[A-ZÀ-Ú\s]{3,}$/.test(t)&&!/R\$|TOTAL|DEPENDE|PENDENTE|PARCELADO|FORTALEZA/i.test(t))nomeParts.unshift(t);
        else break;
      }
      const nome=normNome(nomeParts.join(' '));
      if(nome.length<3)continue;
      // procurar parcela e status nas próximas linhas
      let parcela='',statusTxt='';
      for(let k=i+1;k<=i+5&&k<linhas.length;k++){
        const t=linhas[k].trim();
        const pp=t.match(/^(\d{1,2}\/\d{1,2})/);
        if(pp&&!parcela)parcela=pp[1];
        if(/DEPENDE|PARCELADO|PENDENTE|PAGO|FORTALEZA/i.test(t))statusTxt+=' '+t;
      }
      add(nome,valor,`${anoRef}-${String(mesRef).padStart(2,'0')}-01`,parcela,statusDe(statusTxt),'');
    }
  }

  indenizacoes.sort((a,b)=>(a.data||'').localeCompare(b.data||''));
  DB.nextId=nextId;

  const indReal=indenizacoes.filter(i=>i.status!=='FERIADO');
  const totalPrev=indReal.reduce((s,i)=>s+i.valorLiq,0);
  const previewRows=indenizacoes.map(i=>`
    <div class="imp-item">
      <div class="in">${i.status==='FERIADO'?'FERIADO':i.nome}${i.parcela?` <span style="color:var(--w3);font-size:10px">(${i.parcela})</span>`:''}</div>
      <div class="id2">${fdt(i.data)}</div>
      <div class="iv">${i.valorLiq>0?brl(i.valorLiq):'—'}</div>
      <div class="ib"><span class="badge ${statusColor(i.status)}">${i.status}</span></div>
    </div>`).join('');

  const targetEl=card||document.getElementById('importCard');
  if(!targetEl)return;
  window._importPending={mesKey,labelFull,indenizacoes,pag:indReal.length,ben:new Set(indReal.map(i=>i.nome)).size};

  const amostra=linhas.map(l=>l.trim()).filter(l=>l.length>3).slice(0,14).join('\n');
  targetEl.innerHTML=`
    <div class="ch"><span class="ct">Leitura concluída — ${labelFull}</span></div>
    ${indenizacoes.length?`
    <div style="background:var(--bg3);border-radius:var(--r);padding:10px 12px;margin-bottom:12px;display:flex;gap:20px;flex-wrap:wrap;font-size:12px">
      <span>Total: <strong style="color:var(--y)">${brl(totalPrev)}</strong></span>
      <span>Registros: <strong>${indReal.length}</strong></span>
      <span>Beneficiários: <strong>${new Set(indReal.map(i=>i.nome)).size}</strong></span>
    </div>
    <div style="max-height:340px;overflow-y:auto">${previewRows}</div>
    <div style="display:flex;gap:8px;margin-top:14px;flex-wrap:wrap">
      <button class="btn btn-p" onclick="confirmImportPending()">Confirmar e importar ${indReal.length} registros</button>
      <button class="btn btn-g" onclick="nav('importar')">Cancelar</button>
    </div>`:`
    <p style="color:var(--orange);padding:8px 0;font-size:13px"><strong>Nenhuma indenização reconhecida.</strong><br>
    Cada linha precisa ter pelo menos nome, valor e data.</p>
    <div style="font-size:11px;color:var(--w3);margin-bottom:6px">Texto lido (amostra) — envie um print para suporte:</div>
    <pre style="background:var(--bg3);border-radius:8px;padding:10px;font-size:10px;line-height:1.5;overflow-x:auto;white-space:pre-wrap;max-height:220px;color:var(--w2)">${amostra.replace(/</g,'&lt;')||'(vazio)'}</pre>
    <button class="btn btn-g btn-sm" onclick="nav('importar')" style="margin-top:8px">Voltar</button>`}`;
}

function confirmImportPending(){
  const p=window._importPending;
  if(!p){toast('Nada para importar.','err');return;}
  confirmImport(p.mesKey,p.labelFull,p.indenizacoes,p.pag,p.ben,0,0);
  window._importPending=null;
}


function confirmImport(key,label,indenizacoes,pag,ben,dias,cond){
  const total=indenizacoes.filter(i=>i.status!=='FERIADO').reduce((s,i)=>s+i.valorLiq,0);
  DB.meses[key]={label,indenizacoes,meta:{totalBruto:total,pagamentos:pag,beneficiarios:ben,diasPag:dias,condicionais:cond}};
  const hIdx=HIST.findIndex(h=>h.key===key);
  if(hIdx>=0){HIST[hIdx].valor=total;HIST[hIdx].pagamentos=pag;}
  else HIST.unshift({key,label:label.slice(0,3)+'/'+label.slice(-2),valor:total,pagamentos:pag});
  HIST.sort((a,b)=>b.key.localeCompare(a.key));
  mesAtivo=key;
  saveDBLocal();
  buildNav();
  toast('Importando para o banco...','inf');
  pushNow().then(()=>{ toast(`${label} importado! ${indenizacoes.length} registros.`,'suc'); }).catch(()=>{});
  nav('dashboard');
}

// ══════════════ TOGGLE SENHA (olhinho) ══════════════
function togglePass(inputId,btn){
  const inp=document.getElementById(inputId);
  if(!inp)return;
  const show=inp.type==='password';
  inp.type=show?'text':'password';
  if(btn)btn.innerHTML=show
    ?'<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></svg>'
    :'<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>';
}

// ══════════════ CONSULTORES ══════════════
function addConsultor(){
  const inp=document.getElementById('novoConsInput');
  if(!inp)return;
  const nome=inp.value.trim().toUpperCase();
  if(!nome){toast('Informe o nome do consultor.','err');return;}
  const cons=getConsultores();
  if(cons.includes(nome)){toast('Consultor já cadastrado.','err');return;}
  cons.push(nome);
  saveConsultores(cons);
  inp.value='';
  toast('Consultor adicionado!');
  nav('gesCons');
}

function removerConsultor(nome){
  if(!confirm('Remover consultor "'+nome+'"?'))return;
  const cons=getConsultores().filter(c=>c!==nome);
  saveConsultores(cons);
  toast('Consultor removido.');
  nav('gesCons');
}

// ══════════════ EXPORTAR BACKUP ══════════════
function exportarBackup(){
  try{
    const data={DB,HIST,exportedAt:new Date().toISOString(),version:'ar4-v2'};
    const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});
    const url=URL.createObjectURL(blob);
    const a=document.createElement('a');
    a.href=url;
    a.download='ar4-backup-'+new Date().toISOString().slice(0,10)+'.json';
    a.click();
    URL.revokeObjectURL(url);
    toast('Backup exportado com sucesso!');
    addLog('Backup exportado');
  }catch(e){toast('Erro ao exportar backup: '+e.message,'err');}
}

// ══════════════ APROVAÇÕES ══════════════
function criarAprovacaoTeste(){
  const mes=getMes();
  const ind=mes.indenizacoes||[];
  const indReal=ind.filter(i=>!window.isFerG(i));
  if(!indReal.length){toast('Nenhuma indenização no mês ativo para criar aprovação.','err');return;}
  const total=indReal.reduce((s,i)=>s+(i.valorLiq||0),0);
  const [mAnoS,mNumS]=mesAtivo.split('-');
  addAprovacao({
    id:Date.now(),
    mesKey:mesAtivo,
    mesLabel:mesLabel(mesAtivo),
    mAno:parseInt(mAnoS),
    mNum:parseInt(mNumS),
    status:'pendente',
    criadoEm:new Date().toISOString(),
    solicitante:(currentUser&&currentUser.nome)||'Gestor',
    meta:{
      totalBruto:total,
      pagamentos:indReal.length,
      beneficiarios:new Set(indReal.map(i=>i.nome)).size,
    },
    indenizacoes:JSON.parse(JSON.stringify(ind)),
    obs:'Aprovação de teste criada manualmente.'
  });
  toast('Aprovação de teste criada!');
  nav('aprovacoes');
}


function responderAprovacao(id,acao){
  const aprov=getAprovacoes();
  const idx=aprov.findIndex(a=>a.id===id);
  if(idx<0){toast('Aprovação não encontrada.','err');return;}
  const obs=document.getElementById('obs_'+id)?(document.getElementById('obs_'+id).value||''):'';
  aprov[idx].status=acao; // 'aprovado' ou 'reprovado'
  aprov[idx].resposta=obs;
  aprov[idx].respondidoPor=(currentUser&&currentUser.nome)||role;
  aprov[idx].respondidoEm=new Date().toISOString();
  saveAprovacoes(aprov);
  toast(acao==='aprovado'?'Cronograma aprovado!':'Cronograma reprovado.');
  nav('aprovacoes');
}


function cancelarAprovacao(id){
  if(!confirm('Cancelar esta solicitação?'))return;
  const aprov=getAprovacoes().filter(a=>a.id!==id);
  saveAprovacoes(aprov);
  toast('Solicitação cancelada.');
  nav('aprovacoes');
}

function aplicarAprovacao(id){
  const aprov=getAprovacoes().find(a=>a.id===id);
  if(!aprov){toast('Aprovação não encontrada.','err');return;}
  if(DB.meses[aprov.mesKey]){toast('Mês já existe no sistema.','inf');return;}
  const inds=aprov.indenizacoes||[];
  const indReal=inds.filter(i=>i.status!=='FERIADO');
  const total=indReal.reduce((s,i)=>s+(i.valorLiq||0),0);
  DB.meses[aprov.mesKey]={
    label:aprov.mesLabel,
    ano:aprov.mAno,mes:aprov.mNum,
    indenizacoes:inds,
    meta:(aprov.meta||{totalBruto:total,pagamentos:indReal.length})
  };
  const hIdx=HIST.findIndex(h=>h.key===aprov.mesKey);
  if(hIdx<0)HIST.unshift({key:aprov.mesKey,label:aprov.mesLabel,valor:total,pagamentos:indReal.length});
  HIST.sort((a,b)=>b.key.localeCompare(a.key));
  mesAtivo=aprov.mesKey;
  saveDB();
  addLog('Aprovação aplicada: '+aprov.mesLabel);
}


// ══════════════ NOTIFICAÇÕES ══════════════
function ativarNotificacoes(){
  if(!('Notification' in window)){toast('Notificações não suportadas neste navegador.','err');return;}
  Notification.requestPermission().then(p=>{
    if(p==='granted'){
      try{localStorage.setItem(NOTIF_KEY,'1');}catch(e){}
      toast('Notificações ativadas!');
    } else {
      toast('Permissão negada. Habilite nas configurações do navegador.','err');
    }
    nav('notificacoes');
  });
}

function desativarNotificacoes(){
  try{localStorage.setItem(NOTIF_KEY,'0');}catch(e){}
  toast('Notificações desativadas.');
  nav('notificacoes');
}

function enviarNotificacaoLocal(titulo,corpo){
  if(Notification.permission!=='granted'){toast('Ative as notificações primeiro.','err');return;}
  try{new Notification(titulo,{body:corpo,icon:'https://i.ibb.co/W4myqrMK/logo-ar4-1769255749160-l5ecu2z7ru.png'});}
  catch(e){toast('Erro ao enviar notificação: '+e.message,'err');}
}

function testarNotifDiaria(){
  const mes=getMes();
  const ind=mes.indenizacoes.filter(i=>!window.isFerG(i));
  const pend=ind.filter(i=>i.status==='PENDENTE'||i.status==='DEPENDE DO BOLETO').length;
  const total=ind.reduce((s,i)=>s+i.valorLiq,0);
  enviarNotificacaoLocal('AR4 – Resumo Diário',`${mesLabel(mesAtivo)}: ${ind.length} indenizações | ${window.brl(total)} | ${pend} pendentes`);
  toast('Notificação diária enviada!');
}

function testarNotifSemanal(){
  const brl=window.brl;
  const totalAcum=HIST.reduce((s,h)=>s+h.valor,0);
  enviarNotificacaoLocal('AR4 – Resumo Semanal',`Acumulado total: ${brl(totalAcum)} em ${HIST.length} meses`);
  toast('Notificação semanal enviada!');
}

// ══════════════ AGENDAMENTO ══════════════
function getAgendConfig(){
  try{return JSON.parse(localStorage.getItem('ar4_agend')||'{}');}catch(e){return {};}
}
function salvarAgendConfig(cfg){
  if(!cfg){
    // Read from DOM inputs
    cfg={
      diarioAtivo:document.getElementById('toggleDiario')?.checked||false,
      horaDiario:document.getElementById('horaDiario')?.value||'08:00',
      mesDiario:document.getElementById('mesDiario')?.value||mesAtivo,
      semanalAtivo:document.getElementById('toggleSemanal')?.checked||false,
      diaSemanal:document.getElementById('diaSemanal')?.value||'1',
    };
  }
  try{localStorage.setItem('ar4_agend',JSON.stringify(cfg));toast('Configuração salva!');}catch(e){}
}

function exportarCronogramaPDF(){
  const brl=v=>'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
  const mes=getMes();
  const ind=mes.indenizacoes.filter(i=>i.status!=='FERIADO').sort((a,b)=>a.data.localeCompare(b.data));
  const fdt=d=>d?new Date(d+'T00:00').toLocaleDateString('pt-BR'):'-';
  const rows=ind.map(i=>`
    <tr>
      <td style="padding:8px;border:1px solid #ddd;font-size:12px">${i.nome}</td>
      <td style="padding:8px;border:1px solid #ddd;font-size:12px;text-align:center">${fdt(i.data)}</td>
      <td style="padding:8px;border:1px solid #ddd;font-size:12px;text-align:center">${i.parcela||'—'}</td>
      <td style="padding:8px;border:1px solid #ddd;font-size:12px;text-align:center">${i.status}</td>
      <td style="padding:8px;border:1px solid #ddd;font-size:12px;text-align:right">${brl(i.valorLiq)}</td>
    </tr>`).join('');
  const total=ind.reduce((s,i)=>s+i.valorLiq,0);
  const html=`<!DOCTYPE html><html><head><meta charset="UTF-8"><title>Cronograma ${mesLabel(mesAtivo)}</title>
    <style>body{font-family:Arial,sans-serif;padding:20px}h1{font-size:20px}table{width:100%;border-collapse:collapse}th{background:#222;color:#fff;padding:8px;border:1px solid #ddd;font-size:12px}</style>
    </head><body>
    <h1>AR4 Proteção Veicular — Cronograma ${mesLabel(mesAtivo)}</h1>
    <p>Total: <strong>${brl(total)}</strong> | Pagamentos: <strong>${ind.length}</strong></p>
    <table><thead><tr><th>Associado</th><th>Data</th><th>Parcela</th><th>Status</th><th>Valor Líquido</th></tr></thead>
    <tbody>${rows}</tbody></table>
    <p style="margin-top:20px;font-size:11px;color:#999">Gerado em ${new Date().toLocaleString('pt-BR')}</p>
    </body></html>`;
  const blob=new Blob([html],{type:'text/html'});
  const url=URL.createObjectURL(blob);
  const a=document.createElement('a');
  a.href=url;a.download='cronograma-'+mesAtivo+'.html';a.click();
  URL.revokeObjectURL(url);
  toast('Cronograma exportado!');
}

// ══════════════ IMPORTAR ══════════════
function switchImportTab(tab){
  const tPdf=document.getElementById('tabPdf');
  const tTxt=document.getElementById('tabTexto');
  const pPdf=document.getElementById('painelPdf');
  const pTxt=document.getElementById('painelTexto');
  if(tab==='pdf'){
    if(tPdf)tPdf.classList.add('on');if(tTxt)tTxt.classList.remove('on');
    if(pPdf)pPdf.style.display='';if(pTxt)pTxt.style.display='none';
  } else {
    if(tTxt)tTxt.classList.add('on');if(tPdf)tPdf.classList.remove('on');
    if(pTxt)pTxt.style.display='';if(pPdf)pPdf.style.display='none';
  }
}


function processarTexto(){
  const txt=document.getElementById('textoInput')?document.getElementById('textoInput').value:'';
  if(!txt.trim()){toast('Cole o texto do cronograma primeiro.','err');return;}
  const preview=document.getElementById('textoPreview');
  if(!preview){toast('Erro: área de preview não encontrada.','err');return;}
  preview.id='textoPreview';
  preview.className='card au2';
  preview.style.marginTop='14px';
  parsePDFText(txt,preview);
}


function excluirMes(key){
  if(!confirm('Excluir todos os dados de '+mesLabel(key)+'?'))return;
  delete DB.meses[key];
  const idx=HIST.findIndex(h=>h.key===key);
  if(idx>=0)HIST.splice(idx,1);
  saveDB();
  const remaining=Object.keys(DB.meses).sort();
  if(remaining.length)mesAtivo=remaining[remaining.length-1];
  buildNav();toast('Mês excluído.');nav('backup');
}


function togglePassView(){
  const inp=document.getElementById('lpassword');
  const ic=document.getElementById('eyeIc');
  if(!inp)return;
  const show=inp.type==='password';
  inp.type=show?'text':'password';
  if(ic)ic.innerHTML=show
    ?'<path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/>'
    :'<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/>';
}

function restaurarBackup(input){
  const file=input.files[0];
  if(!file)return;
  const reader=new FileReader();
  reader.onload=e=>{
    try{
      const data=JSON.parse(e.target.result);
      if(!data.DB&&!data.meses){toast('Arquivo inválido.','err');return;}
      if(!confirm('Isso vai substituir TODOS os dados atuais. Confirma?'))return;
      const src=data.DB||data;
      if(src.meses)Object.assign(DB.meses,src.meses);
      if(src.nextId)DB.nextId=src.nextId;
      if(src.aprovacoes)DB.aprovacoes=src.aprovacoes;
      if(src.log)DB.log=src.log;
      if(data.HIST)HIST.splice(0,HIST.length,...data.HIST);
      saveDB();buildNav();
      toast('Backup restaurado com sucesso!');
      nav('dashboard');
    }catch(err){toast('Erro ao restaurar: '+err.message,'err');}
  };
  reader.readAsText(file);
}

// ══════════════ COMPROVANTE ══════════════
function handleComprovante(input){
  const file=input.files&&input.files[0];
  if(!file)return;
  if(file.size>2*1024*1024){toast('Arquivo muito grande (máx 2MB).','err');input.value='';return;}
  const reader=new FileReader();
  reader.onload=e=>{
    window._comprovante={nome:file.name,tipo:file.type,data:e.target.result};
    const lbl=document.getElementById('compLabel');
    if(lbl)lbl.innerHTML='<span style="color:#4ade80">✓ '+file.name+'</span>';
    toast('Comprovante anexado!');
  };
  reader.onerror=()=>toast('Erro ao ler o arquivo.','err');
  reader.readAsDataURL(file);
}
</script>
</body>
</html>
